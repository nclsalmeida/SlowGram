---
phase: 05-harness-hardening-device-validation
plan: 01
type: execute
wave: 1
depends_on:
  - 04-04
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
  - test/harness.js
autonomous: true
requirements:
  - HARN-01
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "CONFIG.harness exists in initConfig (deep-frozen with the rest of CONFIG) and holds maxBatchRecords: 200 — the 5k÷60fps≈83/frame claim with ~2.4× headroom (D-2); the value appears exactly once in the engine source, inside initConfig (CORE-05)"
    - "processBatch yields at the cap: it consumes at most CONFIG.harness.maxBatchRecords records per frame and retains the overflow in a module-level pending queue so the next frame continues — records are never dropped (D-2/D-4 finite-drain precondition)"
    - "A test handle SlowGram._batchState() exposes { lastFrameProcessed, pendingRecords } so suites can assert the per-frame cap and the drain state without timers or performance.now (D-1, SCAN-safe)"
    - "Churn is injected batch-style via FakeMutationObserver.record() (5000 records) and drained via FakeRAF.flush(); the rate is DERIVED as records ÷ frames × 60fps — never sampled wall time (D-3)"
    - "Two acceptance gates pass: (1) no frame processes more than maxBatchRecords (yield — 'never perceptible jank'), (2) the 5000-record churn drains in exactly ceil(5000/200)=25 frames (finite drain) (D-4)"
    - "node test/slowgram.test.js green with the new T-P suite; Edge headless harness.html shows TOTAL parity (Node − NodeOnly scans); no performance.now/setTimeout/setInterval entered the engine or the new tests (Phase 1 SCAN block stays green)"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
    - test/harness.js
  key_links:
    - "processBatch at src/slowgram.js:775-797 — the rAF-batch carrier; the yield inserts between takeRecords() and batchCallback(records); healthScan()/processStalls() tails stay per-frame"
    - "FakeMutationObserver.record(mutations) at test/harness.js:136-158 — the churn injection point; FakeRAF.flush() = one frame = one batch (harness.js:357-362)"
    - "pollLoop at src/slowgram.js:515-534 — the frame carrier; processBatch runs each frame on REELS"
    - "CONFIG.health.driftThreshold=5 at src/slowgram.js:344 — the CONFIG block shape to extend with the new harness sub-object"
  prohibitions:
    - statement: "No record is dropped at the cap — overflow is retained in the pending queue for the next frame; a slice-and-discard yield is a regression that breaks finite drain."
      status: unverified
      flagged: true
    - statement: "No performance.now / Date.now sampling, setTimeout or setInterval in the churn test path or the yield implementation — the SCAN block (Phase 1) must stay green."
      status: unverified
      flagged: true
    - statement: "The 200 cap value appears exactly once (in initConfig) — a duplicate literal anywhere in the engine is value drift (CORE-05)."
      status: unverified
      flagged: true
---

<objective>
Wave 1 tracer of the Phase 5 performance spine: the frozen `CONFIG.harness.maxBatchRecords` cap (CORE-05), the yield-at-cap mechanics in `processBatch` with a retained pending queue, the `SlowGram._batchState()` test handle, and the deterministic churn suite (5000 records injected batch-style, drained via rAF flush, rate derived records÷frames×60fps).

Purpose: prove the HARN-01 claim structurally — "5k mutations/s without perceptible jank (<1% CPU observer budget)" becomes two deterministic gates: no frame over the cap (yield) + finite-frame drain (25 frames for 5000 records at cap 200). No real profiler, no `performance.now` — the SCAN ban stays green. This is the tracer: the engine change, the test handle, and the gates land end-to-end in one commit, so the wall-clock/social/drift/kill-switch/on-device plans (waves 2-7) build on a proven batch spine.
Output: `src/slowgram.js` (CONFIG.harness + pending-queue yield + `_batchState` handle), `test/harness.js` (additive churn-injection helper), `test/slowgram.test.js` (T-P01..T-P06 churn suite).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-harness-hardening-device-validation/05-CONTEXT.md
@.planning/phases/05-harness-hardening-device-validation/05-RESEARCH.md
@.planning/phases/05-harness-hardening-device-validation/05-VALIDATION.md
@.planning/phases/05-harness-hardening-device-validation/05-PATTERNS.md
@.planning/phases/04-overlay-polish/04-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: CONFIG.harness.maxBatchRecords + pending-queue yield in processBatch + _batchState handle — the batch spine, end-to-end</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-1..D-4 (work-count proxy, cap 200, batch churn injection, two gates)
    - 05-RESEARCH.md — Pattern A (work-count as the jank proxy), Verified Facts (200 cap, 83/frame derivation), Pitfalls (single-frame drain as success)
    - 05-PATTERNS.md — §1 (processBatch excerpt at :775-797, yield insertion point, pending-queue mechanics)
    - src/slowgram.js — initConfig (327-367) for the CONFIG block shape; processBatch (:775-797); pollLoop (:515-534); the test-handle precedent (_getWatcherState :1890-1901, _setMutatingForTest :1903-1911)
    - test/harness.js — FakeMutationObserver (125-160), FakeRAF (357-362)
  </action>
  Create the tracer slice: one path — `init → REELS → 5000 records injected → frame-by-frame drain → every frame ≤ cap → drained in exactly 25 frames`. Production-quality, committed for keeps.

  src/slowgram.js — extend the IIFE (same file, same idioms, closure-private):
  - CONFIG addition in initConfig (deep-freeze covers it): `harness: { maxBatchRecords: 200 }` — the ONLY occurrence of `200` in this context; comment cites D-2 (5k÷60fps≈83/frame → ~2.4× headroom).
  - Module var near the batch machinery: `var pendingBatch = [];` — the overflow queue.
  - Rewrite `processBatch()` drain, PRESERVING the existing drift mutual-exclusion (when drifted, the scope source swaps to fallbackScope — that branch replaces the observer path that frame): (1) `var records = pendingBatch.concat(watcher.observer ? watcher.observer.takeRecords() : []); pendingBatch = [];` (2) if `records.length > CONFIG.harness.maxBatchRecords` → `pendingBatch = records.slice(CONFIG.harness.maxBatchRecords); records = records.slice(0, CONFIG.harness.maxBatchRecords);` — the yield (D-2). (3) the drifted branch keeps its existing source swap (fallbackRecords built from fallbackScope) and the same cap applies to whichever records path RUNS that frame — a helper `consume(records)` returning the ≤cap chunk (applied to the observer/pending path AND the fallback path) keeps it DRY; the pending queue serves the non-drift path, the fallback path is bounded by construction (no pending needed). (4) record `lastFrameProcessed = records.length` (the chunk actually fed to batchCallback) into a module var the handle reads. (5) healthScan()/processStalls() stay per-frame (unchanged).
  - `SlowGram._batchState()` test handle (precedent: _getWatcherState): returns `{ lastFrameProcessed, pendingRecords: pendingBatch.length }`.
  - No new timers; no performance.now/Date.now in the batch path — the Phase 1 SCAN block must stay green.

  test/slowgram.test.js — T-P tracer suite (appended after the Phase 4 suite):
  - T-P01: CONFIG.harness frozen with maxBatchRecords 200 (via getConfig — the TEST may assert the spec value).
  - T-P02: yield gate — freshEnv REELS, inject 5000 records (via FakeMutationObserver.instances last observer's record()), flush ONE frame → `_batchState().lastFrameProcessed === 200` and `pendingRecords === 4800` (never over the cap — D-4 gate 1).
  - T-P03: drain gate — continue flushing → after exactly 25 total frames `pendingRecords === 0` and every intermediate `lastFrameProcessed ≤ 200` (D-4 gate 2 — finite drain in ceil(5000/200)=25).
  - T-P04: no-drop invariant — records consumed across all frames sum to exactly 5000 (Σ lastFrameProcessed === 5000 — overflow retained, nothing lost).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "CONFIG.harness is frozen with maxBatchRecords 200 appearing exactly once in the engine (T-P01)"
    - "One frame processes exactly 200 of 5000 injected records and retains 4800 pending — the yield never exceeds the cap (T-P02)"
    - "The churn drains in exactly 25 frames with every frame ≤ 200 — finite drain proven (T-P03)"
    - "Σ per-frame processed === 5000 — the pending queue loses nothing (T-P04)"
    - "All prior suites (715 assertions) stay green; the Phase 1 no-timer/no-performance.now SCAN block passes with the new batch code present"
  </acceptance_criteria>
  <done>The batch spine is proven: frozen cap → yield with retained overflow → drain in exactly 25 frames — HARN-01's structural gates (yield + finite drain) are locked by tests, SCAN-safe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Churn-injection helper + derived-rate assertion + SCAN additions + two-host smoke</name>
  <files>test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-3 (rate derivation records÷frames×60fps), D-4
    - 05-RESEARCH.md — Pattern A, Pitfalls (sampled %CPU in CI)
    - test/harness.js — FakeMutationObserver (125-160) for the additive helper pattern (precedent: recordAttributeMutation added for Phase 1 without breaking suites)
    - test/slowgram.test.js — the existing SCAN block location (the no-timer/no-performance.now source scans) to extend additively
  </action>
  Complete the churn suite: the injection is ergonomic, the derived rate is asserted, the SCAN forbids the anti-patterns, and both hosts prove parity.

  test/harness.js (additive only):
  - `injectChurn(count)` helper — pushes `count` childList mutation records onto the LATEST FakeMutationObserver instance (`FakeMutationObserver.instances[last]`): `{ type: 'childList', addedNodes: [FakeElement('video')], target: null }` — mirrors what a real feed burst produces (registration-path records). Guard: only add if absent; existing suites untouched.

  test/slowgram.test.js (edge coverage):
  - T-P05: derived rate — drain 5000 records in 25 frames → assert `rate === 5000 / 25 * 60` (i.e. records ÷ frames × 60fps === 12000 records/SECOND derived — the DRAIN capacity the batch sustains). The assertion's semantics: the drain rate (200 records/frame × 60fps) is 2.4× the 5000/s incoming — exactly the ~2.4× headroom D-2 claims. The '5k/s' claim is stated via this derived number, never sampled.
  - T-P06: SCAN additions (Node-only) — extend the existing source-scan block: (a) `maxBatchRecords` value `200` appears exactly once in the engine source (inside initConfig); (b) no `performance.now` appears anywhere in src/slowgram.js or the new T-P block (string scan); (c) the no-timer scan (no setTimeout/setInterval) stays green.
  - T-P07: containment — a hostile observer.takeRecords() throwing → flush does NOT break the engine: init + REELS + inject + throw-stub takeRecords → pollLoop's try/catch contains it, getState() still reports phase/context.

  Two-host smoke (run both, record both results in the summary):
  - Node: `node test/slowgram.test.js` — exit 0, all suites green (report the new total).
  - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\slow-edge-smoke5" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count (Node count − NodeOnly scans; the parity contract from Phases 1-4).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "injectChurn is additive in harness.js and drives the same registration path the real observer produces (T-P02..T-P05 use it)"
    - "The 5k/s claim is derived (records ÷ frames × 60fps), never sampled — T-P05 asserts the derivation"
    - "SCAN forbids duplicate cap values, performance.now, and timers in the new code (T-P06)"
    - "A throwing takeRecords is contained — the engine survives (T-P07)"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The churn suite is complete and deterministic on both hosts: batch injection, derived-rate assertion, anti-pattern SCAN, containment — HARN-01's '<1% CPU' claim is locked structurally, SCAN-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observer records → processBatch | the MutationObserver delivers untrusted record streams (a hostile page, or an aggressive feed) into the per-frame batch; the yield is the DoS boundary |
| engine batch path → host page | a batch failure (throwing takeRecords/records) must be contained — never escapes the pollLoop try/catch |
| CONFIG.harness value → engine behavior | the cap is frozen config; a duplicate literal or a drifted value silently changes the jank ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | processBatch under churn — unbounded per-frame work = perceptible jank | high | mitigate | yield-at-cap with retained pending queue; two gates (no frame over cap + finite drain) locked by T-P02/T-P03 |
| T-05-02 | Denial of Service | record loss at the cap (slice-and-discard) silently under-registers videos | medium | mitigate | pending queue retains overflow; T-P04 asserts Σ processed === injected |
| T-05-03 | Tampering | cap value drift (200 duplicated as a literal elsewhere) | low | mitigate | value only in CONFIG.harness; T-P06 source scan forbids duplicates |
| T-05-04 | Spoofing | sampled %CPU / performance.now gate passes flakily in CI | medium | mitigate | structural work-count proxy only (D-1); T-P06 scan forbids performance.now in the path |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior 715 + new T-P suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- Yield proven by T-P02; finite drain in exactly 25 frames by T-P03; no-drop by T-P04; derived rate by T-P05; SCAN by T-P06; containment by T-P07
</verification>

<success_criteria>
- HARN-01's structural claim is locked: 5k mutations/s churn drains with no frame over the 200 cap and completes in exactly 25 frames — 'without perceptible jank' proven deterministically, SCAN-safe, dual-host
- The batch spine (cap → yield → pending queue) is production-quality and committed; waves 2-7 build on it
- No timer, no performance.now, no profiler — the Phase 1 no-timer contract survives the phase's first engine change
</success_criteria>

<output>
Create `.planning/phases/05-harness-hardening-device-validation/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `CONFIG.harness.maxBatchRecords` — frozen config (200)
- `pendingBatch` — module-level overflow queue (closure-private)
- `processBatch()` yield — the ≤cap consume with retained overflow (modified existing function)
- `SlowGram._batchState()` — test handle `{ lastFrameProcessed, pendingRecords }`
- `injectChurn(count)` — additive harness helper (test/harness.js)
- T-P01..T-P07 — the churn test suite (test/slowgram.test.js)
