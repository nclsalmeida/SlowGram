---
phase: 05-harness-hardening-device-validation
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - HARN-05
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "CONFIG.killSwitch exists in initConfig (deep-frozen with the rest of CONFIG) holding enabled: true — the master flag, read once at init into a module latch (D-12; the flag value appears exactly once in the engine, inside initConfig — CORE-05)"
    - "The latch is consulted at the cheapest, highest-leverage gate points: pollLoop (stops accumulation within one frame), batchCallback (no processing), registerVideo (no tracking), overlaySync + overlay handlers (no render) — every entry point no-ops when killed (D-12)"
    - "Disabling REVERTS: revertAll() + overlay teardown → the native feed is restored immediately (D-13 — a paused engine that leaves degradation applied is the failure mode being prohibited); accumulation stops and re-enabling resumes FRESH (D-13 — reversible, ≠ destroy)"
    - "A test handle SlowGram._setKillSwitchForTest(v) flips the latch (precedent: _setMutatingForTest/_setBufferEnabled/_setOverlayHostForTest); the latch resets per init — a flipped flag does not survive re-init (precedent: bufferEnabled at src/slowgram.js:1803)"
    - "The flip test passes: disable → the NEXT flush does zero work (no accumulation, no lever application, no overlay render); re-enable → accumulation resumes (D-14)"
    - "No new timers entered the engine — the kill-switch is synchronous latch checks, SCAN-safe; node test/slowgram.test.js green with the new T-K suite; Edge headless harness.html shows TOTAL parity"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "CONFIG block at src/slowgram.js:327-367 — the killSwitch sub-object joins here (frozen)"
    - "pollLoop at :515-534 — gate point 1: `if (state.destroyed) return;` gains the latch check (one frame stop)"
    - "batchCallback at :810 — gate point 2: returns before registration"
    - "registerVideo at :557 — gate point 3: returns before tracking"
    - "overlaySync at :1547 / overlayTeardown at :1564 — gate point 4: no render when killed; teardown on disable"
    - "revertAll at :1265 (:1944 public) — the D-13 REVERT call on disable"
    - "Test-handle precedent: _setMutatingForTest (:1903-1911), bufferEnabled reset per init (:1803)"
  prohibitions:
    - statement: "Disable must REVERT, never PAUSE — a paused engine leaves degradation applied (a stuck feed); revertAll + overlay removal are mandatory on the disable path (D-13)."
      status: unverified
      flagged: true
    - statement: "The kill-switch adds no new timers or listeners — it is synchronous latch checks at existing gate points; the Phase 1 SCAN block must stay green."
      status: unverified
      flagged: true
    - statement: "The enabled flag appears exactly once (in initConfig) — a second literal `killSwitch` value anywhere is value drift (CORE-05)."
      status: unverified
      flagged: true
---

<objective>
Wave 5 — the kill-switch (HARN-05): a master flag that turns the engine off INSTANTLY and REVERTS the feed to native. The tracer: frozen `CONFIG.killSwitch.enabled` read once into a module latch, consulted at the four cheapest gate points (pollLoop → one-frame stop, batchCallback → no processing, registerVideo → no tracking, overlay → no render), with `SlowGram._setKillSwitchForTest` as the test handle. Disabling reverts (revertAll + overlay teardown — the feed is native immediately, not paused with degradation stuck); re-enabling resumes fresh; the flip test proves the next flush does zero work and accumulation resumes.

Purpose: the one genuinely NEW engine mechanism of the phase — the safety valve a user (or a support runbook) pulls when the degradation itself becomes the problem. It must be instant (≤1 frame), total (every entry point), reversible (≠ destroy), and timer-free.
Output: `src/slowgram.js` (CONFIG.killSwitch + latch + gate checks + test handle), `test/slowgram.test.js` (T-K01..T-K08).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-harness-hardening-device-validation/05-CONTEXT.md
@.planning/phases/05-harness-hardening-device-validation/05-RESEARCH.md
@.planning/phases/05-harness-hardening-device-validation/05-VALIDATION.md
@.planning/phases/05-harness-hardening-device-validation/05-PATTERNS.md
@.planning/phases/05-harness-hardening-device-validation/05-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: CONFIG.killSwitch + module latch + the four gate points + _setKillSwitchForTest handle — the off-switch spine, end-to-end</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-12 (flag + latch + gate points + ≤1 frame + no new timers), D-13 (REVERT not pause), D-14 (flip test)
    - 05-RESEARCH.md — Pattern E (kill-switch gate points), Pitfalls (kill-switch that pauses instead of reverting)
    - 05-PATTERNS.md — §1 (latch precedent bufferEnabled :103, gate-point precedent pollLoop :515-534, test-handle precedent :1903-1921)
    - src/slowgram.js — initConfig (:327-367), pollLoop (:515-534), batchCallback (:810), registerVideo (:557), overlaySync (:1547), overlayTeardown (:1564), revertAll (:1265), the bufferEnabled reset per init (:1803), test-handle block (:1873-1935)
  </action>
  Create the off-switch tracer: one path — `init → REELS phase 3 (degraded) → kill → next frame: zero work + feed reverted → re-enable → accumulates again`. Production-quality, committed for keeps.

  src/slowgram.js — extend the IIFE (same file, same idioms, closure-private):
  - CONFIG addition in initConfig (deep-freeze covers it): `killSwitch: { enabled: true }` — the ONLY occurrence of the killSwitch value; comment cites D-12/D-13.
  - Module var near the other latches (:103 area): `var killSwitchEnabled = CONFIG.killSwitch.enabled;` — read once at module load; per-init reset (see Task 2) mirrors bufferEnabled (:1803).
  - The disable path (D-13): a `disableKillSwitch()` helper — sets the latch false, then calls `revertAll()` + `overlayTeardown()` synchronously (the REVERT-not-pause contract: the feed is native and the overlay is gone the moment the flag flips). The next frame is already clean.
  - Gate checks (synchronous, no timers):
    1. pollLoop (:515-534): gate the WORK, never the heartbeat — inside the `if (state.running && env.requestAnimationFrame)` block, wrap `tick()`/`refresh()`/`processBatch()` in `if (killSwitchEnabled) { ... }` and keep `env.requestAnimationFrame(pollLoop)` UNCONDITIONAL. A plain `return` would kill the loop and re-enable could never resume (breaks D-13/D-14); the one-frame stop comes from tick() not running.
    2. batchCallback (:810): at the top of the try, `if (!killSwitchEnabled) { return; }` before the mutating check — no processing.
    3. registerVideo (:557): at the top, `if (!killSwitchEnabled) { return; }` — no tracking.
    4. overlaySync (:1547): the predicate gains `&& killSwitchEnabled` — no render when killed (overlayTeardown runs in the disable path, not here).
  - `SlowGram._setKillSwitchForTest(v)` — TEST-ONLY handle: `v === false` runs `disableKillSwitch()` (latch off + revert + teardown), `v === true` flips the latch back on (resume). Doc comment follows the _setMutatingForTest precedent.
  - No new timers, no new listeners, no performance.now — SCAN stays green.

  test/slowgram.test.js — T-K tracer suite (appended after the Phase 4 suite):
  - T-K01: CONFIG.killSwitch frozen with enabled true (via getConfig — the TEST may assert the spec value); the value appears exactly once in the engine source (Node-only source scan, CORE-05).
  - T-K02: kill stops the poll — freshEnv REELS, accumulate to phase 3, `_setKillSwitchForTest(false)` → flush → getState().elapsedMs UNCHANGED (the next frame did zero accumulation work — D-12 one-frame stop).
  - T-K03: kill stops registration — with the latch off, inject a mutation (new video via the observer) + flush → _registrySize() UNCHANGED (batchCallback/registerVideo no-op — D-12).
  - T-K04: kill reverts the feed — phase 3 with a degraded video (levers applied per getRegistryState) → `_setKillSwitchForTest(false)` → flush → getRegistryState shows the video NATIVE (revertAll ran on disable — D-13, the REVERT-not-pause contract) AND _overlayState().hostExists === false (overlay teardown).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "CONFIG.killSwitch is frozen with enabled true, appearing exactly once in the engine (T-K01)"
    - "The next frame after the kill does zero accumulation work — elapsed is frozen (T-K02)"
    - "No new video registers while killed — batchCallback/registerVideo no-op (T-K03)"
    - "Disabling REVERTS: the degraded video is native and the overlay is gone — the feed is restored, not paused (T-K04)"
    - "All prior suites stay green; the Phase 1 SCAN block (no timers/performance.now) passes with the gate checks present"
  </acceptance_criteria>
  <done>The off-switch spine is proven: frozen flag → latch → four gate points → instant stop (one frame) + full revert (native feed, no overlay) — HARN-05's core is locked, SCAN-safe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-enable freshness + per-init reset + gate-point completeness + two-host smoke</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-13 (re-enable resumes fresh; reversible ≠ destroy), D-14 (flip → next flush zero work; re-enable → accumulates again)
    - 05-PATTERNS.md — §1 (bufferEnabled per-init reset precedent :1803)
    - src/slowgram.js — the init reset block (:1795-1830) where bufferEnabled is re-seeded per init
  </action>
  Complete the kill-switch suite: re-enable resumes fresh, the latch resets per init, the gate coverage is total, and both hosts prove parity.

  src/slowgram.js (additions only):
  - Per-init reset: in the init reset block (where `bufferEnabled = CONFIG.buffer.enabled;` lives, ~:1803), add `killSwitchEnabled = CONFIG.killSwitch.enabled;` — a flipped _setKillSwitchForTest flag does not survive re-init (D-13 reversible semantics, precedent-blessed).

  test/slowgram.test.js (edge coverage):
  - T-K05: re-enable resumes FRESH — after T-K04's revert, `_setKillSwitchForTest(true)` + flush → accumulation resumes (elapsedMs advances again) BUT the session is fresh: no legacy degradation auto-reapplies (getRegistryState shows native until a NEW phase-3 accumulation re-applies levers via the normal path) — D-13 'resumes fresh, reversible, ≠ destroy'.
  - T-K06: per-init reset — flip the latch off, call init() again (freshEnv) → the latch is true (CONFIG.killSwitch.enabled re-seeded) — the flipped flag did not survive (precedent mirror: the T-* buffer test).
  - T-K07: gate-point completeness scan (Node-only) — the four gate points each consult the latch: slice each function's source body (pollLoop, batchCallback, registerVideo, overlaySync) from the source text and assert each body references `killSwitchEnabled` (source-level slice+scan, the same technique the Phase 1-4 SCAN blocks use for occurrence counting) — a NEW entry point added later without the gate is caught by this scan.
  - T-K08: kill under churn — with 5000 records pending (injectChurn from plan 05-01) and the latch OFF, flush → _batchState().lastFrameProcessed === 0 (the batch no-ops entirely — no processing under kill even under load; D-12 total stop).
  Two-host smoke (run both, record both results in the summary):
  - Node: `node test/slowgram.test.js` — exit 0, all suites green (report the new total).
  - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\slow-edge-smoke5e" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Re-enabling resumes accumulation with a fresh session — no legacy degradation auto-reapplies (T-K05)"
    - "The latch re-seeds from CONFIG on re-init — the flip does not survive (T-K06)"
    - "All four gate points consult the latch — the completeness scan passes (T-K07)"
    - "Kill under churn: even with 5000 pending records, the batch processes zero (T-K08)"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The kill-switch is complete: instant total stop (even under churn), full revert to native, fresh re-enable, per-init reset, and a completeness scan guarding future entry points — HARN-05 locked, dual-host green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kill-switch latch → every engine entry point | the latch is the single authority; a gate point missed = the engine keeps working while the user believes it is off |
| disable path → live feed | revertAll + overlay teardown mutate the live DOM — the revert must restore native exactly |
| test handle → latch | _setKillSwitchForTest is the only writer; the latch must re-seed per init so a test flip never leaks into production sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-16 | Denial of Service | kill leaves degradation applied (pause-not-revert — stuck feed) | high | mitigate | D-13 REVERT: revertAll + overlay teardown on disable; T-K04 asserts native + no overlay |
| T-05-17 | Spoofing | a gate point missed — engine keeps working while the user believes it is off | high | mitigate | four gate points + completeness scan T-K07; kill-under-churn T-K08 proves total stop |
| T-05-18 | Tampering | test flip leaks into a production session (latch survives re-init) | medium | mitigate | per-init re-seed from CONFIG (T-K06), mirroring the bufferEnabled precedent (:1803) |
| T-05-19 | Spoofing | kill-switch flag value drifts (duplicate literal) | low | mitigate | value only in CONFIG.killSwitch; T-K01 source scan forbids duplicates |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior suites + new T-K suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- Stop-by-T-K02; no-registration by T-K03; revert by T-K04; fresh-resume by T-K05; per-init reset by T-K06; gate completeness by T-K07; churn stop by T-K08
</verification>

<success_criteria>
- HARN-05 is locked: the master flag turns the engine off within one frame, reverts the feed to native (never pauses with degradation stuck), and resumes fresh on re-enable — reversible, total, timer-free, dual-host proven
- The completeness scan guards the future: any new entry point that forgets the latch fails the suite
- The one genuinely new engine mechanism of the phase ships production-quality with a full flip-test contract
</success_criteria>

<output>
Create `.planning/phases/05-harness-hardening-device-validation/05-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `CONFIG.killSwitch.enabled` — frozen master flag (true)
- `killSwitchEnabled` — module latch (closure-private)
- Four gate checks — pollLoop, batchCallback, registerVideo, overlay predicate (modified existing functions)
- `SlowGram._setKillSwitchForTest(v)` — test handle
- T-K01..T-K08 — the kill-switch suite (test/slowgram.test.js)
