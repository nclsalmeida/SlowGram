---
phase: 05-harness-hardening-device-validation
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - test/slowgram.test.js
  - .planning/phases/05-harness-hardening-device-validation/05-DRIFT-RUNBOOK.md
autonomous: true
requirements:
  - HARN-04
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A versioned real-DOM snapshot fixture exists (test/fixtures/instagram-shapes.js — the loggedOut/loggedIn Instagram DOM dumps captured 2026-08-15) and a test walks CONFIG.selectors against it, FAILING FIRST in CI with the MISSING SELECTOR NAME when Instagram changes the DOM (D-10 — the test fails first, not production)"
    - "A refresh runbook exists (05-DRIFT-RUNBOOK.md) documenting the process: capture a fresh dump, update the fixture, re-run the suite — the human loop that keeps the snapshot versioned (D-10)"
    - "The existing selectorHealth N=5 drift detection (CONFIG.health.driftThreshold=5, src/slowgram.js:344) is promoted to a FIRST-CLASS test that drives the refresh loop: when the fixture test fails it reports WHICH selector missed and points to the runbook (D-11 — no silent drift)"
    - "No runtime re-scan was added — the engine still depends on exactly the 3 CONFIG.selectors; the drift guard is the fixture test + the existing N=5 health (D-10 cost/network rationale)"
    - "node test/slowgram.test.js green with the new T-D suite; Edge headless harness.html shows TOTAL parity"
  artifacts:
    - test/slowgram.test.js
    - .planning/phases/05-harness-hardening-device-validation/05-DRIFT-RUNBOOK.md
  key_links:
    - "CONFIG.selectors at src/slowgram.js:341-344 — video / [role=main] / [role=dialog] — the ONLY selectors the engine may query (DETC-06 registry)"
    - "CONFIG.health.driftThreshold=5 at src/slowgram.js:344 — the N=5 zero-hit-scan threshold before drift is declared"
    - "healthScan at src/slowgram.js:845-852 — the per-batch selector health accounting (drift declaration emits the drift event exactly once per episode)"
    - "test/fixtures/instagram-shapes.js — the versioned real-DOM dumps (loggedOut/loggedIn shapes, tagged with source)"
    - "test/fixtures/instagram-mock.js — the buildSocialRoute builder (the fixture walk covers the logged-in shape too)"
  prohibitions:
    - statement: "No runtime re-scan of the live DOM was added — the drift guard is the versioned fixture test + the existing N=5 health; a new periodic scanner adds cost and network for a 3-selector dependency (D-10)."
      status: unverified
      flagged: true
    - statement: "A health check without the fixture test is insufficient — drift would stay silent until users complain; the fixture test MUST fail first with the missing selector NAMED (D-11)."
      status: unverified
      flagged: true
---

<objective>
Wave 4 — drift resistance as a first-class guarantee (HARN-04). The drift machinery already exists (Phase 2: selectorHealth, N=5 zero-hit scans → drift declaration, fail-soft prod fallback). This plan makes it provable: the versioned real-DOM snapshot fixture (test/fixtures/instagram-shapes.js) is walked by a test that fails FIRST in CI with the missing selector NAME when Instagram changes its DOM; the existing N=5 health check is promoted to a first-class test that feeds the refresh loop; and a refresh runbook (05-DRIFT-RUNBOOK.md) documents the human loop that keeps the snapshot current. No runtime re-scan — the engine's 3-selector dependency (DETC-06) stays lean.

Purpose: test-only plan (no engine change — the health machinery already exists). HARN-04's deliverable is the fail-first guard + the runbook that makes drift cheap to fix and impossible to ignore.
Output: `test/slowgram.test.js` — T-D01..T-D06 drift suite + `05-DRIFT-RUNBOOK.md`.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-harness-hardening-device-validation/05-CONTEXT.md
@.planning/phases/05-harness-hardening-device-validation/05-RESEARCH.md
@.planning/phases/05-harness-hardening-device-validation/05-VALIDATION.md
@.planning/phases/05-harness-hardening-device-validation/05-PATTERNS.md
@.planning/phases/05-harness-hardening-device-validation/05-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: The versioned fixture walk — every CONFIG.selector found in the real-DOM snapshot, failing first with the missing selector named</name>
  <files>test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-10 (versioned snapshot + refresh runbook), D-11 (health check feeds refresh)
    - 05-RESEARCH.md — Pattern D (versioned snapshot + health feedback), Pitfalls (drift that fails silently in prod)
    - 05-PATTERNS.md — §2 (drift excerpt: walk CONFIG.selectors against instagram-shapes.js)
    - test/fixtures/instagram-shapes.js — the fixture structure (loggedOut/loggedIn shapes, tags, attach wrapper)
    - src/slowgram.js — CONFIG.selectors (:341-344), the DETC-06 registry comment
  </action>
  Create the fixture-walk tracer: one pass — `CONFIG.selectors → every shape in instagram-shapes.js → each selector resolves → else fail with the selector NAME and a runbook pointer`.

  test/slowgram.test.js — T-D tracer suite (appended after the Phase 4 suite):
  - T-D01: fixture integrity — instagram-shapes.js exposes BOTH shapes (loggedOut AND loggedIn), each tagged with its source, and each shape contains at least one <video> element (the engine's core dependency).
  - T-D02: selector walk — for EACH shape, for EACH of CONFIG.selectors (video, [role=main], [role=dialog]): assert the selector resolves to ≥ 1 element in the shape's DOM. On failure the assert message MUST include the selector name (e.g. `drift: selector '[role=main]' not found in instagram-shapes.js loggedIn shape`) AND a pointer to the runbook (05-DRIFT-RUNBOOK.md) — the fail-first, named, actionable guard (D-10/D-11).
  - T-D03: logged-in realism — the loggedIn shape carries the video loop attribute (02-RESEARCH.md Pitfall 6 — fake-vs-live divergence: logged-out dumps show no loop/autoplay; logged-in videos carry loop) — assert the shapes don't blur (each tagged shape keeps its own evidence).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Both fixture shapes are present, tagged, and contain <video> (T-D01)"
    - "Every CONFIG.selector resolves in every shape; a miss fails with the selector NAME and the runbook pointer (T-D02)"
    - "The logged-out/logged-in shapes are distinguishable by their evidence (loop attribute) — no blur (T-D03)"
    - "All prior suites stay green"
  </acceptance_criteria>
  <done>The fail-first guard is locked: Instagram changes a selector → the suite breaks in CI with the selector named and the refresh path pointed at — production never discovers drift first.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Health-check promotion + refresh runbook + two-host smoke</name>
  <files>test/slowgram.test.js, .planning/phases/05-harness-hardening-device-validation/05-DRIFT-RUNBOOK.md</files>
  <read_first>
    - 05-CONTEXT.md — D-11 (health check feeds the refresh loop)
    - 05-RESEARCH.md — Pattern D
    - src/slowgram.js — healthScan (:845-852), the drift declaration path (missStreak, driftThreshold 5, the drift bus event)
    - test/slowgram.test.js — the existing drift suites (Phase 2 T-* drift tests) to extend without duplication
  </action>
  Complete the drift suite: the N=5 health machinery proven as a first-class test that feeds refresh, the runbook that makes the loop executable, and the dual-host proof.

  test/slowgram.test.js (edge coverage):
  - T-D04: N=5 drift declaration — freshEnv REELS with a DOM where the anchor selector is missing: flush 4 batches → health NOT drifted (missStreak < 5); flush the 5th → drifted === true AND the drift bus event fired exactly once (the transition-guarded declaration at :845-852) — the N=5 threshold proven to the scan.
  - T-D05: health-recovery — after drift, restore the anchor (add the selector element) → next batches recover → drifted === false AND the recovery event fired — the drift loop is bidirectional (D-11 feeding the refresh: declare → fix → recover).
  - T-D06: fixture-drift linkage — a mutation of the fixture (temporarily rename a selector key in a copy) makes T-D02 fail with the selector name — the health check and the fixture walk are the SAME guarantee from two sides (D-11: the health test feeds the refresh loop).

  05-DRIFT-RUNBOOK.md — write the refresh runbook (the D-10 human loop):
  - When: T-D02 fails in CI (or health declares drift in prod logs).
  - Steps: (1) identify the missing selector from the assert message; (2) capture a FRESH dump of the current Instagram DOM (logged-out and logged-in, /reels/ with a playing video); (3) update test/fixtures/instagram-shapes.js preserving the tagged-shape structure; (4) re-run `node test/slowgram.test.js`; (5) if a selector genuinely changed, update CONFIG.selectors ONLY via the DETC-06 registry discipline (never a hardcoded query) and note the change in STATE.md; (6) commit the fixture + any selector change together with the runbook note.

  Two-host smoke (run both, record both results in the summary):
  - Node: `node test/slowgram.test.js` — exit 0, all suites green (report the new total).
  - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\slow-edge-smoke5d" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Drift declares at exactly the 5th zero-hit batch, once, and recovers when the anchor returns (T-D04/T-D05)"
    - "The fixture walk and the health check are the same guarantee from two sides — a broken fixture fails the named-selector assert (T-D06)"
    - "05-DRIFT-RUNBOOK.md exists with the capture/update/re-run/commit loop (D-10)"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The drift suite is complete: N=5 declaration proven to the scan, bidirectional recovery, fixture linkage, and an executable runbook — HARN-04's fail-first guard + refresh loop are locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Instagram DOM → CONFIG.selectors | the engine's 3-selector dependency is the drift surface; a changed selector silently breaks detection/registration |
| fixture snapshot → CI | the versioned dump is the drift oracle — it must stay current with the real DOM or the guard itself lies |
| health machinery → prod behavior | the N=5 declaration switches the engine to the fail-soft fallback scope — declared correctly or the feed degrades |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Tampering | selector drift — Instagram changes the DOM, engine silently misses videos | high | mitigate | versioned fixture walk T-D02 fails first, named; N=5 health T-D04 declares; runbook makes the fix cheap |
| T-05-14 | Spoofing | stale fixture — the guard passes against an outdated dump while prod drifts | medium | mitigate | fixture tagged + source-dated (T-D01); runbook step 2 captures fresh dumps; logged-in/out evidence distinct (T-D03) |
| T-05-15 | Denial of Service | health machinery oscillates (declare/recover churn) | low | mitigate | transition-guarded single declaration per episode (T-D04 asserts exactly-once); recovery test T-D05 |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior suites + new T-D suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- Fixture walk by T-D01/T-D02/T-D03; N=5 declaration by T-D04; recovery by T-D05; fixture linkage by T-D06; runbook file exists
</verification>

<success_criteria>
- HARN-04's guard is locked: selector drift fails FIRST in CI with the selector named, the N=5 health check is a first-class bidirectional test, and the refresh runbook makes the fix a documented loop
- No runtime re-scan — the engine stays lean (3 selectors, DETC-06), and the guard is the fixture + health tests
- Test-only plan (plus a doc): zero engine changes, zero new dependencies, both hosts green
</success_criteria>

<output>
Create `.planning/phases/05-harness-hardening-device-validation/05-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- T-D01..T-D06 — the drift suite (test/slowgram.test.js)
- `05-DRIFT-RUNBOOK.md` — the versioned snapshot refresh runbook (capture → update fixture → re-run → commit)
