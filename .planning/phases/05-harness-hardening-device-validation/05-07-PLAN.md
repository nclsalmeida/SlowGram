---
phase: 05-harness-hardening-device-validation
plan: 07
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - test/slowgram.test.js
  - test/harness.html
autonomous: true
requirements:
  - HARN-07
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "A formal dual-host parity test exists: the SAME test file (test/slowgram.test.js) runs under node:test/assert on Node AND in the plain browser page test/harness.html (zero test dependencies — no framework, no npm — HARN-07)"
    - "test/harness.html loads the SAME engine file (src/slowgram.js) + the SAME harness (test/harness.js) + the SAME tests via <script> tags — a single source of truth, no duplicated logic between hosts (the parity contract from Phases 1-4 formalized)"
    - "A Node-side assertion (Node-only scan) verifies harness.html's script-load structure: it references ../src/slowgram.js and ../test/harness.js — the browser host cannot drift to a different engine copy (structure-pinned)"
    - "The parity claim is a first-class assertion: a test documents that the browser host runs the identical suite (Node count − NodeOnly-scan count === browser host count) — the 'zero test dependencies' claim is provable, not asserted by convention"
    - "node test/slowgram.test.js green with the new T-H suite; Edge headless harness.html shows TOTAL parity with the full Phase 5 suite (the final dual-host gate of the milestone)"
  artifacts:
    - test/slowgram.test.js
    - test/harness.html
  key_links:
    - "test/harness.html — the existing browser runner page (Phase 1): <script> load order engine → harness → tests, renderResults pass/fail table"
    - "test/harness.js — the FakeClock/FakeDocument/FakeMutationObserver/FakeRAF/assert runner shared verbatim by both hosts"
    - "test/slowgram.test.js — the single suite file; Node-only scans are the known, deliberate divergence (e.g. source-scan assertions using node:fs)"
    - "The two-host smoke recipe used every wave (Edge headless --dump-dom harness.html → TOTAL parity) — this plan formalizes it as a first-class test artifact"
  prohibitions:
    - statement: "The browser host must never carry its own copy of the engine or tests — harness.html loads src/slowgram.js + test/harness.js + test/slowgram.test.js by path; a duplicated/modified copy breaks the single-source-of-truth contract (structure-pinned by the Node scan)."
      status: unverified
      flagged: true
    - statement: "No test framework or npm dependency may enter the project — node:test/assert + the homemade assert runner are the whole stack (CORE-04 zero-dependency constraint, HARN-07)."
      status: unverified
      flagged: true
---

<objective>
Wave 7 — the final formalization (HARN-07): the dual-host parity that every wave has relied on becomes a FIRST-CLASS, provable claim. The same suite runs on Node and in a plain browser page (test/harness.html) with zero test dependencies — no framework, no npm. The plan pins the structure (harness.html loads the engine + harness + tests by path — a Node scan catches any drift to a copied engine), documents the parity contract as a test, and runs the full-phase two-host smoke as the milestone's closing gate.

Purpose: HARN-07 was true by convention since Phase 1 (Node 715 / Edge 667 parity observed every wave); this plan makes it a tested, structure-pinned guarantee — the 'same engine file, same mocks, plain browser page, zero dependencies' claim ships as evidence, not anecdote.
Output: `test/slowgram.test.js` (T-H01..T-H04), `test/harness.html` (a title/note addition pinning the single-source contract).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-harness-hardening-device-validation/05-CONTEXT.md
@.planning/phases/05-harness-hardening-device-validation/05-RESEARCH.md
@.planning/phases/05-harness-hardening-device-validation/05-VALIDATION.md
@.planning/phases/05-harness-hardening-device-validation/05-PATTERNS.md
@.planning/phases/05-harness-hardening-device-validation/05-06-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: The parity suite — structure-pinned harness.html + the formal zero-dependency claim + the full-phase two-host gate</name>
  <files>test/slowgram.test.js, test/harness.html</files>
  <read_first>
    - 05-CONTEXT.md — the HARN-07 decisions (from the requirements + discussion: same engine file, plain browser page, zero test deps)
    - 05-RESEARCH.md — Pattern F tail / Runtime State Inventory (the dual-host claim), Validation Architecture item 3 (harness.html as the seed)
    - 05-PATTERNS.md — §5 (harness.html reference)
    - test/harness.html — the current page (script load order, renderResults)
    - test/slowgram.test.js — how the Node-only scans are marked (the known divergence pattern to reuse for the structure-pin scan)
  </action>
  Create the parity suite: the browser host is structure-pinned to the single source of truth, the zero-dependency claim is a test, and the full-phase two-host smoke is the closing gate.

  test/harness.html (addition only):
  - Add a header comment near the top documenting the single-source contract: this page loads src/slowgram.js + test/harness.js + test/slowgram.test.js verbatim by path; it must NEVER embed a copy of the engine or tests (HARN-07). Also add a page <title> line the structure-scan asserts.

  test/slowgram.test.js — T-H suite (appended after the Phase 4 suite; the final suite of the phase):
  - T-H01 (Node-only scan): structure pin — read test/harness.html and assert its <script> tags reference `../src/slowgram.js` and `../test/harness.js` and `../test/slowgram.test.js` (the exact relative paths) — the browser host cannot drift to a copied engine without this test breaking (single source of truth).
  - T-H02 (Node-only scan): zero test dependencies — assert no package.json exists at the project root (or that it declares no test framework: the check is 'no node_modules test framework import in the suite' — the simplest honest assert: `require('fs').existsSync('package.json') === false` OR the suite's require surface is only node:test/node:assert + local files). The 'no framework, no npm' claim is provable, not asserted by convention.
  - T-H03: parity contract documented — a Node-only test that reads the harness runner's TOTAL line convention (renderResults format) and asserts it matches the Node suite's assertion count minus the NodeOnly-marked scans — the exact parity arithmetic Phases 1-4 used, now a first-class assertion with a doc comment.
  - T-H04: full-suite smoke helper — a Node-only test that shells the Edge headless command (the exact recipe from every wave) and asserts the harness.html dump contains `TOTAL: N passed / N run` with N === expected (Node count − NodeOnly scans). If Edge is unavailable in the environment (CI without Edge), the test SKIPS with a clear message (env-gated, never a false failure) — the dual-host proof runs where Edge exists, and the structure pins (T-H01/T-H02) hold everywhere.

  Two-host smoke (the milestone's closing gate — run both, record both results in the summary):
  - Node: `node test/slowgram.test.js` — exit 0, ALL suites green (report the final total, which includes every Phase 1-5 suite).
  - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\slow-edge-smoke5f" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count (Node − NodeOnly; the HARN-07 parity).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "harness.html's script tags are structure-pinned to the shared paths — a copied engine in the browser host breaks T-H01"
    - "The zero-dependency claim is a test: no test framework/npm in the project per T-H02"
    - "The parity arithmetic is first-class: T-H03 documents and asserts the Node−NodeOnly === browser-host equation"
    - "The Edge smoke is env-gated (skips cleanly without Edge) and green where Edge exists — the closing dual-host gate (T-H04)"
    - "node test/slowgram.test.js exits 0 with the FULL phase suite; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The parity claim is first-class: structure-pinned single source, provable zero-dependency, documented arithmetic, env-gated full smoke — HARN-07 locked as the milestone's closing gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser host → shared suite | harness.html must load the same engine/harness/tests by path — a copy drifting from the single source silently breaks the parity contract |
| project root → dependency surface | the zero-dependency claim is a trust boundary — a framework entering the project invalidates HARN-07 and CORE-04 |
| environment → dual-host proof | the Edge smoke needs Edge installed; the gate must be env-aware (skip, never false-fail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24 | Tampering | browser host drifts to a copied engine — parity becomes fiction | high | mitigate | structure pin T-H01 (script paths asserted); single-source contract documented in harness.html |
| T-05-25 | Tampering | a test framework/npm dep enters the project silently | medium | mitigate | zero-dependency assert T-H02; no install task exists in any Phase 5 plan |
| T-05-26 | Spoofing | parity arithmetic asserted by convention instead of test | medium | mitigate | T-H03 documents + asserts the Node−NodeOnly === browser-host equation |
| T-05-27 | DoS | CI without Edge false-fails the dual-host gate | low | mitigate | T-H04 env-gates the smoke (skip with message when Edge is absent) |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the FULL phase suite (every Phase 1-5 suite, the final total)
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump — the HARN-07 parity (Node − NodeOnly === browser-host count)
- Structure pins by T-H01/T-H02; arithmetic by T-H03; env-gated smoke by T-H04
</verification>

<success_criteria>
- HARN-07 is a first-class, provable claim: the same engine file runs under mocks in a plain browser page with zero test dependencies — structure-pinned, arithmetic-tested, and closed by the full-phase dual-host smoke
- The milestone's validation story is complete: every wave's two-host run was the same guarantee, now formalized as the closing gate
- Zero new dependencies, zero new engine surface — the phase ends as it began: pure validation hardening
</success_criteria>

<output>
Create `.planning/phases/05-harness-hardening-device-validation/05-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- T-H01..T-H04 — the parity suite (test/slowgram.test.js)
- test/harness.html — single-source-contract header + title (modified)
