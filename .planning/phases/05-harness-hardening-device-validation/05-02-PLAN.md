---
phase: 05-harness-hardening-device-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - test/slowgram.test.js
autonomous: true
requirements:
  - HARN-02
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "TWO first-class wall-clock tests exist: counting equivalence (elapsedMs === visible delta; the hidden period contributes zero) AND reset equivalence (a gap > fatigueWindowMs resets — the engine does not continue from where it stopped) (D-5)"
    - "Each test runs BOTH scenarios: the normal visibility driver (setVisibility + dispatch 'visibilitychange') AND the WebView missed-event case (hidden WITHOUT the event — the engine recovers via the Phase 1 hiddenAt=null → lastBoundary fallback) (D-6)"
    - "Each test asserts BOTH the real-clock delta (fake clock advanced by a known visible delta → elapsedMs equals exactly that delta) AND the state invariants (hiddenAt/lastBoundary/elapsedMs in the expected states after each transition) (D-7)"
    - "The tests are deterministic: fake clock + visibility driver + rAF flush only — no setTimeout/Date.now/performance.now in the test path (Phase 1 SCAN stays green)"
    - "node test/slowgram.test.js green with the new T-W suite; Edge headless harness.html shows TOTAL parity"
  artifacts:
    - test/slowgram.test.js
  key_links:
    - "The engine clock: env.clock.now() DI-injected (Phase 1) — the fake clock IS the 'real clock' for the test; advance(ms) moves time, rAF.flush() ticks the poll"
    - "The visibility driver: FakeDocument.setVisibility + dispatchEvent('visibilitychange') (test/harness.js:23-58) — the normal-flow scenario; the WebView case calls setVisibility WITHOUT dispatchEvent"
    - "Phase 1 hiddenAt=null → lastBoundary fallback — the missed-event recovery the WebView scenario exercises (src/slowgram.js lifecycle)"
    - "Fatigue reset: fatigueWindowMs 300000 (CONFIG, src/slowgram.js:328) — the reset-equivalence gap threshold; prior suites T23/T24 already exercise reset, this plan formalizes them as first-class named tests"
  prohibitions:
    - statement: "No wall-clock test uses real timers (setTimeout/setInterval) or sampled time (Date.now/performance.now) — the Phase 1 SCAN ban is absolute; flakiness in CI is the failure mode being prohibited."
      status: unverified
      flagged: true
    - statement: "A single scenario (normal flow only) is insufficient — the WebView missed-event case is where 'the session lies' would hide; both scenarios are required (D-6)."
      status: unverified
      flagged: true
---

<objective>
Wave 2 — the wall-clock truth suite (HARN-02): "the session never lies" becomes two first-class tests with dedicated hidden-period fixtures. Counting equivalence proves the engine accumulates exactly the visible delta (hidden contributes zero); reset equivalence proves a gap > the 5-min fatigue window resets the session (it does not continue from where it stopped). Both run in the normal visibility flow AND the WebView missed-event case (hidden without the event — the Phase 1 `hiddenAt=null → lastBoundary` fallback), each with the dual assert: real-clock delta vs. accumulation + state invariants.

Purpose: this is a test-only plan (no engine change) — the engine already implements the accumulation/reset semantics (Phases 1-2, T23/T24). HARN-02 formalizes them as named, first-class guarantees with explicit equivalence asserts and the hardest scenario (missed event) included, so the 'never lies' promise is locked for the milestone.
Output: `test/slowgram.test.js` — T-W01..T-W08 wall-clock suite.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-harness-hardening-device-validation/05-CONTEXT.md
@.planning/phases/05-harness-hardening-device-validation/05-RESEARCH.md
@.planning/phases/05-harness-hardening-device-validation/05-VALIDATION.md
@.planning/phases/05-harness-hardening-device-validation/05-PATTERNS.md
@.planning/phases/05-harness-hardening-device-validation/05-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Counting equivalence — normal flow + WebView missed-event, with the dual assert (delta + invariants)</name>
  <files>test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-5, D-6, D-7 (two tests, both scenarios, dual assert)
    - 05-RESEARCH.md — Pattern B (fake-clock wall equivalence), Pitfalls (wall-clock test with real timers)
    - 05-PATTERNS.md — §2 (fake clock + visibility driver excerpt)
    - test/slowgram.test.js — the existing T23/T24 reset/visibility suites to mirror the driver pattern; freshEnv/advance/flush helpers
    - test/harness.js — FakeDocument.setVisibility/dispatchEvent (23-58), FakeClock.advance
    - src/slowgram.js — the hiddenAt/lastBoundary fallback (Phase 1 lifecycle) and the getState surface { elapsedMs, phase, context, visible, hiddenAt, lastBoundary }
  </action>
  Create the counting-equivalence suite: the engine's accumulated time equals exactly the visible real-clock delta, and the hidden period contributes zero — in both scenarios, with state invariants asserted alongside the number.

  test/slowgram.test.js — T-W tracer suite (appended after the Phase 4 suite):
  - Helper: `hiddenPeriod(doc, env, ms, withEvent)` — hides via `doc.setVisibility('hidden')` (+ dispatchEvent('visibilitychange') when withEvent), advances the fake clock by ms, restores visible (with event), flushes. Returns nothing — the driver pattern only.
  - T-W01 (normal flow, counting): freshEnv REELS visible → advance(120000) + flush → record elapsedA → hiddenPeriod(60000, withEvent=true) → advance(30000) + flush → assert elapsedMs === 120000 + 30000 (the 60s hidden contributed ZERO) AND `hiddenAt` was set during the hidden period / `lastBoundary` advanced past it (invariant half of D-7).
  - T-W02 (WebView missed-event, counting): same as T-W01 but hiddenPeriod(withEvent=false) — setVisibility('hidden') WITHOUT the event → advance(60000) → setVisibility('visible') WITHOUT the event → advance(30000) + flush → assert elapsedMs === 150000 (the engine's Phase 1 fallback recovered: no unverifiable hidden time counted) AND the hiddenAt/lastBoundary invariants hold through the missed-event path.
  - T-W03: delta-exactness — the elapsed delta equals the VISIBLE segment to the millisecond (no rounding, no off-by-frame): advance(123456) visible → elapsedMs === 123456 (exact).
  - T-W04: invariant matrix — after hidden → hiddenAt set and running false; after small visible gap (< fatigueWindowMs) → gap discounted (elapsed continues, no reset); after hidden → visible with NO gap (immediate resume) → no discontinuity in lastBoundary. Each asserted as the D-7 mechanism proof.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Counting equivalence holds in the normal flow: a 60s hidden period contributes exactly zero to elapsedMs (T-W01)"
    - "Counting equivalence holds in the WebView missed-event case: hidden WITHOUT the event still contributes zero (T-W02)"
    - "The elapsed delta matches the visible segment exactly, to the millisecond (T-W03)"
    - "hiddenAt/lastBoundary/elapsedMs invariants are asserted alongside each delta — the mechanism is proven, not just the number (T-W04)"
    - "All prior suites stay green; no timer or sampled-time API entered the test path"
  </acceptance_criteria>
  <done>The counting half of HARN-02 is locked: elapsedMs equals the visible real-clock delta exactly, hidden time contributes zero in both the normal and missed-event scenarios, with the state mechanism proven.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reset equivalence — both scenarios + formalization of the existing T23/T24 reset coverage</name>
  <files>test/slowgram.test.js</files>
  <read_first>
    - 05-CONTEXT.md — D-5 (reset equivalence: gap > window resets), D-6, D-7
    - 05-RESEARCH.md — Pattern B, Pitfalls
    - test/slowgram.test.js — the existing T23/T24 suites (the informal reset coverage this plan formalizes as first-class)
    - src/slowgram.js — the fatigue reset path (:504 revertAll on reset) and fatigueWindowMs 300000 (:328)
  </action>
  Formalize the reset half: a gap longer than the 5-min fatigue window resets the session — the engine does not continue from where it stopped — in both scenarios, with the delta + invariant dual assert.

  test/slowgram.test.js (edge coverage):
  - T-W05 (normal flow, reset): REELS visible, accumulate 180000 → hiddenPeriod(360000, withEvent=true) → visible + advance(60000) + flush → assert elapsedMs === 60000 ONLY (the 6-min gap reset the session; pre-gap 180s did not survive — 'the session never lies' means it also never pretends continuity) AND the invariants show a fresh lastBoundary (reset happened).
  - T-W06 (WebView missed-event, reset): same but hiddenPeriod(withEvent=false) — a 6-min hidden period without the event → resume → elapsedMs === visible-only post-resume time; the fallback path still honors the reset.
  - T-W07: boundary — a gap of exactly fatigueWindowMs (300000) does NOT reset (strict > comparison — CONFIG comment at :328: 'strict > comparison lands in Plan 03'); a gap of 300001 does. The fatigue window boundary is asserted to the millisecond.
  - T-W08: reset side-effect — on reset, revertAll restores every live video to native (elapsed reset + applied levers cleared — assert via getRegistryState that a previously degraded video is native after the reset; the LEVR-07 path at :504).
  Two-host smoke (run both, record both results in the summary):
  - Node: `node test/slowgram.test.js` — exit 0, all suites green (report the new total).
  - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\slow-edge-smoke5b" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "A >5-min hidden gap resets the session in the normal flow: only post-resume visible time accumulates (T-W05)"
    - "The same reset holds in the WebView missed-event case (T-W06)"
    - "The fatigue boundary is exact: 300000 does NOT reset, 300001 does (T-W07)"
    - "Reset restores native condition — a previously degraded video is native after the reset (T-W08)"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The reset half of HARN-02 is locked: gap > 5-min window resets (never continues), at the exact boundary, in both scenarios, with the native-restore side effect proven — 'the session never lies' is a first-class guarantee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document visibility events → engine clock | the visibility driver (event or missed-event) is the only input that distinguishes 'verifiable' from 'unverifiable' time; a wrong handling = the session lies |
| clock → elapsedMs | the injected clock is the test's 'real clock'; the assert compares engine accumulation against it directly |
| hidden period → session state | hiddenAt/lastBoundary transitions are the mechanism that must never be bypassed (especially the missed-event fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Spoofing | hidden time counted as visible (the session lies — the exact HARN-02 failure) | high | mitigate | counting equivalence in both scenarios (T-W01/T-W02); hidden contributes zero; delta asserted to the millisecond (T-W03) |
| T-05-06 | Spoofing | session pretends continuity across a >5-min gap (no reset) | high | mitigate | reset equivalence in both scenarios (T-W05/T-W06) + exact boundary (T-W07) |
| T-05-07 | Tampering | state invariants drift while the number still passes (mechanism rot) | medium | mitigate | dual assert — delta AND hiddenAt/lastBoundary/elapsedMs invariants in every test (D-7, T-W04) |
| T-05-08 | DoS | flaky wall-clock tests (real timers/sampled time) erode CI trust | medium | mitigate | fake clock + rAF flush only; SCAN forbids timers/Date.now/performance.now in the test path |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior suites + new T-W suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- Counting proven by T-W01/T-W02/T-W03; invariants by T-W04; reset by T-W05/T-W06/T-W07; native-restore by T-W08
</verification>

<success_criteria>
- HARN-02's promise is a first-class guarantee: the session counts exactly the verifiable visible time (never more, never across a fatigue reset), in the normal flow AND the WebView missed-event case, with the mechanism proven by state invariants
- The existing informal T23/T24 coverage is promoted to named, dual-assert first-class tests with exact boundary assertions
- Test-only plan: zero engine changes, zero new dependencies, both hosts green
</success_criteria>

<output>
Create `.planning/phases/05-harness-hardening-device-validation/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `hiddenPeriod(doc, env, ms, withEvent)` — test helper (test/slowgram.test.js)
- T-W01..T-W08 — the wall-clock equivalence suite (test/slowgram.test.js)
