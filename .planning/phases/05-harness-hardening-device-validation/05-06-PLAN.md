---
phase: 05-harness-hardening-device-validation
plan: 06
type: execute
wave: 6
depends_on:
  - 05-05
files_modified:
  - device-check.html
  - .planning/phases/05-harness-hardening-device-validation/05-DEVICE-CHECKLISTS.md
autonomous: false
requirements:
  - HARN-06
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "device-check.html exists — a standalone zero-dependency page (no framework, no npm — same constraint as HARN-07/demo.html) running the REAL engine (src/slowgram.js) against the REAL DOM of the device browser (not the harness fakes) (D-15)"
    - "The page displays the live engine verdict (context/phase/elapsed) AND the frozen clamp tables from CONFIG (webkit/chromium playbackRate/volume limits) so the human can verify the platform clamps render on the actual device (D-15 — items map 1:1 to existing guarantees)"
    - "Per-platform markdown checklists exist (05-DEVICE-CHECKLISTS.md) covering iOS Safari, Android Chrome, AND Android WebView — three distinct surfaces (iOS Chrome is WKWebView, Android WebView ≠ Chrome Android — RESEARCH Pattern F) (D-15)"
    - "Every checklist item maps 1:1 to an existing engine guarantee: clamps displayed, iOS filter visual, volume audibility, 6-min background reset, social preservation + overlay (D-15 / HARN-06 list)"
    - "Variance across platforms is ANNOTATED, not a hard gate — the checklist records per-device results with notes where a surface differs (D-15 / Pattern F: WebView variance is expected, human-visual judgment decides)"
    - "node test/slowgram.test.js stays green (no regression from this plan — it is a human-validation deliverable, not a suite change); the checklists and page are committed for the on-device run"
  artifacts:
    - device-check.html
    - .planning/phases/05-harness-hardening-device-validation/05-DEVICE-CHECKLISTS.md
  key_links:
    - "CONFIG.clampTables at src/slowgram.js:344-363 — webkit/chromium playbackRate/volume limits the page displays (frozen)"
    - "demo.html — the existing zero-dependency browser page driving src/slowgram.js (the device-check.html analog; Phase 4 preview precedent)"
    - "CONFIG leverParams at src/slowgram.js:350-363 — the per-phase saturation/playbackRate/volume values the checklist verifies audibly/visually"
    - "The 6-min reset — fatigueWindowMs 300000 (CONFIG :328) — the background-reset checklist item"
    - "The overlay pill — CONFIG.overlay (Phase 4) — the 'overlay visible on REELS ≥ 3min, absent on social/fullscreen' checklist item"
  prohibitions:
    - statement: "device-check.html must run the REAL engine against the REAL device DOM — a fake-DOM page cannot validate iOS filter rendering or WebView variance (D-15, RESEARCH Pitfall 'on-device checklist without real DOM')."
      status: unverified
      flagged: true
    - statement: "Platform variance is annotated, never gated — the checklist records what each surface does; it does not fail a device because its rendering differs (D-15 / Pattern F)."
      status: unverified
      flagged: true
---

<objective>
Wave 6 — on-device validation (HARN-06), the HUMAN checkpoint of the phase. Two deliverables: `device-check.html` — a standalone zero-dependency page the user opens on each real device (iOS Safari, Android Chrome, Android WebView) that runs the REAL engine against the REAL DOM, shows the live verdict and the frozen clamp tables, and drives the visual/audible checks; and `05-DEVICE-CHECKLISTS.md` — the per-platform checklist where every item maps 1:1 to an existing engine guarantee (clamps displayed, iOS filter rendering, volume audibility, 6-min background reset, social preservation + overlay).

Purpose: the milestone's validation is only honest when it happens on the surfaces the product ships on. RESEARCH Pattern F confirmed the variance (iOS Chrome = WKWebView ≠ Android Chrome ≠ Android WebView; CSS filter on video renders inconsistently across WebKit) — so the checklist is human-visual on real devices, with variance annotated, never gated.
Output: `device-check.html` + `05-DEVICE-CHECKLISTS.md`. NOTE: this plan is `autonomous: false` — the checklist execution is a human run on physical devices; the plan produces the page + checklists, and the checklist RECORDS are filled during the on-device run (the phase verification records the run).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-harness-hardening-device-validation/05-CONTEXT.md
@.planning/phases/05-harness-hardening-device-validation/05-RESEARCH.md
@.planning/phases/05-harness-hardening-device-validation/05-VALIDATION.md
@.planning/phases/05-harness-hardening-device-validation/05-PATTERNS.md
@.planning/phases/05-harness-hardening-device-validation/05-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: device-check.html — the standalone real-DOM validation page</name>
  <files>device-check.html</files>
  <read_first>
    - 05-CONTEXT.md — D-15 (device-check.html with REAL DOM)
    - 05-RESEARCH.md — Pattern F (on-device reality), Pitfalls (fake-DOM page)
    - 05-PATTERNS.md — §4 (device-check.html role + demo.html analog)
    - demo.html — the existing standalone page pattern (script load order, driver, zero deps) to mirror
    - src/slowgram.js — CONFIG.clampTables (:344-363) and CONFIG.leverParams (:350-363) — the values the page displays
  </action>
  Create the page the human opens on each real device — the REAL engine, REAL DOM, live verdict, and the clamp tables that make the checks verifiable.

  device-check.html (new file, zero dependencies, no npm/framework — mirrors demo.html):
  - Loads src/slowgram.js (the REAL engine) plus the browser fixtures (test/fixtures/*.js) via <script> — no harness fakes, no node require: the page runs in the device browser against the REAL DOM.
  - Live verdict panel: init SlowGram on the page; display getState() (context, phase, elapsedMs) updating on the bus events (contextchange/phasechange/elapsed) — the human sees the engine's real verdict while browsing the real feed.
  - Clamp display: render the frozen clamp tables from CONFIG (getConfig().clampTables — webkit/chromium playbackRate/volume limits) AND the leverParams per phase — the "clamps displayed" checklist item is verifiable against what the device actually does.
  - Checklist driver section: for each checklist item (see 05-DEVICE-CHECKLISTS.md), the page shows the current state that feeds it — e.g. the overlay pill visibility (the page shows whether the overlay host exists via the same predicate), the current playbackRate/volume of the active video, the elapsed-to-phase mapping. The human judges the visual/audible outcome; the page shows the engine-side truth.
  - Zero-dependency constraint: no external CSS/JS/fonts — everything inline; the page must work from file:// (double-tap to open on the device).
  - Containment: the page is a validation tool — it must never break the device feed; all slowgram calls are the engine's own contained surface.
  </action>
  <verify>
    <automated>open device-check.html in a browser and confirm it renders the verdict panel + clamp tables without console errors</automated>
  </verify>
  <acceptance_criteria>
    - "device-check.html loads src/slowgram.js + browser fixtures only — no harness fakes, no node/require, no framework, no external assets (file://-openable)"
    - "The page shows the live engine verdict (context/phase/elapsed from getState) and the frozen clamp tables from CONFIG (webkit + chromium playbackRate/volume)"
    - "The page renders without console errors and never mutates the feed beyond the engine's own surface"
    - "node test/slowgram.test.js stays green (no suite regression from this new file)"
  </acceptance_criteria>
  <done>device-check.html exists — the human opens it on each real device and sees the engine's live truth + the clamp contract, ready to drive the checklist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 05-DEVICE-CHECKLISTS.md — the per-platform checklist mapping 1:1 to engine guarantees</name>
  <files>.planning/phases/05-harness-hardening-device-validation/05-DEVICE-CHECKLISTS.md</files>
  <read_first>
    - 05-CONTEXT.md — D-15 (markdown checklist per platform, items map 1:1)
    - 05-RESEARCH.md — Pattern F (three surfaces, variance annotated), Verified Facts (iOS Chrome = WKWebView)
    - 05-PATTERNS.md — §4
    - 04-* phases — the guarantees the checklist verifies: clamps (Phase 3 D-21/D-22), iOS filter (Phase 3 D-15), volume (Phase 3 D-24), 6-min reset (Phase 1 fatigue), social preservation (Phase 2 trust), overlay (Phase 4)
  </action>
  Write the checklist document the human executes on each device — per-platform sections, every item mapped to the guarantee it validates, variance annotated.

  05-DEVICE-CHECKLISTS.md (new file):
  - Header: purpose (on-device validation of the complete SlowGram engine), the three surfaces (iOS Safari, Android Chrome, Android WebView — noting iOS Chrome IS WKWebView and Android WebView ≠ Chrome Android), and the 'annotate, don't gate' rule (Pattern F).
  - For EACH platform (3 sections, identical item set):
    1. Clamps displayed — open device-check.html; confirm the clamp tables (webkit/chromium) render and the device honors them (e.g. playbackRate beyond the platform cap is clamped by the browser — note the actual behavior).
    2. iOS filter visual — a real Reel degrades to phase 1: the saturation filter on the wrapper is visually imperceptible-to-gradual (the D-15 wrapper-not-video mitigation); record what the surface renders (known WebKit variance on video filters).
    3. Volume audibility — phase 3: the volume lever lowers the video audibly (record audible/not).
    4. 6-min background reset — background the tab 6+ minutes (iOS throttling), return: elapsed reset to post-resume (the session never lies — HARN-02's guarantee on-device).
    5. Social preservation — navigate to /direct/ (and one more preserved route): no degradation, no overlay, native feed (HARN-03 on-device).
    6. Overlay pill — REELS ≥ 3 min: the 'N min' pill appears bottom-left, non-interactive; absent on social/fullscreen (Phase 4 OVER-01..03).
  - Result table per platform: item | expected | observed | variance note. Variance is ANNOTATED (a note on what the surface did differently), never a fail gate.
  - A 'How to run' block: load device-check.html from file://, load the engine into the feed (bookmarklet/userscript), execute each item, record observed + variance notes.
  </action>
  <verify>
    <automated>file exists with all three platform sections, each with the 6 items mapped to their guarantee IDs (D-21/D-22, D-15, D-24, HARN-02, HARN-03, OVER-01..03)</automated>
  </verify>
  <acceptance_criteria>
    - "05-DEVICE-CHECKLISTS.md has three platform sections (iOS Safari / Android Chrome / Android WebView) with the identical 6-item set"
    - "Every item carries its guarantee reference (clamps D-21/D-22, filter D-15, volume D-24, reset HARN-02, social HARN-03, overlay OVER-01..03)"
    - "A variance-note column exists and the doc states the annotate-don't-gate rule (Pattern F)"
    - "The 'How to run' block is executable by a human with a phone + a browser"
  </acceptance_criteria>
  <done>The on-device checklist is written: three surfaces, six guarantees each, every item mapped to its engine contract, variance annotated — ready for the human device run that completes HARN-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device page → engine | device-check.html runs the REAL engine in the REAL device DOM — the page must never break the feed (engine containment) |
| device platform → validation truth | the three surfaces render differently (WebKit/Chromium/WebView) — the checklist records variance, it does not gate on it |
| checklist record → milestone claim | the filled checklist is the on-device evidence for HARN-06 — items must map 1:1 to guarantees or the record proves nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-20 | Spoofing | fake-DOM validation passes while real-device rendering fails (iOS filter / WebView variance) | high | mitigate | device-check.html runs the REAL engine on the REAL DOM (D-15); human-visual judgment on physical devices |
| T-05-21 | Tampering | checklist items don't map to guarantees — the record proves nothing | medium | mitigate | every item carries its guarantee ID (D-21/D-22, D-15, D-24, HARN-02, HARN-03, OVER-01..03) |
| T-05-22 | Denial of Service | device-check.html breaks the feed it validates | medium | mitigate | engine containment (all slowgram calls use the contained surface); page is file://-safe, zero deps |
| T-05-23 | Spoofing | platform variance misread as failure (a device 'fails' because WKWebView renders differently) | low | mitigate | annotate-don't-gate rule stated in the doc header and enforced by the variance-note column |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- `node test/slowgram.test.js` stays green (this plan is a human-validation deliverable — no suite change)
- device-check.html renders the verdict + clamp tables in a real browser with no console errors
- 05-DEVICE-CHECKLISTS.md has the three platform sections, six mapped items each, and the variance-note rule
- The on-device RUN (human, physical devices) is recorded in the phase verification report (05-VERIFICATION.md) as the HARN-06 evidence
</verification>

<success_criteria>
- HARN-06's deliverables exist: the real-DOM device page and the per-platform checklist where every item maps 1:1 to an engine guarantee
- The milestone's validation is honest — the checklist runs on the actual surfaces the product ships on, with variance annotated not gated
- The human device run is the phase's checkpoint (autonomous: false) and feeds the final verification report
</success_criteria>

<output>
Create `.planning/phases/05-harness-hardening-device-validation/05-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `device-check.html` — the standalone real-DOM validation page (root)
- `05-DEVICE-CHECKLISTS.md` — the per-platform on-device checklist (3 surfaces × 6 mapped items)
