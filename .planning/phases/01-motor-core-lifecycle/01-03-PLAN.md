---
phase: 01-motor-core-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - CORE-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "a background gap strictly greater than CONFIG.fatigueWindowMs (300000) resets the session: elapsedMs becomes 0, phase becomes 0, 'reset' event emitted"
    - "a background gap exactly equal to fatigueWindowMs (300000) does NOT reset (strict > comparison)"
    - "a background gap less than or equal to fatigueWindowMs does NOT accumulate — elapsedMs is unchanged by the background period (gap discounted: lastBoundary=now, hiddenAt=null)"
    - "all four resume signals trigger the same catch-up: document visibilitychange→visible, document resume, window pageshow with persisted===true, window focus"
    - "window pageshow with persisted===false is ignored (initial load / same-window navigation)"
    - "the fatigue window value comes from CONFIG.fatigueWindowMs, never a literal in the handler"
    - "reset() emits exactly one 'reset' event and getState() shows elapsedMs 0, phase 0, context preserved"
  artifacts:
    - src/slowgram.js (FatigueManager: onHidden, onResume, resetSession, bindLifecycle full 4-signal wiring)
    - test/slowgram.test.js (fatigue suite)
  key_links:
    - "catch-up base: state.hiddenAt when set, else state.lastBoundary — delta = now - base, clamped at 0"
    - "resetSession() calls phaseMachine.sync(0) which emits phasechange 0 when coming from a higher phase"
    - "lifecycle event targets: visibilitychange/resume → document; pageshow/focus → window (Chrome Page Lifecycle API)"
  prohibitions:
    - statement: "The session clock never counts unverifiable time: background/hidden gaps are never accumulated, even when within the fatigue window (gap discounted, lastBoundary=now)."
      status: unverified
      flagged: true
    - statement: "A fatigue reset always emits the observable 'reset' event — accumulated session state is never silently discarded."
      status: unverified
      flagged: true
---

<objective>
Wave 3: the FatigueManager — record `hiddenAt` on any hidden signal, and on ANY of four resume signals compute the wall-clock catch-up delta; strictly greater than the 5-minute fatigue window resets the session, otherwise the gap is discounted (never counted as watch time).

Purpose: the session clock must never "lie" across backgrounding (HARN-02 spirit) and must survive WebView/Chrome-frozen-tab resumes that skip `visibilitychange` (Pitfall 3). No timer ever runs in the background.
Output: FatigueManager logic + full 4-signal lifecycle wiring in `src/slowgram.js`, fatigue suite in `test/slowgram.test.js`.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-motor-core-lifecycle/01-RESEARCH.md
@.planning/phases/01-motor-core-lifecycle/01-PATTERNS.md
@.planning/phases/01-motor-core-lifecycle/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FatigueManager core — hiddenAt, strict >5min reset, gap discount (CORE-03)</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js (the file being modified — current minimal bindLifecycle/visibility handler from Plan 01, CONFIG with fatigueWindowMs from Plan 02)
    - 01-RESEARCH.md — Pattern 3 (FatigueManager, lines 223–241), Pitfall 3 (visibilitychange-only reset, 322–326), Pitfall 5 (accumulating unverifiable time, 334–338), Pitfall 7 (NTP jumps, 346–350), Open Question 2 (strict > vs >=, 464–467)
    - 01-PATTERNS.md — lines 107–121 (FatigueManager verbatim), 284–287 (Shared Pattern 6 — fatigue catch-up)
  </read_first>
  <behavior>
    - Test 1: strict reset — setContext('REELS'), advance(180000) + flush (3 min elapsed), doc.setVisibility('hidden') + dispatchEvent(visibilitychange), clock.advance(300001) (5min+1ms gap), doc.setVisibility('visible') + dispatchEvent(visibilitychange) → getState().elapsedMs === 0 AND phase === 0 AND one 'reset' event emitted
    - Test 2: exact boundary does NOT reset — same setup but gap exactly 300000 → no 'reset' event, elapsedMs back to pre-background value (gap discounted, not accumulated)
    - Test 3: short gap does NOT reset and does NOT accumulate — gap 60000 (1 min) → elapsedMs unchanged from pre-background value (the hidden minute is never counted)
    - Test 4: no hiddenAt fallback — dispatch a resume signal (window focus) with hiddenAt still null (missed visibilitychange — WebView case) → uses lastBoundary as catch-up base, no crash, no reset when delta small
    - Test 5: negative delta clamp — resume with clock stepped backwards (advance(-10000) since hidden) → delta clamped to 0, no reset, no accumulation
    - Test 6: reset preserves context — after reset, getState().context still 'REELS' (reset zeroes time, not context)
  </behavior>
  <action>
    Replace the Plan 01 minimal visibility handler in src/slowgram.js with the real FatigueManager per RESEARCH.md Pattern 3:

    - `onHidden()` — `state.hiddenAt = env.clock.now()`; `state.visible = false`; `updateRunning()` (clock pauses — rAF poll self-stops because running is false). No tick needed on hidden beyond the boundary refresh already done by the caller.
    - `onResume()` — `var now = env.clock.now(); var since = state.hiddenAt !== null ? state.hiddenAt : state.lastBoundary;` (fallback base per RESEARCH.md:232 — missed-visibilitychange case); `var delta = now - since;` if `delta < 0` clamp to 0 (Pitfall 7 NTP back-step); `if (delta > CONFIG.fatigueWindowMs)` → `resetSession()` and return (strict `>`, RESEARCH.md:467 — exactly 300000 does NOT reset); else → discount the gap: `state.hiddenAt = null; state.lastBoundary = now;` (never accumulate unverifiable time, Pitfall 5). Then `state.visible = true; updateRunning(); tick(now)`. Idempotent: with hiddenAt null and delta ≤ window it just refreshes lastBoundary — safe on repeated resume signals.
    - `resetSession()` — `state.elapsedMs = 0; state.hiddenAt = null; state.visible = true; state.lastBoundary = env.clock.now(); phaseMachine.sync(0);` (sync emits phasechange 0 when coming from a higher phase — Plan 02's sync handles the 0 boundary identically); `emit('reset');` then `updateRunning()`. Context is preserved (reset zeroes time, not context — behavior Test 6).
    - All handlers wrapped in try/catch (containment, T-01-01).
    - Read the window ONLY from `CONFIG.fatigueWindowMs` — no 300000 literal in handlers.

    test/slowgram.test.js — add the fatigue suite with the six behavior tests. Drive via FakeDocument.setVisibility + dispatchEvent and FakeWindow.dispatchEvent with plain event-like objects (`{ type: 'pageshow', persisted: true/false }`, `{ type: 'focus' }`, `{ type: 'resume' }`). Count 'reset' emissions via SlowGram.on('reset', cb). Compute the window from `SlowGram.getConfig().fatigueWindowMs` — never a literal (RESEARCH.md:463).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with the fatigue suite green (all six tests)
    - `src/slowgram.js` contains `onHidden`, `onResume`, and `resetSession` functions
    - `src/slowgram.js` onResume compares `delta > CONFIG.fatigueWindowMs` (strict >, no >=)
    - `src/slowgram.js` onResume discount path sets `state.lastBoundary = now` and `state.hiddenAt = null`
    - `src/slowgram.js` resetSession sets elapsedMs 0, calls `phaseMachine.sync(0)`, emits 'reset', preserves context
    - `rg -n "fatigueWindowMs" src/slowgram.js` shows the handler reading CONFIG.fatigueWindowMs (not a literal)
  </acceptance_criteria>
  <done>FatigueManager core proven: >5min gap resets, ≤5min gap discounts and never accumulates, hiddenAt-missed fallback works, reset is observable via the 'reset' event.</done>
</task>

<task type="auto">
  <name>Task 2: Full 4-signal lifecycle wiring + pageshow persisted guard (CORE-03)</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js (the file being modified — FatigueManager core from Task 1, current bindLifecycle)
    - 01-RESEARCH.md — Common Operation 3 (lifecycle listener registration with correct targets, lines 398–409), Pitfall 8 (pageshow fires on initial load too, 352–355), Assumption A3 (document resume signal, 453)
    - 01-PATTERNS.md — lines 156–165 (lifecycle binding verbatim), 297–302 (Shared Pattern 9 — event target mapping)
  </read_first>
  <behavior>
    - Test 1: document 'resume' signal triggers catch-up — advance gap > fatigueWindowMs, dispatchEvent({type:'resume'}) on the DOCUMENT → session resets
    - Test 2: window 'pageshow' with persisted:true triggers catch-up — same gap → resets
    - Test 3: window 'pageshow' with persisted:false does NOT trigger catch-up — same gap → no reset (initial load guard)
    - Test 4: window 'focus' triggers catch-up — same gap → resets
    - Test 5: document 'visibilitychange'→visible triggers catch-up (already covered in Task 1 Test 1 — re-assert here as the fourth signal contract)
    - Test 6: destroy() removes all four lifecycle listeners — after SlowGram.destroy(), dispatching any resume signal leaves state untouched (no listener leak)
  </behavior>
  <action>
    Replace the Plan 01 minimal `bindLifecycle(env)` with the full four-signal wiring per RESEARCH.md Common Operation 3 (event targets locked — Chrome Page Lifecycle API):

    - `document.addEventListener('visibilitychange', onVisibilityChange)` — `if (env.visibilityState() === 'hidden') onHidden(); else onResume();`
    - `document.addEventListener('resume', onResume)` (Chrome 68+ frozen→active; A3)
    - `window.addEventListener('pageshow', function (e) { if (e.persisted) onResume(); })` — the persisted guard is mandatory (Pitfall 8: pageshow fires on initial load, same-window navigation, background-tab open)
    - `window.addEventListener('focus', onResume)` (WebView/PWA fallback)
    - Keep handler registration idempotent with destroy(): `bindLifecycle` stores the bound handlers so `destroy()` (currently a stub — completed in Plan 04) can remove them; for THIS plan, implement `destroy()` to remove all four listeners from the injected document/window and stop the rAF poll loop (set a `state.destroyed = true` flag checked by pollLoop), returning SlowGram. This makes behavior Test 6 possible.
    - Event targets matter: visibilitychange/resume → document; pageshow/focus → window. The fake document/window from Plan 01 already mirror these targets.

    test/slowgram.test.js — extend the fatigue suite with the six behavior tests. For each signal, set up a >fatigueWindowMs background (setContext('REELS'), advance+flush, setVisibility('hidden')+dispatch, advance window+1), then dispatch the signal under test on the CORRECT target (document vs window) and assert reset. For Test 3, dispatch `{type:'pageshow', persisted:false}` after the same gap and assert NO reset. For Test 6, call `SlowGram.destroy()` then dispatch each signal and assert state unchanged.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with the extended fatigue suite green (all six tests)
    - `src/slowgram.js` bindLifecycle registers exactly four listeners: visibilitychange + resume on document, pageshow + focus on window
    - `src/slowgram.js` pageshow handler checks `e.persisted` before calling onResume
    - `src/slowgram.js` destroy() removes all four listeners and stops the rAF poll (state.destroyed flag consulted by pollLoop)
    - behavior Test 6 asserts no listener leak after destroy
  </acceptance_criteria>
  <done>All four resume signals drive the same wall-clock catch-up with correct event targets; pageshow initial-load is guarded by persisted; destroy() removes every listener.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less edge probe — unresolved rows, surfaced, never dropped)

| # | Probe row | Status | Locked resolution implemented by this plan |
|---|-----------|--------|---------------------------------------------|
| FA-05 | CORE-03 (boundary) — "What happens exactly at each min/max/threshold — and one step either side?" | unresolved | Locked contract: strict `>` comparison — a background gap of exactly `fatigueWindowMs` (300000 ms) does NOT reset; 300001 ms does. One step either side is asserted in the suite (Test 1 vs Test 2). The 6-minute device test in HARN-06 is a safe margin, not the threshold. |
| FA-08 | CORE-03 (precision) — "Where can precision loss, overflow, or rounding/tie-breaking occur — and what is the exact contract?" | unresolved | Locked contract: `delta = now - since` is integer wall-clock ms from `env.clock.now()`; a negative delta (NTP back-step) clamps to 0; no float math, no rounding — the comparison is exact integer `>`. `now - since` cannot overflow at realistic wall-clock ranges (Date.now is ~1.7e12 ms, well under Number.MAX_SAFE_INTEGER). |

## Artifacts this phase produces

- `src/slowgram.js` — `onHidden()`, `onResume()` (catch-up base = hiddenAt || lastBoundary, delta clamp, strict `> CONFIG.fatigueWindowMs` reset, gap-discount path), `resetSession()` (elapsedMs 0, sync(0), emit 'reset', context preserved), full `bindLifecycle(env)` with four listeners (document: visibilitychange/resume; window: pageshow[persisted]/focus), `destroy()` now functional (listener removal + rAF poll stop via `state.destroyed`), `state.destroyed` flag, `'reset'` bus event
- `test/slowgram.test.js` — fatigue suite: strict-reset boundary pair (300001 vs 300000), short-gap discount, hiddenAt-null fallback, negative-delta clamp, context preservation on reset, per-signal catch-up (resume/pageshow[persisted]/focus/visibilitychange), pageshow persisted:false guard, destroy listener-leak check

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle events → FatigueManager | document/window events from the host page drive hidden/resume transitions; a missed or spoofed event changes reset semantics |
| destroy() → event bus | removing listeners mid-flight must not leave half-bound handlers or a running rAF poll |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | CONFIG.fatigueWindowMs value | high | mitigate | deep-frozen CONFIG from Plan 02; handler reads `CONFIG.fatigueWindowMs` only (rg gate asserts no literal in handler); strict-mode write throws |
| T-01-09 | Spoofing | lifecycle events (fake visibilitychange/resume) | medium | mitigate | All four signals are treated as equivalent catch-up points; a spurious resume with hiddenAt null falls back to lastBoundary and only refreshes it (idempotent — no spurious reset, no accumulation). Cross-checked against `env.visibilityState()` on visibilitychange. |
| T-01-10 | DoS (host) | listener leak across repeated init/destroy cycles | medium | mitigate | `destroy()` removes all four listeners and stops the rAF poll via `state.destroyed`; behavior Test 6 asserts no listener leak |
| T-01-11 | Tampering | session state (silent reset or silent accumulation) | medium | mitigate | resetSession always emits observable 'reset'; gap never accumulates (discount path); both asserted in suite; engine strict mode + try/catch containment |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages by hard project constraint — no dependency graph to audit |

No threat rated high is left unmitigated; mitigations reference the specific implementation in Task 1/Task 2 actions (CONFIG-only window reads, idempotent resume, destroy listener removal, observable reset).
</threat_model>

<verification>
- `node test/slowgram.test.js` exits 0 (fatigue suite green)
- Grep gate: `rg -n "fatigueWindowMs" src/slowgram.js` → handler references CONFIG.fatigueWindowMs
- Boundary assertions derive from `getConfig().fatigueWindowMs`, never literals
</verification>

<success_criteria>
- Backgrounding >5 min resets the session via ANY of the four resume signals; shorter gaps neither reset nor accumulate
- The session clock never counts unverifiable time; reset is always observable via the 'reset' event
- destroy() removes every listener and stops the poll — no leak across init/destroy cycles
</success_criteria>

<output>
Create `.planning/phases/01-motor-core-lifecycle/01-03-SUMMARY.md` when done
</output>