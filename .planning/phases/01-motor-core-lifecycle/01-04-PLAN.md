---
phase: 01-motor-core-lifecycle
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/slowgram.js
  - test/harness.js
  - test/slowgram.test.js
  - test/harness.html
autonomous: true
requirements:
  - CORE-04
  - CORE-06
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "init(overrides) validates the injected env shape and fails loudly (throws a descriptive Error) on malformed dependencies instead of crashing the host page"
    - "init() with no overrides resolves all capabilities from globals in a browser (default env path)"
    - "destroy() removes all lifecycle listeners, stops the rAF poll, and resets engine state so a subsequent init() re-initializes cleanly (re-init cycle)"
    - "the SAME src/slowgram.js file runs the full suite under Node mocks (node test/slowgram.test.js exits 0) AND in a plain browser page (test/harness.html renders a green pass/fail table)"
    - "init() called twice without destroy() does not duplicate listeners (idempotent init guard)"
    - "engine code contains zero references to bare globals outside resolveEnv (source scan)"
  artifacts:
    - src/slowgram.js (env validation, destroy completion, init idempotence guard)
    - test/harness.js (browser result renderer verified in harness.html)
    - test/slowgram.test.js (DI suite)
    - test/harness.html (browser host running the same suite)
  key_links:
    - "init() → resolveEnv() → bindLifecycle() is the single bootstrap path; destroy() must undo exactly what init() set up"
    - "harness.html loads engine → harness → tests in order and calls renderResults to prove the browser host"
    - "the DI suite asserts the default-env path by running init() inside a stub global shim (no real DOM required)"
  prohibitions:
    - statement: "The engine never reads session time from any source other than env.clock.now() — no bare Date.now()/performance.now() in the engine body outside resolveEnv."
      status: unverified
      flagged: true
    - statement: "The engine never silently discards accumulated session state — a fatigue reset always emits the observable 'reset' event; watch time is never erased without an observable transition."
      status: unverified
      flagged: true
---

<objective>
Wave 4: complete the DI seam — defensive env-shape validation in `init()`, a fully functional `destroy()` that undoes everything `init()` set up, an idempotent-init guard, and the two-host smoke proving the SAME engine file runs under Node mocks and in a plain browser page.

Purpose: CORE-04/CORE-06's contract is "the same file runs under mocks and in the WebView." Plan 01 proved the mock host; this plan proves the default-env path and the browser host, and hardens the seam so a malformed harness/container fails loudly instead of crashing the host page.
Output: env validation + destroy + init guard in `src/slowgram.js`, DI suite in `test/slowgram.test.js`, `test/harness.html` browser smoke verified in Edge.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-motor-core-lifecycle/01-RESEARCH.md
@.planning/phases/01-motor-core-lifecycle/01-PATTERNS.md
@.planning/phases/01-motor-core-lifecycle/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DI seam hardening — env validation, destroy completion, idempotent init (CORE-04)</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js (the file being modified — current resolveEnv, init, destroy stub from Plans 01–03)
    - 01-RESEARCH.md — Pattern 1 (IIFE + DI seam, lines 149–192), Security Domain V5 (env validation, 528), PATTERNS.md lines 167–169 (error handling / seam validation)
    - 01-PATTERNS.md — lines 264–270 (Shared Pattern 2 — DI seam env resolution with global fallback)
  </read_first>
  <behavior>
    - Test 1: malformed env — init({ clock: 'not-a-clock' }) throws a descriptive Error (clock must be an object with a now function)
    - Test 2: missing overrides — init() with no arguments in a stub-global shim (globalThis.document/window/MutationObserver/requestAnimationFrame defined as fakes) resolves all capabilities from the shim and runs setContext('REELS') + advance + flush without throwing
    - Test 3: double init without destroy — init(), then init() again → 'contextchange' subscribers still receive exactly one emission per setContext change (no duplicated listeners)
    - Test 4: destroy() — after init() + destroy(), dispatching any of the four lifecycle signals leaves state untouched; a subsequent init() + setContext('REELS') + advance + flush accumulates normally (clean re-init)
    - Test 5: destroy() resets state — after destroy(), getState() returns zeros with context 'UNKNOWN' (fresh state) OR destroy() is documented as terminal-only; lock ONE behavior: destroy() returns the engine to pre-init state so re-init works
  </behavior>
  <action>
    Harden the DI seam in src/slowgram.js per RESEARCH.md Pattern 1 + Security Domain V5:

    - `resolveEnv(overrides)` — before resolving, validate each PROVIDED override: `clock` must be an object with a `now` function (throw `Error('SlowGram: env.clock must provide now()')`); `document` must be null or an object with `addEventListener`; `window` must be null or an object with `addEventListener`; `MutationObserver` must be null or a constructor; `requestAnimationFrame` must be null or a function. Omitted keys default from globals exactly as in Plan 01 (no change to the default path). Malformed provided deps fail loudly — never silently substitute (that would create mock-vs-live divergence, the exact failure CORE-04 forbids).
    - `init(overrides)` — add an idempotence guard: if the engine is already initialized and not destroyed, call `destroy()` internally before re-binding (or throw — LOCK the internal-destroy-then-reinit behavior, matching behavior Test 3/4: init() is safe to call repeatedly). Wrap the whole bootstrap in try/catch; on failure, console.error and rethrow the descriptive error (containment means the host page isn't broken by a partially-bound engine; the caller sees a loud error).
    - `destroy()` — complete the Plan 03 stub: remove all four lifecycle listeners (visibilitychange/resume from document; pageshow/focus from window), set `state.destroyed = true` (pollLoop already consults it), and reset `state` to a fresh `{elapsedMs: 0, phase: 0, context: 'UNKNOWN', visible: true, hiddenAt: null, lastBoundary: 0, running: false}` so a subsequent init() re-initializes cleanly. Clear the subscriber list ONLY if documented — LOCK: destroy() preserves registered subscribers across re-init (Phase 2+ consumers subscribe once at page load); document this in a comment.
    - Keep `SlowGram.on`/`emit` unchanged; the engine body must still contain zero bare-global references outside resolveEnv.

    test/slowgram.test.js — add the DI suite with the five behavior tests. For the default-env path (Test 2), construct a stub-global shim: temporarily set `globalThis.document`/`globalThis.window`/`globalThis.MutationObserver`/`globalThis.requestAnimationFrame` to fakes, call `SlowGram.init()`, drive a REELS accumulation, then restore the globals (try/finally). For Test 3, count 'contextchange' emissions across a double init. For Test 4/5, drive a full init → destroy → re-init cycle and assert the accumulation works after re-init and that post-destroy signals are inert.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with the DI suite green (all five tests)
    - `src/slowgram.js` resolveEnv throws descriptive Errors on malformed provided overrides (clock without now, document without addEventListener)
    - `src/slowgram.js` init() handles repeated calls (internal destroy-then-reinit) without duplicating listeners (asserted)
    - `src/slowgram.js` destroy() removes all four listeners, sets state.destroyed, resets state, preserves subscribers
    - behavior Test 5 asserts re-init accumulates normally after destroy
    - `rg -n "Date\.now|document\.|window\.|MutationObserver|requestAnimationFrame" src/slowgram.js` shows matches ONLY inside resolveEnv and the try/catch bootstrap (no bare-global leaks in module functions)
  </acceptance_criteria>
  <done>DI seam hardened: malformed env fails loudly, default env works from globals, repeated init is safe, destroy fully undoes init and enables clean re-init, and subscribers survive re-init.</done>
</task>

<task type="auto">
  <name>Task 2: Two-host smoke — same engine file under Node and in a plain browser (CORE-04/06)</name>
  <files>test/harness.js, test/harness.html, test/slowgram.test.js</files>
  <read_first>
    - test/harness.html (the file being modified — current runner page from Plan 01)
    - test/harness.js (the file being modified — assert runner + renderResults from Plan 01)
    - 01-RESEARCH.md — Validation Architecture (lines 489–505), Recommended Project Structure (136–147), CORE-06 test map (504)
    - 01-VALIDATION.md — Test Infrastructure (18–26), Manual-Only Verifications (63–67)
  </read_first>
  <behavior>
    - Test 1 (browser, via harness.html): the page loads src/slowgram.js → test/harness.js → test/slowgram.test.js in order and the results table renders with ALL suites green (clock, config, phase, fatigue, DI)
    - Test 2 (Node, already covered): node test/slowgram.test.js exits 0 — re-asserted here as the two-host parity baseline
    - Test 3: harness.js renderResults produces a row per test with pass/fail status and a total summary line (assertable in the browser DOM)
  </behavior>
  <action>
    Complete the browser host so the same suite runs in a plain page:

    - test/harness.html — keep the three `<script>` tags in load order `src/slowgram.js` → `test/harness.js` → `test/slowgram.test.js`; ensure `test/slowgram.test.js` ends by calling `renderResults('results')` after all suites run; the page must render a table with one row per test (label + PASS/FAIL) and a summary line (total passed / total run). Add a small `window.addEventListener('error', ...)` handler that renders any uncaught error as a FAIL row so a broken engine shows as a red table, not a blank page (containment observability).
    - test/harness.js — verify `renderResults(containerId)` is robust: creates the table in the given container, uses textContent (no innerHTML injection — the test labels are trusted harness text, but textContent is the safe default), appends a summary row.
    - test/slowgram.test.js — the test file must be side-effect-safe in the browser: all suites register via the harness's `describe`/`test`-style registration (or a flat run list) and only call renderResults at the end; no process/require/fs usage in the browser path (wrap the fs-based no-timer source scan from Plan 01 in a `typeof process !== 'undefined'` guard so harness.html skips it — Node-only assertion).
    - After implementation: run `node test/slowgram.test.js` (Node host) and open `test/harness.html` in Edge (browser host) to confirm the same engine file passes on both hosts.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
    <human-check>Open test/harness.html in Edge (path: C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe). The page must render a pass/fail table with ALL suites green (clock, config, phase, fatigue, DI) and a summary line showing zero failures. No red rows, no console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 (Node host green — full suite)
    - `test/harness.html` loads the three files in order and renders one row per test via renderResults (source assertion)
    - `test/slowgram.test.js` contains a `typeof process !== 'undefined'` guard around the fs-based no-timer scan (browser-safe)
    - `test/harness.js` renderResults uses textContent and emits a summary line (source assertion)
    - human-check: harness.html renders a fully green table in Edge with no console errors
  </acceptance_criteria>
  <done>The same src/slowgram.js passes the full suite on both hosts — Node mocks and a plain browser page — satisfying CORE-06's "same file, two hosts" contract.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less edge probe — unresolved rows, surfaced, never dropped)

| # | Probe row | Status | Locked resolution implemented by this plan |
|---|-----------|--------|---------------------------------------------|
| FA-02 | CORE-04 (unclassified) — manual review | unresolved | DI seam contract completed here: `init(overrides)` validates every provided dep shape and throws descriptive Errors on malformed input (fail loudly, never silently substitute — mock-vs-live divergence is the forbidden failure); omitted deps default from globals; `destroy()` removes all four listeners, stops the poll, resets state, preserves subscribers; repeated `init()` internally re-initializes safely. Verifier must confirm the DI suite + default-env shim test. |
| FA-06 | CORE-06 (unclassified) — manual review | unresolved | Two-host contract proven here: the same `src/slowgram.js` runs the full suite under Node mocks (`node test/slowgram.test.js` exits 0) and in `test/harness.html` (green table in Edge, human-checked). The fake clock drives all time on both hosts; tests assert deltas only, never absolute time. |

## Artifacts this phase produces

- `src/slowgram.js` — env-shape validation in `resolveEnv` (descriptive Errors per dep), idempotent `init()` (internal destroy-then-reinit), complete `destroy()` (listener removal + state.destroyed + state reset + subscriber preservation), documented re-init contract
- `test/harness.js` — hardened `renderResults(containerId)` (textContent, summary row)
- `test/slowgram.test.js` — DI suite (malformed env throw, default-env shim path, double-init no-duplicate-listeners, destroy-then-reinit cycle, state reset on destroy) + Node-only guard around the fs-based no-timer scan
- `test/harness.html` — two-host runner: script load order, renderResults invocation, window 'error' → FAIL row handler

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container/harness → init() | the injection seam accepts arbitrary env objects from a harness or a WebView container; malformed deps previously would crash the host page |
| engine → browser host | harness.html runs the engine in a plain page; an uncaught error must surface as a FAIL row, not break the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | DoS (host) | init() with malformed injected env | high | mitigate | `resolveEnv` validates every provided override and throws descriptive Errors; bootstrap try/catch contains failures and rethrows loudly — a bad container cannot crash the host page, it gets a loud init error |
| T-01-13 | Tampering | listener duplication across repeated init | medium | mitigate | Idempotent `init()` (internal destroy-then-reinit); `destroy()` removes all four listeners; DI suite asserts single emission per contextchange across double init |
| T-01-14 | DoS (host) | uncaught engine error in browser host | medium | mitigate | harness.html window 'error' handler renders a FAIL row; engine handlers are try/catch-contained from Plan 01; error containment keeps the host page alive |
| T-01-15 | Tampering | bare-global references leaking outside the seam | medium | mitigate | resolveEnv is the only place engine code touches globals; rg scan confines `Date.now`/`document`/`window`/`MutationObserver`/`requestAnimationFrame` to resolveEnv and the bootstrap |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages by hard project constraint — no dependency graph to audit |

No threat rated high is left unmitigated; mitigations reference the specific implementation in Task 1/Task 2 actions (env validation, idempotent init, destroy, error-to-FAIL-row handler, rg-scoped global access).
</threat_model>

<verification>
- `node test/slowgram.test.js` exits 0 (full suite: clock, config, phase, fatigue, DI)
- human-check: `test/harness.html` renders a fully green table in Edge with no console errors
- Source scan: bare-global references confined to resolveEnv + bootstrap (rg gate)
</verification>

<success_criteria>
- The DI seam is complete: validation, destroy, re-init, idempotence all proven under the fake clock
- CORE-06's "same file, two hosts" contract holds: Node suite green AND browser table green
- Phase 1's success criteria from ROADMAP.md are fully satisfiable by the executed engine
</success_criteria>

<output>
Create `.planning/phases/01-motor-core-lifecycle/01-04-SUMMARY.md` when done
</output>