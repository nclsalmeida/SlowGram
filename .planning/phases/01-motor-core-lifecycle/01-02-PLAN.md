---
phase: 01-motor-core-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - CORE-02
  - CORE-05
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CONFIG is a single object with keys phaseBoundariesMin, degradationMatrix, selectors, preservedRoutes, fatigueWindowMs, segmentCapMs — no magic numbers scattered elsewhere in the engine"
    - "Object.isFrozen(CONFIG) is true AND Object.isFrozen returns true for every nested node (phaseBoundariesMin array, degradationMatrix, selectors, preservedRoutes)"
    - "a strict-mode write to CONFIG or any nested node throws TypeError"
    - "CONFIG.phaseBoundariesMin deep-equals [3, 7, 12] — phase 0: <3m, 1: 3–7m, 2: 7–12m, 3: >=12m (boundary decision locked per research)"
    - "phaseFor(elapsedMs) is total over [0, Infinity): elapsedMs < 0 returns 0; elapsedMs exactly at a boundary returns the next phase (>= comparison)"
    - "phasechange is emitted only on real transitions — sync() with an unchanged phase emits nothing"
    - "advancing time across a phase boundary emits exactly one phasechange with the new phase number"
  artifacts:
    - src/slowgram.js (initConfig full shape, deepFreeze, phaseFor, sync)
    - test/slowgram.test.js (config suite + phase suite)
  key_links:
    - "phaseFor reads CONFIG.phaseBoundariesMin — the config array is the single source of truth; tests never hardcode 3/7/12 literals"
    - "tick(now) calls phaseMachine.sync(elapsedMs) — the only wiring between clock accumulation and phase emission"
    - "resetSession() (Plan 03) will call sync(0) — sync must handle the 0 boundary identically to any other value"
  prohibitions:
    - statement: "The engine never silently discards accumulated session state — a fatigue reset always emits the observable 'reset' event; watch time is never erased without an observable transition."
      status: unverified
      flagged: true
---

<objective>
Wave 2: the frozen CONFIG single object (CORE-05) and the pure PhaseMachine (CORE-02) — `elapsedMs → phase 0..3` as a total pure function driven by the CONFIG boundary array, with a transition-guarded `phasechange` emitter.

Purpose: downstream consumers (Phase 3 levers, Phase 4 overlay) must never disagree about the current phase, and CONFIG must be a single immutable constants object from day one. The phase machine is pure — no DOM, no timers — so it is fully unit-testable with the fake clock from Plan 01.
Output: full deep-frozen CONFIG in `src/slowgram.js`, `phaseFor`/`sync` replacing the Plan 01 stub, `SlowGram.getConfig()` test handle, config + phase suites in `test/slowgram.test.js`.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-motor-core-lifecycle/01-RESEARCH.md
@.planning/phases/01-motor-core-lifecycle/01-PATTERNS.md
@.planning/phases/01-motor-core-lifecycle/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full deep-frozen CONFIG — single constants object (CORE-05)</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js (the file being modified — current initConfig from Plan 01)
    - 01-RESEARCH.md — Pattern 5 (deep-frozen CONFIG, lines 267–284), Assumption A5 (CONFIG shape, 454), Pitfall 2 (shallow freeze, 316–320), Code Example MDN deepFreeze (361–379)
    - 01-PATTERNS.md — lines 141–154 (CONFIG shape + deepFreeze verbatim) and lines 249–264 (Shared Pattern 1)
    - 01-01-SUMMARY.md — what Plan 01 shipped (tick reads CONFIG.segmentCapMs already)
  </read_first>
  <behavior>
    - Test 1: Object.isFrozen(CONFIG) === true AND Object.isFrozen(CONFIG.phaseBoundariesMin) === true AND Object.isFrozen(CONFIG.degradationMatrix) === true AND Object.isFrozen(CONFIG.selectors) === true AND Object.isFrozen(CONFIG.preservedRoutes) === true — every nested node frozen
    - Test 2: assert.throws(() => { 'use strict'; CONFIG.fatigueWindowMs = 1; }) — strict-mode write to frozen object throws TypeError
    - Test 3: assert.throws(() => { 'use strict'; CONFIG.phaseBoundariesMin[0] = 99; }) — nested array write throws TypeError
    - Test 4: CONFIG.phaseBoundariesMin deep-equals [3, 7, 12]; CONFIG.fatigueWindowMs === 300000; CONFIG.segmentCapMs === 900000
    - Test 5: CONFIG.degradationMatrix has keys '0','1','2','3' (one entry per phase); CONFIG.preservedRoutes is an array containing '/direct/' and '/messages/'
  </behavior>
  <action>
    Replace the Plan 01 minimal `initConfig()` in src/slowgram.js with the full single constants object, deep-frozen recursively. Locked values (research + prior-plan decisions, not negotiable in this plan):

    - `phaseBoundariesMin: [3, 7, 12]` (LOCKED — RESEARCH.md:462–463; phase 0: 0–3m, phase 1: 3–7m, phase 2: 7–12m, phase 3: 12m+; the "15+" prose in REQUIREMENTS.md:13 is descriptive of the stop-point phase's tail)
    - `fatigueWindowMs: 300000` (5 min; the strict `>` comparison semantics land in Plan 03 — the VALUE lives here)
    - `segmentCapMs: 900000` (15 min — already consumed by tick from Plan 01)
    - `degradationMatrix`: per-phase lever applicability, e.g. `{0: [], 1: ['saturation'], 2: ['saturation', 'playbackRate'], 3: ['saturation', 'playbackRate', 'volume', 'autoplay']}` — Phase 3 consumes this; Phase 1 only stores it (no magic numbers scattered, per CORE-05)
    - `selectors`: placeholder registry for Phase 2, e.g. `{video: 'video', roleMain: '[role="main"]'}` — Phase 2 owns real values
    - `preservedRoutes: ['/direct/', '/messages/']` — Phase 2 RouteGuard consumes

    Implementation: build the literal object, then return `deepFreeze(config, new WeakSet())`. Keep the WeakSet cycle-guard signature from Plan 01. Confirm `deepFreeze` recurses into arrays (Object.isFrozen on arrays works the same). Add `SlowGram.getConfig()` returning the CONFIG object (already stubbed in Plan 01's Task 2 — keep it). All magic numbers now live ONLY inside this factory — scan the rest of the engine for stray literals (e.g. 60000 ms-per-minute may appear only as a documented conversion inside phaseFor in Task 2, and 300000/900000/[3,7,12] nowhere else).

    test/slowgram.test.js — add the config suite with the five behavior tests. Access CONFIG exclusively via `SlowGram.getConfig()`. For the strict-mode-throw tests, wrap the mutation in a function expression with 'use strict' and pass it to `assert.throws` (the harness assert runner from Plan 01). Use `JSON.stringify` comparison or an explicit deep-equal helper for the [3,7,12] assertion.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with the config suite green
    - `Object.isFrozen` holds for CONFIG and every nested node (asserted per behavior Test 1)
    - strict-mode writes to `CONFIG.fatigueWindowMs` and `CONFIG.phaseBoundariesMin[0]` both throw (asserted)
    - `CONFIG.phaseBoundariesMin` equals [3, 7, 12], `fatigueWindowMs` equals 300000, `segmentCapMs` equals 900000 (asserted)
    - `rg -n "300000|900000" src/slowgram.js` matches ONLY the initConfig factory body (no stray literals elsewhere)
  </acceptance_criteria>
  <done>CONFIG is a single deep-frozen object holding all phase constants, degradation matrix, selectors, preserved routes, and the fatigue window — violations throw in strict mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PhaseMachine — pure phaseFor + transition-guarded phasechange (CORE-02)</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js (the file being modified — current phaseMachine stub from Plan 01, CONFIG from Task 1)
    - 01-RESEARCH.md — Pattern 4 (PhaseMachine, lines 244–264), anti-pattern "PhaseMachine emitting on every sync" (294), Assumption A1 (boundary values, 450)
    - 01-PATTERNS.md — lines 123–139 (PhaseMachine verbatim) and 271–273 (Shared Pattern 3 — transition-guarded emission)
  </read_first>
  <behavior>
    - Test 1: phaseFor(0) === 0; phaseFor(179999) === 0 (just below 3m)
    - Test 2: phaseFor(180000) === 1 (exactly at 3m boundary — >= comparison); phaseFor(419999) === 1 (just below 7m)
    - Test 3: phaseFor(420000) === 2 (exactly at 7m); phaseFor(719999) === 2 (just below 12m)
    - Test 4: phaseFor(720000) === 3 (exactly at 12m); phaseFor(Number.MAX_SAFE_INTEGER) === 3 (total over [0, Infinity))
    - Test 5: phaseFor(-1) === 0 (defensive totality for negative elapsedMs)
    - Test 6: boundary values computed from CONFIG.phaseBoundariesMin — e.g. b[0]*60000 === 180000, never literal 3/7/12 in assertions
    - Test 7: transition guard — setContext('REELS'), advance 4 minutes, flush → exactly ONE 'phasechange' emitted with payload 1; advance 3 more minutes (crossing 7m), flush → exactly one more 'phasechange' with payload 2; repeated flush with no time passing → zero additional emissions
  </behavior>
  <action>
    Replace the Plan 01 `phaseMachine.sync` no-op stub in src/slowgram.js with the real pure machine:

    - `phaseFor(elapsedMs)` — total pure function over `[0, Infinity)` per RESEARCH.md Pattern 4, with the precision contract locked: convert boundaries to integer milliseconds ONCE (`CONFIG.phaseBoundariesMin[i] * 60000`, integer math — no float division, no rounding/tie-breaking ambiguity) and compare `elapsedMs >= boundaryMs` in a loop, returning the highest phase index reached (0-based). `elapsedMs < 0` → return 0 (defensive totality). Reads ONLY `CONFIG.phaseBoundariesMin` — no 3/7/12 literals in the function body; the `60000` ms-per-minute conversion constant is the sole literal (documented).
    - `sync(elapsedMs)` — compute `phaseFor(elapsedMs)`; if it differs from `state.phase`, set `state.phase` and `emit('phasechange', state.phase)`; otherwise emit nothing (transition guard — mandatory, RESEARCH.md:294). Must handle the 0 boundary identically to any other value so Plan 03's `resetSession()` → `sync(0)` emits phasechange 0 when coming from a higher phase.
    - Wire: `tick(now)` already calls `phaseMachine.sync(state.elapsedMs)` from Plan 01 — keep that call; `getState().phase` now reflects the real machine.
    - Expose `SlowGram._phaseFor` as a test-only direct handle for the pure-function boundary tests (documented test-only; the sync path + getState().phase is the production surface).

    test/slowgram.test.js — add the phase suite with the seven behavior tests. Compute boundary values from `SlowGram.getConfig().phaseBoundariesMin` — never literals (RESEARCH.md:463). Use `SlowGram.on('phasechange', cb)` with an invocation counter for Test 7; drive time with FakeClock.advance + FakeRAF.flush exactly as in Plan 01. Verify idempotence by flushing twice with no advance between and asserting zero additional emissions.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with the phase suite green (all seven tests)
    - `src/slowgram.js` `phaseFor` body contains no literal 3, 7, or 12 (grep `phaseFor` region: only CONFIG reads and the 60000 constant)
    - crossing a boundary emits exactly one phasechange; no-op sync emits zero (asserted via counters)
    - `phaseFor(-1) === 0` and `phaseFor(Number.MAX_SAFE_INTEGER) === 3` (asserted)
    - `SlowGram._phaseFor` exists and is documented test-only in a comment
  </acceptance_criteria>
  <done>The PhaseMachine maps any elapsedMs to phase 0..3 driven solely by CONFIG.phaseBoundariesMin, and phasechange fires only on real transitions — downstream consumers can rely on it.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less edge probe — unresolved rows, surfaced, never dropped)

| # | Probe row | Status | Locked resolution implemented by this plan |
|---|-----------|--------|---------------------------------------------|
| FA-03 | CORE-02 (boundary) — "What happens exactly at each min/max/threshold — and one step either side?" | unresolved | Locked contract: phaseFor uses `elapsedMs >= boundaryMs` (integer ms) — exactly at 3m/7m/12m returns the NEXT phase; one millisecond below returns the previous; elapsedMs < 0 returns 0; any value ≥ 12m returns 3. Boundary values come ONLY from `CONFIG.phaseBoundariesMin = [3, 7, 12]`; tests assert one-step-either-side (179999/180000, 419999/420000, 719999/720000). |
| FA-04 | CORE-02 (precision) — "Where can precision loss, overflow, or rounding/tie-breaking occur — and what is the exact contract?" | unresolved | Locked contract: integer-millisecond math only — boundaries converted once via `CONFIG.phaseBoundariesMin[i] * 60000` (integer multiply, no float division, no rounding); `elapsedMs` is an integer ms accumulator; comparisons are exact `>=`. No tie-breaking exists (single comparison path). `Number.MAX_SAFE_INTEGER` maps to phase 3 (loop termination safe). |
| FA-07 | CORE-05 (unclassified) — manual review | unresolved | CONFIG is the single deep-frozen object holding ALL phase constants (phaseBoundariesMin [3,7,12], fatigueWindowMs 300000, segmentCapMs 900000), the per-phase degradationMatrix, the selectors registry, and preservedRoutes — no magic numbers elsewhere in the engine (verified by rg scan of 300000/900000 confined to initConfig). |

## Artifacts this phase produces

- `src/slowgram.js` — full `initConfig()` factory (phaseBoundariesMin, fatigueWindowMs, segmentCapMs, degradationMatrix, selectors, preservedRoutes; deep-frozen recursively); `phaseFor(elapsedMs)` pure total function; `sync(elapsedMs)` transition-guarded phase emitter; `SlowGram.getConfig()`; `SlowGram._phaseFor` (test-only handle); `'phasechange'` bus event (payload = phase number 0..3); `phaseMachine` object now real (replaces Plan 01 stub)
- `test/slowgram.test.js` — config suite (deep-freeze on all nested nodes, strict-mode throw on write, locked values [3,7,12]/300000/900000, degradationMatrix keys '0'..'3', preservedRoutes contents) + phase suite (boundary one-step-either-side ×4, totality, negative input, CONFIG-derived values, transition-guard emission counts)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine modules → CONFIG | every module reads the shared constants object; a nested mutation silently changes phase thresholds or degradation behavior |
| PhaseMachine → event bus | phasechange emission is consumed by Phase 3 levers; per-frame re-emission would re-apply levers every frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | CONFIG nested nodes (degradationMatrix, phaseBoundariesMin) | high | mitigate | Recursive `deepFreeze` at initConfig (WeakSet cycle guard) + `'use strict'` at IIFE top — any nested write throws TypeError; config suite asserts Object.isFrozen on every node (behavior Test 1) and throw-on-write (Tests 2–3) |
| T-01-06 | Denial of Service (host) | phasechange emission storm | medium | mitigate | Transition guard in `sync(elapsedMs)` — emission only when phase changes; idempotent sync emits nothing; phase suite asserts zero emissions on no-op flush (Test 7) |
| T-01-07 | Tampering | phaseFor boundary values | medium | mitigate | `phaseFor` reads ONLY `CONFIG.phaseBoundariesMin` (frozen); no literals in the function body; rg scan confines 3/7/12 and 300000/900000 to initConfig |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages by hard project constraint — no dependency graph to audit |

No threat rated high is left unmitigated; mitigations reference the specific implementation in Task 1/Task 2 actions (deepFreeze recursion, strict-mode throws, transition guard).
</threat_model>

<verification>
- `node test/slowgram.test.js` exits 0 (config + phase suites green)
- Source scan: `rg -n "300000|900000" src/slowgram.js` matches only the initConfig factory
- Boundary-value assertions in the phase suite derive from `getConfig().phaseBoundariesMin`, never literals
</verification>

<success_criteria>
- CONFIG is one deep-frozen object; accidental mutation anywhere throws
- PhaseMachine maps elapsedMs → phase 0..3 deterministically with `phasechange` only on real transitions
- CORE-02 and CORE-05 are fully testable under the fake clock
</success_criteria>

<output>
Create `.planning/phases/01-motor-core-lifecycle/01-02-SUMMARY.md` when done
</output>