---
phase: 01-motor-core-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slowgram.js
  - test/harness.js
  - test/slowgram.test.js
  - test/harness.html
autonomous: true
requirements:
  - CORE-01
  - CORE-04
  - CORE-06
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "advance(180000) while context==REELS && visible increases elapsedMs by exactly 180000"
    - "advance(60000) while context==SOCIAL, context==UNKNOWN, or visible==false leaves elapsedMs unchanged"
    - "a negative clock delta (advance to a now earlier than lastBoundary) leaves elapsedMs unchanged"
    - "a single advance(1200000) (20 min) while REELS+visible adds only CONFIG.segmentCapMs (900000), never more"
    - "src/slowgram.js contains zero calls to timer scheduling APIs"
    - "node test/slowgram.test.js exits 0 with the clock suite green"
    - "test/harness.html loads src/slowgram.js, then test/harness.js, then test/slowgram.test.js via <script> tags in that order"
    - "SlowGram.init({clock, document, window, MutationObserver}) accepts injected mocks and getState() reflects them"
  artifacts:
    - src/slowgram.js
    - test/harness.js
    - test/slowgram.test.js
    - test/harness.html
  key_links:
    - "env.clock.now() is the only time source read by tick(now) — no bare Date.now() outside resolveEnv"
    - "tick(now) is the only accumulation path; every boundary handler and the rAF poll call it"
  prohibitions:
    - statement: "The session clock never counts unverifiable time: background/hidden gaps are never accumulated, even when within the fatigue window (gap discounted, lastBoundary=now)."
      status: unverified
      flagged: true
    - statement: "The engine never reads session time from any source other than env.clock.now() — no bare Date.now()/performance.now() in the engine body outside resolveEnv."
      status: unverified
      flagged: true
---

<objective>
Wave 1 tracer: the thinnest end-to-end slice of the state spine — engine IIFE skeleton + DI seam, harness scaffold (FakeClock/FakeDocument/FakeWindow/FakeMutationObserver/FakeRAF/assert), the CORE-01 session-clock accumulation path driven by the fake clock, and the browser host page that runs the same tests.

Purpose: prove the architecture (DI seam → injected clock → single tick() accumulation → getState) works end-to-end before any expansion. A dead end in the seam or fake-clock contract costs one commit, not ten committed layers.
Output: `src/slowgram.js` (minimal engine skeleton), `test/harness.js` (FakeClock, FakeDocument, FakeWindow, FakeMutationObserver stub, FakeRAF, assert runner), `test/slowgram.test.js` (clock suite), `test/harness.html` (browser runner).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-motor-core-lifecycle/01-RESEARCH.md
@.planning/phases/01-motor-core-lifecycle/01-PATTERNS.md
@.planning/phases/01-motor-core-lifecycle/01-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Engine skeleton + DI seam + SessionClock accumulation end-to-end (CORE-01/04/06)</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js, test/harness.html</files>
  <read_first>
    - 01-RESEARCH.md — Pattern 1 (IIFE + DI seam, lines 149–192), Pattern 2 (SessionClock tick, 194–221), Common Operation 2 (Clock DI, 382–396), Common Operation 4 (harness usage, 411–429), Recommended Project Structure (136–147)
    - 01-PATTERNS.md — Pattern Assignments section (lines 41–241) which reproduces the verbatim excerpts for all four files
    - 01-VALIDATION.md — Wave 0 requirements (lines 54–59) and requirement→test map (497–505 in RESEARCH.md)
  </read_first>
  <action>
    Create the four-file vertical slice. This task IS the tracer: one path — `SlowGram.init(mocks) → setContext('REELS') → clock.advance(180000) → raf.flush() → getState().elapsedMs === 180000` — wired through every layer, production-quality, committed for keeps.

    src/slowgram.js — strict-mode IIFE `(function (global) { 'use strict'; ... })(typeof window !== 'undefined' ? window : globalThis);` exposing a single `global.SlowGram` handle with:
    - `resolveEnv(overrides)` — resolves `clock` (default `{now: Date.now}`), `document`, `window`, `MutationObserver`, `requestAnimationFrame` (defaults from globals when present, else null), and a `visibilityState()` helper returning `env.document.visibilityState` when document exists else `'visible'`. The engine body NEVER references bare `Date.now()`, `document`, `window`, `MutationObserver`, or `requestAnimationFrame` outside this function.
    - `initConfig()` — minimal deep-frozen CONFIG containing only `segmentCapMs: 900000` (full shape lands in Plan 02; tick reads CONFIG.segmentCapMs from day one so no magic number sits in the accumulation path).
    - `deepFreeze(object, seen)` — WeakSet cycle-guarded recursion per RESEARCH.md Pattern 5.
    - `state` object `{elapsedMs: 0, phase: 0, context: 'UNKNOWN', visible: true, hiddenAt: null, lastBoundary: 0, running: false}`.
    - `updateRunning()` — `state.running = (state.context === 'REELS' && state.visible)`.
    - `setContext(context)` — throws `Error('SlowGram: invalid context "' + context + '"')` unless context is `'REELS'|'SOCIAL'|'UNKNOWN'`; if changed, sets `state.context`, emits `'contextchange'` with the context, calls `updateRunning()`, then `tick(env.clock.now())`. Default context `'UNKNOWN'` (fail-safe — clock stays paused until REELS).
    - `tick(now)` — the ONLY accumulation path: if `state.running`, compute `delta = now - state.lastBoundary`; if `delta > 0`, `state.elapsedMs += Math.min(delta, CONFIG.segmentCapMs)`; always `state.lastBoundary = now`; then call `phaseMachine.sync(state.elapsedMs)` (sync is a no-op stub in this plan — the real PhaseMachine lands in Plan 02, a function-level fill with no architectural change).
    - `pollLoop()` — while `state.running && env.requestAnimationFrame`, `tick(env.clock.now())` then `env.requestAnimationFrame(pollLoop)`; a frame callback, NOT a timer.
    - `bindLifecycle(env)` — minimal: `document.addEventListener('visibilitychange', ...)` setting `state.visible = (env.visibilityState() !== 'hidden')`, calling `updateRunning()` and `tick(env.clock.now())`. Full FatigueManager catch-up logic lands in Plan 03.
    - `SlowGram.init(overrides)` — `env = resolveEnv(overrides); CONFIG = initConfig(); bindLifecycle(env); return SlowGram;` wrapped in try/catch that swallows and console.error's any init failure (engine must never break the host page).
    - `SlowGram.getState()` — returns a shallow copy `{elapsedMs, phase, context, visible, hiddenAt}`.
    - `SlowGram.on(event, cb)` / `emit(event, data)` — tiny emitter; `destroy()` is a stub returning SlowGram (full impl in Plan 04).
    - Wrap every event handler body in try/catch (containment per RESEARCH.md Security Domain).

    test/harness.js — per RESEARCH.md Common Operation 2 + Wave 0 spec:
    - `FakeClock(startMs)` — `{ now: function(){ return t; }, advance: function(ms){ t += ms; return t; } }` (Phase 1: advance merely moves now).
    - `FakeDocument(init)` — `visibilityState` (default `'visible'`), `addEventListener(type, cb)`, `dispatchEvent(event)` (invokes registered listeners for event.type synchronously), `setVisibility(v)` helper.
    - `FakeWindow()` — `addEventListener`/`dispatchEvent` (same shape).
    - `FakeMutationObserver` — constructor stub (engine feature-detects; injectable as the stub or null).
    - `FakeRAF` — `{ request(cb){ this.pending = cb; }, flush(){ if (this.pending) { var cb = this.pending; this.pending = null; cb(); } } }` — one flush = one frame = one tick.
    - `assert` runner (~30 lines): `equal(actual, expected, label)`, `ok(value, label)`, `throws(fn, label)` — each records `{label, pass}` into a results array; `renderResults(containerId)` renders a pass/fail table (consumed by harness.html).
    - `FakeEvent` not needed — Node v24 has a global `Event` constructor; tests dispatch `new Event('visibilitychange')` etc.

    test/slowgram.test.js — clock suite (CORE-01), fully synchronous, driven by FakeClock.advance + FakeRAF.flush:
    - Test 1: `init({clock: FakeClock(1000000), document: FakeDocument({visibilityState:'visible'}), window: FakeWindow(), MutationObserver: null, requestAnimationFrame: raf.raf})`; `setContext('REELS')`; `clock.advance(180000)`; `raf.flush()` → `getState().elapsedMs === 180000`.
    - Test 2: same setup, `setContext('SOCIAL')`; `advance(60000)`; `raf.flush()` → elapsedMs stays 0.
    - Test 3: `setContext('UNKNOWN')`; `advance(60000)`; `raf.flush()` → elapsedMs stays 0.
    - Test 4: `doc.setVisibility('hidden')`; `doc.dispatchEvent(new Event('visibilitychange'))`; `advance(60000)`; `raf.flush()` → elapsedMs unchanged.
    - Test 5: DI seam — `getState().context === 'UNKNOWN'` immediately after init (default fail-safe) and setContext('REELS') reflects in getState().
    - Each test creates a fresh FakeClock/FakeDocument/FakeWindow/FakeRAF instance; tests run sequentially in one process.
    - Cross-cutting: assert the engine file text contains no calls to timer scheduling APIs (read `src/slowgram.js` via `require('fs').readFileSync` and assert the file text does not match the timer-API call pattern).

    test/harness.html — plain page, three `<script>` tags in load order `src/slowgram.js` → `test/harness.js` → `test/slowgram.test.js`, plus a `<div id="results">` and a call to `renderResults('results')` after the tests run. No framework, no build step.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with all clock-suite tests passing
    - `src/slowgram.js` contains the exact public API surface: `init`, `getState`, `setContext`, `on`, `emit`, `destroy` (grep `SlowGram\.(init|getState|setContext|on|emit|destroy)` finds all six)
    - `src/slowgram.js` contains zero `setInterval`/`setTimeout` call sites (source scan)
    - `src/slowgram.js` contains no bare `Date.now()` outside `resolveEnv` (the only `Date.now` occurrence is the default clock in `resolveEnv`)
    - `test/harness.js` exports `FakeClock` with `now` and `advance` methods, `FakeDocument` with `setVisibility`, `FakeRAF` with `request` and `flush`, and an `assert` object with `equal`/`ok`/`throws`
    - `test/harness.html` loads the three files in order engine → harness → tests
  </acceptance_criteria>
  <done>The single happy path works end-to-end: fake clock drives accumulation gated on REELS+visible, verified by `node test/slowgram.test.js` and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the CORE-01 clock contract — exclusions, clamping, segment cap, elapsed event (CORE-01/04)</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js (the file being modified — current state from Task 1)
    - test/slowgram.test.js (the file being modified — current suite from Task 1)
    - 01-RESEARCH.md — Pattern 2 (tick, 194–221), Pitfall 7 (NTP jumps, 346–350), Assumption A4 (segment cap, 453), event bus list (119)
    - 01-PATTERNS.md — Shared Pattern 5 (wall-clock delta hygiene, lines 280–283)
  </read_first>
  <behavior>
    - Test 1: negative delta — after setContext('REELS') and one flush, advance the clock to a now EARLIER than lastBoundary (FakeClock.advance with a negative argument), flush → elapsedMs unchanged and non-negative
    - Test 2: segment cap — setContext('REELS'), advance(1200000) (20 min) in one segment, flush → elapsedMs adds exactly CONFIG.segmentCapMs (900000), not 1200000
    - Test 3: elapsed event — subscribe via SlowGram.on('elapsed'), setContext('REELS'), advance(60000), flush → callback fired exactly once with payload equal to the new elapsedMs
    - Test 4: contextchange event — subscribe via SlowGram.on('contextchange'), setContext('REELS') then setContext('REELS') again → callback fired exactly once (no emission on no-op set)
    - Test 5: invalid context — setContext('FOO') throws an Error
  </behavior>
  <action>
    Extend the engine and suite to the full CORE-01 accumulation contract:

    src/slowgram.js:
    - In `tick(now)`: keep the running-gated accumulation; the `delta > 0` guard already clamps negative deltas (Pitfall 7) — keep it and document it in a comment as the NTP back-step clamp.
    - Add `emit('elapsed', state.elapsedMs)` at the end of `tick(now)` after the phase sync (the `'elapsed'` bus event per RESEARCH.md line 119 — Phase 1 consumers are the harness tests).
    - `setContext(context)`: emission of `'contextchange'` only when the context actually changes (already the shape from Task 1 — assert it stays idempotent); keep the throw on invalid context.
    - Do NOT add any timer-based scheduling anywhere in the engine.

    test/slowgram.test.js — extend the clock suite with the five behavior tests above. Drive time exactly as in Task 1 (FakeClock.advance + FakeRAF.flush). For the negative-delta test, call `clock.advance(-5000)` after a first flush so `now < lastBoundary`. For the segment-cap test, compute the cap via `SlowGram.getState()`-independent source: read the frozen CONFIG through a new test-only handle `SlowGram.getConfig()` — add `getConfig()` to the engine returning the CONFIG object (documented as test/consumer handle; Plan 02 builds the full shape) — and assert `SlowGram.getConfig().segmentCapMs === 900000` as part of the test setup. For the elapsed-event test use `SlowGram.on('elapsed', cb)` and count invocations. Assert setContext idempotence via an invocation counter.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node test/slowgram.test.js` exits 0 with the extended clock suite green (all Task 1 tests still pass plus the five new ones)
    - `src/slowgram.js` contains `emit('elapsed'` and `emit('contextchange'` call sites
    - `src/slowgram.js` `tick(now)` contains the `delta > 0` guard before accumulation
    - `SlowGram.getConfig().segmentCapMs === 900000` (asserted in the suite)
    - `setContext('FOO')` throws (asserted via assert.throws)
    - `src/slowgram.js` still contains zero timer scheduling API call sites (source scan in suite)
  </acceptance_criteria>
  <done>The CORE-01 clock contract is complete: REELS+visible accumulates, everything else does not, negative deltas clamp, single segments cap at segmentCapMs, and the elapsed/contextchange events fire on the bus.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less edge probe — unresolved rows, surfaced, never dropped)

| # | Probe row | Status | Locked resolution implemented by this plan |
|---|-----------|--------|---------------------------------------------|
| FA-01 | CORE-01 (unclassified) — manual review | unresolved | Session clock accumulates ONLY REELS+visible time via `Date.now()`-derived `env.clock.now()` deltas at event boundaries; the only accumulation path is `tick(now)` called from boundary handlers and the rAF poll; no timer ticks anywhere; negative deltas clamped; single segments capped at `segmentCapMs` (900000). Verifier must confirm the exclusion contract (SOCIAL/UNKNOWN/hidden) and the no-timer scan. |
| FA-02 | CORE-04 (unclassified) — manual review | unresolved | DI seam contract as locked: `init(env)` accepts injected `clock`/`document`/`window`/`MutationObserver`/`requestAnimationFrame` with global fallbacks; engine body never touches bare globals outside `resolveEnv`; `setContext` added to the public API with default context `UNKNOWN` (fail-safe); env shape validation completes in Plan 04. Verifier must confirm mock-vs-live divergence is impossible by construction. |
| FA-06 | CORE-06 (unclassified) — manual review | unresolved | FakeClock `advance(ms)` is the keystone: time moves only on `advance`; one `raf.flush()` = one frame = one `tick`; the same `src/slowgram.js` runs under mocks (`node test/slowgram.test.js`) and in `test/harness.html` (browser host); tests never assert absolute time, only deltas. Verifier must confirm the two-host contract in Plan 04's smoke. |

## Artifacts this phase produces

- `src/slowgram.js` — IIFE engine: `SlowGram` global handle; functions `resolveEnv(overrides)`, `initConfig()`, `deepFreeze(object, seen)`, `updateRunning()`, `setContext(context)`, `tick(now)`, `pollLoop()`, `bindLifecycle(env)`, `init(overrides)`, `getState()`, `getConfig()` (test handle), `on(event, cb)`, `emit(event, data)`, `destroy()` (stub); `state` object `{elapsedMs, phase, context, visible, hiddenAt, lastBoundary, running}`; module const `CONFIG` (deep-frozen, `{segmentCapMs}` in this plan); bus events `'contextchange'` and `'elapsed'`; `phaseMachine.sync` (no-op stub, filled Plan 02)
- `test/harness.js` — `FakeClock(startMs)` `{now, advance}`, `FakeDocument(init)` `{visibilityState, addEventListener, dispatchEvent, setVisibility}`, `FakeWindow()` `{addEventListener, dispatchEvent}`, `FakeMutationObserver` stub, `FakeRAF` `{request, flush}`, `assert` runner `{equal, ok, throws}`, `renderResults(containerId)`
- `test/slowgram.test.js` — clock suite: accumulation contract, SOCIAL/UNKNOWN/hidden exclusions, negative-delta clamp, segment cap, elapsed/contextchange emission, invalid-context throw, DI seam reflection, no-timer source scan
- `test/harness.html` — browser runner page: `<script>` load order engine → harness → tests, `renderResults` pass/fail table

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected-env → engine | `init(overrides)` accepts arbitrary objects as `clock`/`document`/`window`/`MutationObserver`/`requestAnimationFrame` from a harness or a (potentially hostile) host page |
| engine → host page | engine event handlers run inside Instagram's page global scope; an uncaught engine exception propagates to the host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | DoS | event handlers + init() in src/slowgram.js | high | mitigate | Bootstrap try/catch containment: `init()` and every event-handler body wrap their work in try/catch that console.error's and returns — engine failures never propagate to the host page's global scope |
| T-01-02 | Tampering | global namespace | medium | mitigate | Single `SlowGram` handle on the injected global; everything else closure-private; `'use strict'` at IIFE top |
| T-01-03 | Tampering | CONFIG.segmentCapMs accumulation constant | medium | mitigate | `deepFreeze` recursion freezes the object at initConfig() time; strict mode converts any write into a TypeError (full CONFIG shape and config-suite assertions land in Plan 02) |
| T-01-04 | Spoofing | env.clock injected by host | low | accept | The container/WebView is a trusted host per RESEARCH.md Security Domain; clock spoofing is a container-domain trust boundary documented, not defended against in Phase 1 |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages by hard project constraint (CORE-04) — there is no dependency graph to audit; the package-legitimacy gate is N/A for this phase |

Mitigations reference the specific implementation described in Task 1 actions (try/catch in init and handlers; closure-private state; deepFreeze at initConfig; single SlowGram handle). No threat rated high is left unmitigated.
</threat_model>

<verification>
- `node test/slowgram.test.js` exits 0 (clock suite green on Node host)
- `test/harness.html` renders a green pass/fail table when opened in Edge (browser host smoke — formal two-host gate lands in Plan 04)
- Source scans: no timer scheduling API call sites; no bare `Date.now()` outside `resolveEnv`
</verification>

<success_criteria>
- The DI seam → injected clock → single tick() accumulation → getState() path is proven end-to-end on the Node host
- The same `src/slowgram.js` file is loaded by `test/harness.html` (browser host prepared)
- CORE-01 accumulation and exclusion contracts are testable with the fake clock today
</success_criteria>

<output>
Create `.planning/phases/01-motor-core-lifecycle/01-01-SUMMARY.md` when done
</output>