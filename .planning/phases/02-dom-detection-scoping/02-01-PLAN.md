---
phase: 02-dom-detection-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slowgram.js
  - test/harness.js
  - test/slowgram.test.js
  - test/fixtures/instagram-shapes.js
  - test/dom-mocks/instagram-mock.js
autonomous: true
requirements:
  - DETC-01
  - DETC-07
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "classifyPathname('/reels/') === 'REELS' and classifyPathname('/direct/') === 'SOCIAL' and classifyPathname('/') === 'UNKNOWN'"
    - "a FakeElement tree with [role=main] containing a <video> under pathname /reels/ yields setContext('REELS') and a VideoRegistry entry after one rAF batch"
    - "node test/slowgram.test.js exits 0 with the prior Phase 1 suites still green"
    - "test/fixtures/instagram-shapes.js encodes BOTH the logged-out live-dump shape and the CITED logged-in shape, each tagged with its source"
    - "test/dom-mocks/instagram-mock.js builds [role=main] and [role=dialog] trees from the fixture shapes via the FakeElement factory"
  artifacts:
    - src/slowgram.js
    - test/harness.js
    - test/slowgram.test.js
    - test/fixtures/instagram-shapes.js
    - test/dom-mocks/instagram-mock.js
  key_links:
    - "env.window.location.pathname is the ONLY pathname source (FakeLocation drives it) — never window.location.href"
    - "FakeMutationObserver.record() feeds the rAF batch exactly like FakeRAF.flush() drives the poll loop"
    - "all new selectors/route keywords read from CONFIG via SlowGram.getConfig() — no magic strings in module bodies"
  prohibitions:
    - statement: "The engine never queries the DOM by CSS class (no class-selector prefixes in CONFIG.selectors values, no class-based query API calls anywhere in src/slowgram.js)."
      status: unverified
      flagged: true
    - statement: "The engine never registers or degrades a video while pathname is not /reels/ or /reels/<id> (pathname authoritative, D-02)."
      status: unverified
      flagged: true
---

<objective>
Wave 1 tracer: the thinnest end-to-end slice of the Phase 2 detection spine — harness fakes growth (FakeElement mini-DOM, record-producing FakeMutationObserver, FakeVideoElement, FakeLocation), CONFIG real selector/route/health values, a minimal classifyPathname + ContextDetector + DomWatcher + VideoRegistry + SelectorRegistry wiring, plus the fixture data files and real-DOM verification that make detection deterministic in both hosts.

Purpose: prove the architecture (pathname → classifyPathname → setContext → DomWatcher observes feed root → FakeMutationObserver.record(video) → rAF batch → VideoRegistry.register → healthScan hit) works end-to-end before any expansion. A dead end in the fakes or the observer contract costs one commit, not ten committed layers.
Output: `src/slowgram.js` (detection spine skeleton), `test/harness.js` (FakeElement, FakeMutationObserver with record(), FakeVideoElement, FakeLocation), `test/slowgram.test.js` (tracer suite), `test/fixtures/instagram-shapes.js` (live-dump-derived fixture data), `test/dom-mocks/instagram-mock.js` (fixture builder).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dom-detection-scoping/02-RESEARCH.md
@.planning/phases/02-dom-detection-scoping/02-PATTERNS.md
@.planning/phases/02-dom-detection-scoping/02-CONTEXT.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Detection spine end-to-end — classify, observe, register, health-scan one /reels/ video</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — Pattern 1 (classifyPathname decision table, lines 181-198), Pattern 2 (two-root observer set, 209-260), Pattern 3 (WeakMap VideoRegistry, 235-260), Pattern 4 (Self-Mutation Filtering, 249-261), Pattern 5 (FakeElement factory, 263-287), Common Operation 2 (healthScan/SelectorRegistry, 382-398), Anti-Patterns (289-296), Runtime seam note (Common Operation 1 note, line 380)
    - 02-PATTERNS.md — CONFIG additions (lines 74-100), harness fakes spec (157-205), freshEnv extension (217-243), source-scan idiom (245-250)
    - 02-CONTEXT.md — D-01, D-02, D-05, D-09, D-11, D-12, D-13 (the decisions this task wires)
  </read_first>
  <action>
    Create the tracer slice. This task IS the tracer: one path — `FakeLocation.pathname='/reels/' → SlowGram.init(mocks) → context REELS → DomWatcher observes [role="main"] → FakeMutationObserver.record([{addedNodes:[video]}]) → raf.flush() → VideoRegistry contains video → getSelectorHealth().status === 'ok'` — wired through every layer, production-quality, committed for keeps.

    src/slowgram.js — extend the existing IIFE (same file, same idioms as Phase 1, all new code closure-private):
    - CONFIG additions (read via getConfig(); deep-freeze already covers nested nodes per T11): `selectors: { video: 'video', roleMain: '[role="main"]', roleDialog: '[role="dialog"]' }`, `reelsPrefix: '/reels/'`, `routeKeywords: ['reels','direct','messages','p','explore','accounts','stories']`, `preservedRoutes: ['/direct/','/messages/','/p/','/explore/','/accounts/','/stories/']`, `health: { driftThreshold: 5 }`. The existing placeholder `selectors`/`preservedRoutes` keys get real values; add the new keys. All values come from D-04/D-05/D-11 (02-CONTEXT.md:22-23,33).
    - `currentPathname()` — returns `env.window && env.window.location ? env.window.location.pathname : '/'` (the seam source; never href).
    - `classifyPathname(pathname)` — the D-05 prefix + keyword-guard decision table (Pattern 1):
      1. REELS (D-02 authoritative): `pathname === CONFIG.reelsPrefix || pathname.indexOf(CONFIG.reelsPrefix + '/') === 0` → `'REELS'` (covers `/reels/` and `/reels/<id>`; D-01: nothing else is REELS).
      2. SOCIAL (preserved prefixes, D-04): for each prefix in `CONFIG.preservedRoutes`, `pathname === prefix || pathname.indexOf(prefix + '/') === 0` → `'SOCIAL'`.
      3. SOCIAL (profile guard, D-05): single-segment pathname `'/<username>/'` whose segment is not in `CONFIG.routeKeywords` → `'SOCIAL'` (profiles preserved, no curated allowlist). Single-segment = pathname matches `^\/([^\/]+)\/?$`.
      4. UNKNOWN (fail-safe, DETC-03): everything else — home `/`, `/{username}/reels/`, `/reel/<id>/`, etc. → `'UNKNOWN'`. UNKNOWN never degrades, never connects the observer.
    - `ContextDetector` minimal — `detect()`: `var c = classifyPathname(currentPathname()); if (c !== 'SOCIAL') setContext(c); else setContext('SOCIAL');` — i.e. it simply classifies and pushes through the existing setContext bus (which already guards change + emits contextchange). In this task the DOM-refine signal is stubbed to no-op (the full D-13 refresh contract lands in Plan 02; the pathname→context link is the architectural spine, the refinement is a function-level fill).
    - `DomWatcher` minimal — module-var `watcher = { observer: null, connected: false }`; `connect()`: if `state.context === 'REELS'`, create `new env.MutationObserver(batchCallback)`, `observer.observe(feedRoot(), { childList: true, subtree: true, attributeFilter: ['src','loop','autoplay','role'] })`, set connected (the attributeFilter literal is the D-11 locked value — assert it in tests); `disconnect()`: `observer && observer.disconnect(); connected = false;`. `feedRoot()` = `env.document.querySelector(CONFIG.selectors.roleMain)` (null-safe). Hook connect/disconnect into `setContext` (extend the existing function minimally: on context change to REELS → connect, to SOCIAL/UNKNOWN → disconnect; D-12 connect-on-REELS-only). Keep the existing setContext validation and contextchange emission intact.
    - `VideoRegistry` minimal — module-var `WeakMap` `videoStates`; `register(video)`: `if (!videoStates.has(video)) videoStates.set(video, { registeredAt: env.clock.now() });` — idempotent; full lifecycle (loadstart/emptied) lands in Plan 03 (a function-level fill, no architectural change).
    - `SelectorRegistry` minimal — module-var `health = { missStreak: 0, drifted: false }`; `healthScan()` per Pattern 4/Common Operation 2: `anchor = env.document.querySelector(CONFIG.selectors.roleMain); videos = env.document.querySelectorAll(CONFIG.selectors.video); hit = (anchor !== null) || (videos.length > 0); if (hit) { health.missStreak = 0; if (health.drifted) recover(); } else { health.missStreak++; if (health.missStreak >= CONFIG.health.driftThreshold) health.drifted = true; }`. The N=5 drift event + fail-loud/fail-soft (D-08/D-10) land in Plan 04; here only missStreak accounting + `getSelectorHealth()` returning `{ status: health.drifted ? 'drift' : 'ok', missStreak: health.missStreak }`.
    - `SlowGram._classifyPathname(pathname)` — test-only handle exposing the pure decision table (follows the `_phaseFor` precedent; the T16-T20 guard pattern asserts it exists).
    - `SlowGram.getSelectorHealth()` — public handle (D-10 shape; the `selectorHealth` bus event lands in Plan 04).
    - Wire the rAF batch: extend the existing `pollLoop` (or add a sibling frame callback) so each frame, when `state.context === 'REELS'`, drains `watcher.observer.takeRecords()` into `batchCallback(records)` which: (1) for each added/attributeChanged record whose target is a video element (tagName 'VIDEO'), calls `VideoRegistry.register(target)`; (2) calls `healthScan()`. `batchCallback` performs NO synchronous DOM queries itself (Anti-Pattern: querySelectorAll in the callback → jank; the records carry the targets). No timer APIs anywhere (Phase 1 ban — the existing source scan must stay green).
    - Every new handler body wrapped in try/catch (containment per Phase 1 security domain); engine must never break the host page.

    test/harness.js — extend, don't restructure (keep the IIFE + global-attach + module.exports dual-host shape):
    - `FakeElement(tagName, attrs, children)` — per Pattern 5 (lines 267-285): nodeType 1, children/parentNode wiring, getAttribute/hasAttribute/setAttribute, addEventListener/dispatchEvent (listeners map), closest() returning null, contains() recursive. ADD the query surface the engine's real selectors need (per line 269 note): `matches(selector)` and `querySelector(selector)`/`querySelectorAll(selector)` supporting ONLY the 3 registered selectors — `'video'`, `'[role="main"]'`, `'[role="dialog"]'` — implemented by walking the children recursively against tagName/role attribute. `querySelector` returns first match or null; `querySelectorAll` returns array. `tagName` is normalized to uppercase for the engine's `'VIDEO'` comparison.
    - `FakeMutationObserver(callback)` — record-producing: `observe(target, config)` stores `{target, config}`; `disconnect()` clears; `takeRecords()` returns and drains the internal records array; test helper `record(mutations)` appends mutation objects `{type, addedNodes, removedNodes, attributeName, target}` to the internal queue (mirrors FakeRAF.flush() driving the batch; per PATTERNS.md lines 183-188). Also expose the last `observe` call via a property (e.g. `lastObserved`) so tests can assert the target + config.
    - `FakeVideoElement(tagName, attrs, children)` — extends FakeElement with a writable `src` property; `dispatchEvent` supports `'loadstart'`/`'emptied'` (DETC-05 lifecycle, used by Plan 03).
    - `FakeLocation(pathname)` — `{ pathname: pathname, history: { pushState: function(){}, replaceState: function(){} }, dispatchEvent: function(ev){ ... } }` with test helpers to fire `popstate`/`hashchange` (used by Plan 02; here just pathname read/write + the shape).
    - `freshEnv()` extension: add `location` (FakeLocation default `'/'`), `observer` (FakeMutationObserver), and a helper `env.elem(...)` constructing FakeElement trees. Existing tests must keep passing unchanged — add new fields, do not rename existing ones.

    test/slowgram.test.js — tracer suite (DETC-01/07 spine), appended after the Phase 1 suites:
    - T-D1: `_classifyPathname` decision table spot-check — `'REELS'` for `/reels/` and `/reels/abc`, `'SOCIAL'` for `/direct/`, `/messages/`, `/p/post-id/`, `/explore/`, `/accounts/`, `/stories/`, `'SOCIAL'` for `/someuser/`, `'UNKNOWN'` for `/`, `/someuser/reels/`, `/reel/abc/` (A4/A5 verified arithmetic). Guarded by the `typeof SlowGram._classifyPathname === 'function'` pattern.
    - T-D2: end-to-end — `init({clock, document: FakeDocument + FakeElement tree with role=main > video, window: FakeWindow + FakeLocation('/reels/'), MutationObserver: FakeMutationObserver, requestAnimationFrame: raf})`; `setContext` path fires: after init, assert `getState().context === 'REELS'` (ContextDetector ran on init); assert `observer.lastObserved.target` is the role=main element and `observer.lastObserved.config.attributeFilter` deep-equals `['src','loop','autoplay','role']`; `observer.record([{type:'childList', addedNodes:[video], target: feedRoot}])`; `raf.flush()`; assert the video is registered (via a test-only `_registrySize()` handle returning `videoStates.size` — add it next to `_classifyPathname`); assert `getSelectorHealth().status === 'ok'`.
    - T-D3: pathname authoritative (D-02) — same tree but `FakeLocation('/')`: context `'UNKNOWN'`, `observer.lastObserved` is undefined (no connect), `_registrySize() === 0` after record+flush (UNKNOWN never degrades, DETC-03 spine).
    - T-D4: DOM does not upgrade a non-reels pathname — `FakeLocation('/someuser/')` with a video present: context stays `'SOCIAL'` (profile preserved, D-04), `_registrySize() === 0`.
    - Cross-cutting source scan (extend the existing idiom at lines ~718-734): assert `src/slowgram.js` (comments stripped) contains no calls to class-based DOM query APIs (the CONFIG.selectors values must contain no `.`-prefixed selector, and the engine text must not contain the class-based query API names); assert no timer scheduling API calls (existing scan stays green).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "T-D1..T-D4 pass and all Phase 1 suites (T1-T40, D1-D6, prior source scans) remain green"
    - "classifyPathname returns REELS only for /reels/ or /reels/<id> (D-01/D-02)"
    - "DomWatcher.observe is called exactly with {childList:true, subtree:true, attributeFilter:['src','loop','autoplay','role']} on the role=main root when context is REELS"
    - "UNKNOWN and profile-SOCIAL paths register zero videos even with a video present in the tree"
  </acceptance_criteria>
  <done>One /reels/ video is classified REELS, observed on the role=main root, registered via a recorded mutation + one rAF flush, and health reports ok; UNKNOWN/SOCIAL paths register nothing; all prior suites stay green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Instagram DOM fixture data + mock builder + real-DOM verification</name>
  <files>test/fixtures/instagram-shapes.js, test/dom-mocks/instagram-mock.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — verified live dump evidence (line 545: role=main 1 hit, 4× video with blob src, role inventory, aria-labels, zero dialog logged-out), Pitfall 6 (encode BOTH fixture shapes, tag sources, lines 347-351), Pattern 5 fixture-builder note (line 287)
    - 02-PATTERNS.md — dom-mocks/fixtures purpose (lines 36, 274)
    - The verified on-disk dump at C:\Users\Usuario\AppData\Local\Temp\eco-ig-dump.html (captured 2026-08-15, 860727 bytes) — the logged-out shape source
  </read_first>
  <action>
    Create the DETC-07 fixture layer and run real-DOM verification. Both fixture shapes are encoded from real evidence, each tagged with its source; the mock builder composes FakeElement trees from them so every later DETC test runs deterministic Instagram-shaped DOM.

    test/fixtures/instagram-shapes.js — a plain object literal, no engine dependency:
    - `loggedOut` shape (source: live dump C:\Users\Usuario\AppData\Local\Temp\eco-ig-dump.html, verified 2026-08-15): `{ source: 'live-dump-2026-08-15', roleMain: 1, videos: 4, videoAttrs: ['playsinline','preload','src'], srcPattern: 'blob:https://www.instagram.com/<uuid>', roles: ['button','group','img','link','main','presentation','slider'], hasDialog: false, hasLoop: false, ariaLabels: ['Video player','Press to play','Play button icon','Adjust volume','Audio is muted','Comment','Like','Share','More'] }` — derived from RESEARCH.md:545 (the actual class values like `x1lliihq` are documented as obfuscated and MUST NOT appear in fixtures or engine code).
    - `loggedIn` shape (source: CITED community evidence — kbrianps/instagram-video-controls; NOT live-verified, non-load-bearing): `{ source: 'cited-community', hasLoop: true, hasAutoplay: true, hasDialog: true, dialogRole: '[role="dialog"]' }`.
    - A `SHAPES` export object + `sourceTags` map so tests can assert which evidence each fixture carries (Pitfall 6: never let the two shapes blur). Attach to the browser host as a single namespaced global `window.instaShapes = { SHAPES, sourceTags, verifiedSelectors }` (the name `instaShapes` is pinned and referenced by Plan 04's demo driver — do not rename).
    - Include a `verifiedSelectors` array: `['video', '[role="main"]', '[role="dialog"]']` — the only selectors the engine may use (DETC-06).

    test/dom-mocks/instagram-mock.js — the fixture builder (pattern: harness.js factory idiom, closure-private, no globals beyond a single namespaced attach for the browser host): `window.instaMocks = { buildReelsFeed, buildDialogRoot, buildSocialRoute }` (the name `instaMocks` is pinned and referenced by Plan 04's demo driver — do not rename).
    - `buildReelsFeed(shape)` — assembles a FakeElement tree matching the shape: one `role="main"` element containing `shape.videos` video children (logged-out: no loop/autoplay attrs, blob-style src; logged-in: adds loop + autoplay attrs). Uses the FakeElement factory from test/harness.js — in the browser host the script order must be harness.js before instagram-mock.js.
    - `buildDialogRoot(shape)` — for the loggedIn shape, a `role="dialog"` root containing one video (D-03: the fullscreen viewer counts as REELS; A2/A3: dialog is CITED and a sibling overlay — the mock mirrors that).
    - `buildSocialRoute(route)` — a minimal tree for `/direct/`, `/messages/`, `/p/...` etc. (no role=main with videos, per verified shape) so SOCIAL-route tests have realistic DOM.

    Real-DOM verification (required work per ROADMAP/STATE — not optional):
    - Capture a fresh snapshot: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-ig-capture" --dump-dom "https://www.instagram.com/reels/" 2>$null | Out-String | Set-Content -Path "$env:TEMP\eco-ig-dump-phase2.html" -Encoding UTF8`. If the network capture fails (no connectivity / Instagram blocking headless), fall back to the verified on-disk dump at C:\Users\Usuario\AppData\Local\Temp\eco-ig-dump.html and record that in the summary.
    - Validate against the fixture: assert `role="main"` count is 1, `<video` count >= 1, and that no `class="x…"` value is referenced by any selector the engine uses (spot-check via Select-String on the dump for the 3 registered selectors). If the fresh dump contradicts the fixture (e.g. zero videos), update `loggedOut` shape fields and note the divergence in the summary — do NOT silently keep a stale fixture.
    - Wire into tests: a small T-D5 block loads `test/fixtures/instagram-shapes.js` (via require in Node; the browser host keeps the script-order pattern) and asserts the `loggedOut` shape matches the validated dump facts (roleMain===1, videos>=1, hasDialog===false, hasLoop===false). The fixture file itself is committed; the test runs without network (fixture is static data).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "test/fixtures/instagram-shapes.js and test/dom-mocks/instagram-mock.js exist and are committed"
    - "both fixture shapes are present and each carries its source tag (live-dump vs cited-community)"
    - "the real-DOM capture ran (fresh dump or verified fallback) and the loggedOut fixture matches the validated dump facts"
    - "node test/slowgram.test.js exits 0 with T-D5 green and all prior suites still green"
  </acceptance_criteria>
  <done>Both fixture shape files and the mock builder are committed, source-tagged, and match the validated live dump; the real-DOM capture ran (fresh or verified fallback); T-D5 passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page DOM → engine | Instagram markup is untrusted input; role/attribute values read via querySelector/observer must never be executed or trusted for anything beyond classification |
| location.pathname / history → engine | navigation signals drive classification; a hijacked or spoofed pathname must never expand the degradation scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | classifyPathname / ContextDetector | high | mitigate | pathname authoritative (D-02): DOM signals refine but never upgrade; UNKNOWN never degrades (DETC-03); decision table asserted in T-D1/T-D4 |
| T-02-02 | Tampering | DOM spoofing (fake role/attr feeding false video signals) | high | mitigate | only the 3 registered selectors are ever queried (fixtures assert); register() only fires from observer records on video elements; never execute DOM-derived strings |
| T-02-03 | Denial of Service | observer feedback / jank | medium | mitigate | narrow roots (feed + dialog), attributeFilter limits to 4 attrs, takeRecords() drained once per rAF batch, batchCallback does zero synchronous DOM queries (Anti-Pattern, RESEARCH.md:291) |
| T-02-04 | Tampering | selector drift (markup regeneration) | high | mitigate | SelectorRegistry missStreak accounting from this plan; drift event + fail-loud/fail-soft land in Plan 04 (D-08..D-10); fixture refresh runbook HARN-04 |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — all fakes hand-rolled per project ban; no install task exists, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0
- Phase 1 suites (T1-T40, D1-D6, existing source scans) stay green — the fakes grew, not restructured
- The 3 registered selectors (`video`, `[role="main"]`, `[role="dialog"]`) appear in CONFIG via getConfig() and nowhere as magic strings
- Source scan: no class-selector usage, no timer APIs, no DOM queries inside the mutation batch callback
</verification>

<success_criteria>
- The one path — pathname /reels/ → REELS → observe [role=main] → recorded video mutation → rAF batch → registered → health ok — works end-to-end and is committed
- Fixture data files exist, are source-tagged, and match the validated live dump
- UNKNOWN and profile-SOCIAL pathnames never register a video (trust contract spine)
</success_criteria>

<output>
Create `.planning/phases/02-dom-detection-scoping/02-01-SUMMARY.md` when done
</output>