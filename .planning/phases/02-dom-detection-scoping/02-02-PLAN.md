---
phase: 02-dom-detection-scoping
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/slowgram.js
  - test/harness.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - DETC-01
  - DETC-02
  - DETC-03
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ContextDetector refreshes context from pathname events, role/attribute mutations, and each rAF batch (D-13), but a video present on a non-reels pathname never upgrades the context"
    - "RouteGuard re-asserts preservation on pushState, replaceState, popstate, hashchange, AND the rAF pathname re-check fallback (D-06)"
    - "UNKNOWN context (home /, /{username}/reels/, /reel/<id>/) never degrades: no observer connect, no video registration (DETC-03)"
    - "profiles /<username>/ and all preserved prefixes (/direct/, /messages/, /p/, /explore/, /accounts/, /stories/) classify SOCIAL and never degrade"
    - "node test/slowgram.test.js exits 0 with the full DETC-01/02/03 suites green"
  artifacts:
    - src/slowgram.js
    - test/harness.js
    - test/slowgram.test.js
  key_links:
    - "classifyPathname is the single decision source consumed by ContextDetector, RouteGuard, and DomWatcher — no duplicated matching logic"
    - "RouteGuard signals all funnel into ContextDetector.refresh(); DomWatcher reacts to context change via setContext, never to the signals directly"
    - "FakeLocation.pathname is the only pathname the engine reads; route signals dispatch through FakeLocation's popstate/hashchange helpers"
  prohibitions:
    - statement: "DOM signals (video presence, role attributes) never upgrade a non-reels pathname to REELS — classification requires pathname authority (D-02)."
      status: unverified
      flagged: true
    - statement: "No timer-based route polling exists — the rAF pathname re-check piggybacks the DomWatcher batch (Phase 1 timer ban, D-06)."
      status: unverified
      flagged: true
---

<objective>
Wave 2 expansion: the full ContextDetector and RouteGuard contracts — the complete D-05 decision table with keyword-guard profiles, the D-13 refresh contract (pathname events + role/attr mutations + rAF batch, with the never-upgrade rule), and the D-06 RouteGuard interception surface (pushState/replaceState + popstate + hashchange + rAF re-check fallback) that re-asserts preservation on every SPA navigation.

Purpose: make classification and route-preservation exhaustive and provably fail-safe. The tracer proved the spine; this plan fills every classification path and every navigation signal so the trust contract (social routes never degrade, UNKNOWN never degrades) is deterministic across all real navigation types (Pitfall 7).
Output: complete `classifyPathname` decision table, ContextDetector refresh + DOM-refine gating, RouteGuard bindings, and their full test suites.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dom-detection-scoping/02-RESEARCH.md
@.planning/phases/02-dom-detection-scoping/02-PATTERNS.md
@.planning/phases/02-dom-detection-scoping/02-CONTEXT.md
@.planning/phases/02-dom-detection-scoping/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ContextDetector full contract — decision table completeness, D-13 refresh sources, never-upgrade rule</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — Pattern 1 (181-198), Pattern 2 root-set note re dialog (209-213), D-13 refresh sources (line 162), Anti-Pattern "Classification that requires video presence" (294), Pitfall 1 (317-321)
    - 02-PATTERNS.md — ContextDetector contract (lines 100-130), rAF poll batch carrier (122), source-scan idiom (245-250)
    - 02-CONTEXT.md — D-01, D-02, D-03, D-05, D-13
  </read_first>
  <action>
    Complete ContextDetector to its locked contract. The tracer left it as pathname→classify→setContext; this task adds the D-13 refresh sources and the DOM-refine gating, and exhausts the decision table.

    src/slowgram.js:
    - `classifyPathname(pathname)` — finalize the decision table exactly per D-05:
      1. REELS: `pathname === CONFIG.reelsPrefix || pathname.indexOf(CONFIG.reelsPrefix + '/') === 0` → REELS (D-02). NOTE: `'/reels'` (no trailing slash) does NOT match — the prefix contract is `/reels/` with slash (assert this edge).
      2. SOCIAL preserved prefixes (D-04): `pathname === prefix || pathname.indexOf(prefix + '/') === 0` for each of `CONFIG.preservedRoutes` → SOCIAL.
      3. SOCIAL profile guard (D-05): single-segment `^\/([^\/]+)\/?$` where segment is NOT in `CONFIG.routeKeywords` → SOCIAL. Edge: keyword as a username is impossible by construction (keyword list wins), but a bare `/reels` without slash hits the profile guard? NO — `/reels` is single-segment and IS in routeKeywords, so the guard excludes it → falls to UNKNOWN. Verify with an assertion (D-05 arithmetic: single-segment pathnames that DO start with a known keyword are not profiles).
      4. UNKNOWN: everything else (home `/`, `/{username}/reels/`, `/reel/<id>/`, `/{keyword}-suffix` oddities) → UNKNOWN (DETC-03 fail-safe). A4/A5 verified: `/someuser/reels/` and `/reel/abc/` are UNKNOWN.
    - `ContextDetector` full:
      - `refresh(source)` — reclassifies from `classifyPathname(currentPathname())` and pushes through setContext (change-guarded). `source` is one of `'pathname' | 'mutation' | 'batch' | 'route'` (logged nowhere — the argument exists to keep call sites explicit and testable; no external logging).
      - Refresh sources wired (D-13): (1) pathname events — every RouteGuard signal calls `refresh('route')`; (2) role/attr mutations — the DomWatcher batch, when it sees a mutation touching role attributes on the two roots, calls `refresh('mutation')`; (3) rAF batch — each frame's batch calls `refresh('batch')` BEFORE draining records (cheap: one classifyPathname per frame, piggybacks the existing poll loop; no timer).
      - DOM-refine gating (D-13 tail + D-02): `refineFromDOM()` — ONLY when `classifyPathname(currentPathname()) === 'REELS'`, check whether the feed root or dialog root holds a video; if yes, keep REELS (that's the refine signal for the empty-reels-tab edge — actually the edge is ACCEPTED per D-02: a REELS pathname with zero videos stays REELS, so refinement is: REELS pathname + video → REELS (no-op), REELS pathname + no video → still REELS; non-REELS pathname + any video → NEVER upgrades). Implement `refineFromDOM()` as a guard that returns the context UNCHANGED for non-REELS pathnames — the anti-upgrade rule is the load-bearing behavior (Anti-Pattern line 294).
      - `getContext()` — via existing `getState().context`; no new handle needed.
    - Keep `SlowGram._classifyPathname` test handle (Plan 01) — the full decision-table suite drives it.

    test/harness.js — no new fakes required for classification, but extend FakeLocation with `setPathname(p)` helper that writes `location.pathname` AND dispatches `popstate` if the test asks (or keep it purely read/write and let RouteGuard tests dispatch explicitly — pick the explicit-dispatch design so tests control signal type; document the choice in a comment).

    test/slowgram.test.js — DETC-01/03 suite (append; keep T- prefix numbering scheme — continue T-D6+):
    - T-D6: decision table exhaustiveness — for EVERY route shape table in 02-RESEARCH Pattern 1, assert expected class: `/reels/`→REELS, `/reels/abc/`→REELS, `/reels`→UNKNOWN (no-slash edge), `/direct/`→SOCIAL, `/messages/`→SOCIAL, `/messages/inbox/`→SOCIAL, `/p/postid/`→SOCIAL, `/explore/`→SOCIAL, `/explore/search/`→SOCIAL, `/accounts/`→SOCIAL, `/stories/`→SOCIAL, `/someuser/`→SOCIAL, `/someuser`→SOCIAL (profile guard, trailing slash optional), `/`→UNKNOWN, `/someuser/reels/`→UNKNOWN, `/reel/abc/`→UNKNOWN, `''`→UNKNOWN (empty), `/someuser/direct/`→UNKNOWN? — NO: `/someuser/direct/` is two segments and not a preserved prefix → UNKNOWN (assert it; two-segment profiles are not preserved by the D-05 single-segment guard).
    - T-D7: never-upgrade (D-02/DETC-03): init with a video-rich tree on `/someuser/` → context SOCIAL; on `/` → UNKNOWN; on `/p/x/` → SOCIAL; each with `observer.record(video mutation)` + `raf.flush()` → `_registrySize() === 0` in all three.
    - T-D8: refresh sources — (a) `setPathname('/direct/')` + dispatch popstate → context SOCIAL; (b) mutation on roleMain that removes role attribute → `refresh('mutation')` fires via batch → context stays REELS (pathname still /reels/) — assert the anti-upgrade both ways: mutation can trigger refresh but never changes a pathname-authoritative verdict.
    - T-D9: empty-reels-tab edge accepted (D-02): `/reels/` with ZERO videos → context REELS, `_registrySize() === 0`, `getSelectorHealth().status === 'ok'` (anchor hit counts as health even with no video — healthScan anchor logic from Plan 01).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "every row of the Pattern 1 decision table asserts the expected class in T-D6"
    - "a video present on any non-reels pathname never produces a REELS context nor a registry entry"
    - "UNKNOWN contexts (home, profile-reels subpages, /reel/<id>/) never connect the observer and never register"
    - "all Phase 1 + Plan 01 suites remain green"
  </acceptance_criteria>
  <done>The decision table is exhaustive (every Pattern 1 row asserted), no DOM signal upgrades a non-reels pathname, UNKNOWN contexts never connect or register, and all prior suites pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RouteGuard full contract — interception + rAF re-check fallback, re-assert on every navigation signal</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — Common Operation 1 (RouteGuard wiring, 361-380), Pitfall 7 (missed navigation types, 353-357), Pattern 1 classifier reuse (182)
    - 02-PATTERNS.md — route-event wiring analog (FakeWindow listeners map, lines 54-75; FakeLocation shape per lines 180-182)
    - 02-CONTEXT.md — D-04, D-05, D-06, D-07
  </read_first>
  <action>
    Implement the D-06 RouteGuard contract and its full test matrix. The tracer created no route bindings; this task adds interception of every SPA navigation signal plus the rAF re-check fallback, all funneling into ContextDetector.refresh.

    src/slowgram.js:
    - `bindRouteGuard()` — per Common Operation 1 (lines 364-378):
      - `h = env.window.history`; if present, wrap `h.pushState` and `h.replaceState` with bound originals; each wrapper calls `orig.apply(h, arguments)` then `ContextDetector.refresh('route')`. Store the originals and expose `unbindRouteGuard()` (teardown hygiene mirroring Phase 1 lifecycleHandlers) — destroy() must restore originals.
      - `w.addEventListener('popstate', onRouteSignal)` and `w.addEventListener('hashchange', onRouteSignal)` where `onRouteSignal = function(){ ContextDetector.refresh('route'); }` (wrapped in try/catch per containment).
      - rAF re-check fallback: the DomWatcher batch carrier (the existing per-frame callback) calls `ContextDetector.refresh('batch')` each frame while running — this is the bypass-proof fallback (Pitfall 7: SPA links / back-forward gestures / WebView navigations that skip pushState are caught within one frame).
    - Destroy/teardown: extend `SlowGram.destroy()` (existing) to call `unbindRouteGuard()` so re-init is clean (mirrors Phase 1 teardown discipline).
    - Re-assert semantics (D-06 tail + D-07): refresh is idempotent — flipping to a preserved route sets SOCIAL (which pauses the clock via existing setContext and, in Plan 03, disconnects the observer); flipping back to `/reels/` sets REELS. RouteGuard itself only routes signals; DomWatcher's connect/disconnect reacts via context (delegation documented in the code comment).
    - `currentPathname()` is the only pathname source (already in place from Plan 01) — RouteGuard must never read `window.location.href`.

    test/harness.js:
    - Extend FakeWindow with `history` (pushState/replaceState recordable + wrappable: `{ pushState: function(state,title,url){ history.calls.push(['pushState',url]); }, replaceState: function(...){...}, calls: [] }`) so tests assert the engine wrapped them.
    - FakeLocation: add `dispatchPopstate()` and `dispatchHashchange()` helpers that invoke registered listeners; keep `setPathname` (see Task 1) as the mutation entry point.

    test/slowgram.test.js — DETC-02 suite (T-D10+):
    - T-D10: pushState interception — init on `/reels/` (REELS), call `win.history.pushState(null,'','/direct/')` → context SOCIAL; `pushState(null,'','/reels/')` → context REELS again.
    - T-D11: replaceState interception — same pattern via `replaceState` (route flip REELS→SOCIAL and back).
    - T-D12: popstate — `loc.dispatchPopstate()` after `setPathname('/messages/')` → SOCIAL (assert the engine's popstate listener fires).
    - T-D13: hashchange — `loc.dispatchHashchange()` → refresh fires (context per current pathname).
    - T-D14: rAF re-check fallback — the bypass case: `setPathname('/p/somepost/')` WITHOUT any event dispatch, then `raf.flush()` → context SOCIAL (the batch carrier caught the change; no timer involved). Assert a timer-API source scan stays green (no setInterval anywhere).
    - T-D15: every preserved route re-asserts (D-04/D-05): iterate `CONFIG.preservedRoutes` + a profile pathname; for each, init REELS then pushState to the route → assert SOCIAL and `getState().elapsedMs` unchanged after `clock.advance(60000)` (clock paused on SOCIAL — ties RouteGuard to the trust contract).
    - T-D16: destroy() restores history originals and removes window listeners (re-init works cleanly; second init's RouteGuard still intercepts).
    - Cross-cutting source scan: assert `src/slowgram.js` (comments stripped) contains no timer scheduling API calls and no direct `window.location.href` read outside resolveEnv.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "pushState, replaceState, popstate, hashchange, AND the rAF batch re-check each independently re-assert context (T-D10..T-D14)"
    - "every preserved route and every single-segment profile classifies SOCIAL with the clock paused"
    - "destroy() unbinds the RouteGuard (originals restored, listeners removed) and re-init works"
    - "the timer-API source scan remains green — the rAF re-check is frame-based, never timer-based"
  </acceptance_criteria>
  <done>All five navigation signals (pushState, replaceState, popstate, hashchange, rAF re-check) independently re-assert context; every preserved route + profile classifies SOCIAL with the clock paused; destroy() unbinds cleanly and re-init works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history API / navigation events → engine | pushState/replaceState/popstate/hashchange are untrusted signals from the page; they must only ever drive re-classification, never expand scope |
| location.pathname → engine | the authoritative classification input; a spoofed pathname must never cause degradation outside /reels/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | History API hijack breaking classification | high | mitigate | pushState/replaceState wrapped with bound originals + popstate/hashchange listeners + rAF re-check fallback (D-06, Pitfall 7); destroy() restores originals |
| T-02-06 | Tampering | pathname spoofing (location rewrite) | high | mitigate | pathname authoritative (D-02); classifyPathname is the single decision source; UNKNOWN never degrades; source scan bans href reads |
| T-02-07 | Denial of Service | navigation-signal flood (rapid pushState spam) | low | mitigate | refresh is change-guarded via setContext (no-op on same context); one classifyPathname per frame max in the batch path |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0
- Decision table exhaustiveness proven by T-D6 (every Pattern 1 row asserted)
- All five navigation signal types proven by T-D10..T-D14 (no signal left untested — Pitfall 7)
- Clock-pause-on-SOCIAL proven by T-D15 (trust contract)
- destroy()/re-init proven by T-D16
</verification>

<success_criteria>
- Classification is exhaustive: REELS only for `/reels/`+suffix, SOCIAL for preserved prefixes + profiles, UNKNOWN fail-safe for everything else
- Every navigation type re-asserts preservation; a bypass navigation is caught within one rAF batch
- No route, profile, or UNKNOWN context ever degrades or registers a video
</success_criteria>

<output>
Create `.planning/phases/02-dom-detection-scoping/02-02-SUMMARY.md` when done
</output>