---
phase: 02-dom-detection-scoping
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
  - demo.html
autonomous: true
requirements:
  - DETC-06
  - DETC-07
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SelectorRegistry surfaces health via SlowGram.getSelectorHealth() AND a 'selectorHealth' bus event emitted on drift-declared and drift-recovered (D-10)"
    - "drift is declared at N=5 consecutive zero-hit scans (CONFIG.health.driftThreshold), counted per rAF batch, never on a timer (D-09)"
    - "dev/harness mode fails loud (console.warn + event); prod mode fails soft via the bounded document-scoped <video> fallback on /reels/ only (D-08)"
    - "CONFIG.selectors values contain no CSS class selector (no '.' prefix) and src/slowgram.js never queries the DOM by class (DETC-06)"
    - "demo.html loads src/slowgram.js + fixtures + mocks and renders a deterministic detection verdict (DETC-07)"
    - "node test/slowgram.test.js AND the Edge headless harness.html smoke both pass (TOTAL: N passed / N run)"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
    - demo.html
  key_links:
    - "healthScan runs inside the same per-frame batch as the DomWatcher drain — one scan per frame, no timer"
    - "getSelectorHealth() and the selectorHealth event both read the same health object (single source of truth)"
    - "demo.html script order: src/slowgram.js → test/harness.js → test/fixtures/instagram-shapes.js → test/dom-mocks/instagram-mock.js → demo inline script (mirrors harness.html's load order)"
  prohibitions:
    - statement: "No timer-based health check exists — the health scan rides the rAF batch only (Phase 1 timer ban, D-09)."
      status: unverified
      flagged: true
    - statement: "No CSS-class selector ever appears in CONFIG.selectors or in any engine DOM query (DETC-06)."
      status: unverified
      flagged: true
---

<objective>
Wave 4 completion: the SelectorRegistry health check (N=5 drift detection, selectorHealth event + getSelectorHealth handle, fail-loud dev / fail-soft prod bounded fallback) and the demo.html DETC-07 deliverable, finishing with the full two-host smoke (Node + Edge headless).

Purpose: make selector drift loud instead of silent (Pitfall 3 — Instagram regenerates markup ~monthly) and ship the deterministic demo that proves detection works against the fixture shapes in a plain browser. This plan closes the phase: every DETC requirement is now implemented and verified in both hosts.
Output: complete SelectorRegistry + demo.html + final verification.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dom-detection-scoping/02-RESEARCH.md
@.planning/phases/02-dom-detection-scoping/02-PATTERNS.md
@.planning/phases/02-dom-detection-scoping/02-CONTEXT.md
@.planning/phases/02-dom-detection-scoping/02-01-SUMMARY.md
@.planning/phases/02-dom-detection-scoping/02-02-SUMMARY.md
@.planning/phases/02-dom-detection-scoping/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SelectorRegistry health check — N=5 drift, selectorHealth event + getSelectorHealth, fail-loud dev / fail-soft prod fallback</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — Common Operation 2 (healthScan/SelectorRegistry, 382-398), Common Operation 3 (fail-soft fallback, 400-412), Pattern 4 (Self-Mutation Filtering, 249-261), Pitfall 3 (329-333), D-08/D-09/D-10 decision texts (02-CONTEXT.md:28-30)
    - 02-PATTERNS.md — health block in CONFIG (86), event bus reuse (17), source-scan idiom (245-250)
    - 02-CONTEXT.md — D-08, D-09, D-10
  </read_first>
  <action>
    Complete the SelectorRegistry health contract. Plan 01 added missStreak accounting + getSelectorHealth(); this task adds the drift-declared / drift-recovered events, the dev/prod mode split, the bounded fail-soft fallback, and the source scans proving class-free selectors.

    src/slowgram.js:
    - `health = { missStreak: 0, drifted: false }` (module-var, exists) + module-var `dev = false` (default prod).
    - `healthScan()` — per Common Operation 2 (lines 385-397), full version:
      - `anchor = env.document.querySelector(CONFIG.selectors.roleMain); videos = env.document.querySelectorAll(CONFIG.selectors.video); hit = (anchor !== null) || (videos.length > 0);`
      - `if (hit) { health.missStreak = 0; if (health.drifted) { health.drifted = false; emit('selectorHealth', { status: 'ok', pathname: currentPathname() }); } return; }`
      - `else { health.missStreak++; if (health.missStreak >= CONFIG.health.driftThreshold && !health.drifted) { health.drifted = true; emit('selectorHealth', { status: 'drift', pathname: currentPathname() }); if (dev) console.warn('SlowGram: selector drift — feed anchor missing for 5 scans'); } }`
      - Runs inside the existing per-frame batch (after the DomWatcher drain) — NO timer (Phase 1 ban; the existing timer-API source scan stays green).
    - Dev/prod mode (D-08):
      - Test handle `SlowGram._setDevMode(flag)` — sets module `dev`; default false (prod). (Follows `_phaseFor`/`_classifyPathname` precedent.)
      - Fail-loud dev: `console.warn` on drift (as above) — assertable via a stubbed console in tests.
      - Fail-soft prod: when `health.drifted === true`, the video scope falls back to `fallbackScope()` (Common Operation 3, lines 403-411): `var p = currentPathname(); return (p === CONFIG.reelsPrefix || p.indexOf(CONFIG.reelsPrefix + '/') === 0) ? env.document.querySelectorAll(CONFIG.selectors.video) : [];` — document-scoped <video> ONLY while pathname says /reels/ (bounded; never body-wide observation; never on other routes). Wire: the batch, when drifted, registers from `fallbackScope()` results instead of record targets (a function-level fallback — the scope source swaps, the registration path stays).
    - Source scans (extend the existing idiom, comments stripped):
      - Assert no CONFIG.selectors value contains '.' (class selector prefix) — read CONFIG via getConfig in the test and scan the values.
      - Assert src/slowgram.js text contains no class-based DOM query API calls (the class-query API names must not appear).
      - Assert no timer scheduling API calls (existing scan stays green).
    - Event: 'selectorHealth' plugs into the existing on/emit bus (Phase 1 emitter survives destroy/re-init — consumers subscribe once at page load, per CONTEXT.md:79).

    test/slowgram.test.js — DETC-06 suite (T-D32+):
    - T-D32: no class selectors — every value in CONFIG.selectors has no '.' prefix; scan the engine source for class-based query APIs (absent).
    - T-D33: drift declared at N=5 — REELS pathname, feed tree WITHOUT role=main (anchor missing): 4 consecutive `raf.flush()` (each running healthScan with zero hits) → no drift; 5th flush → `getSelectorHealth().status === 'drift'` AND a 'selectorHealth' event captured with `{status:'drift'}` (subscribe via SlowGram.on before).
    - T-D34: drift recovered — after drift, restore the anchor → next flush → `getSelectorHealth().status === 'ok'` + event `{status:'ok'}`; missStreak back to 0.
    - T-D35: fail-loud dev — `_setDevMode(true)`, stub console.warn, 5 zero-hit flushes → console.warn called once with the drift message; `_setDevMode(false)` → no warn.
    - T-D36: fail-soft prod fallback — `_setDevMode(false)`, drift declared, pathname /reels/ with page-scoped videos OUTSIDE role=main → next batch registers those videos (`_registrySize()` grows from fallbackScope); pathname switched to /direct/ (SOCIAL) → fallbackScope returns [] → no registration.
    - T-D37: per-batch not timer — the timer-API source scan green (already), plus assert healthScan is only invoked from the batch path (scan the source for the call site context).
    - T-D38: CONFIG.health.driftThreshold === 5 (locked value in CONFIG, T14-style assertion) — no magic literal 5 in module bodies.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "drift is declared at exactly the 5th consecutive zero-hit scan and recovered on the first hit after"
    - "selectorHealth event fires on drift-declared and drift-recovered with the documented payload; getSelectorHealth() reflects the same state"
    - "dev mode warns once per drift; prod mode registers from the bounded document-scoped fallback on /reels/ only"
    - "source scans prove zero class selectors, zero class-based query APIs, zero timers"
  </acceptance_criteria>
  <done>Drift declares at exactly the 5th zero-hit scan and recovers on the first hit; selectorHealth event + getSelectorHealth() agree; dev warns once, prod uses the bounded /reels/-only fallback; all class/timer source scans pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: demo.html — deterministic detection demo + final two-host smoke</name>
  <files>demo.html, test/slowgram.test.js</files>
  <read_first>
    - test/harness.html (the structural analog — script load order + status render; read it first)
    - 02-PATTERNS.md — demo.html analog mapping (line 36)
    - 02-RESEARCH.md — Pattern 5 fixture builder (287), verified aria-labels (545)
    - The fixture/mock files created in Plan 01 (test/fixtures/instagram-shapes.js, test/dom-mocks/instagram-mock.js)
  </read_first>
  <action>
    Build demo.html — the DETC-07 deliverable that proves the whole detection spine runs in a plain browser against Instagram-shaped DOM, deterministically.

    demo.html (project root — new file, modeled on test/harness.html's plain static orchestration):
    - Script load order (mirror harness.html): `src/slowgram.js` → `test/harness.js` (FakeElement/FakeMutationObserver/FakeVideoElement/FakeLocation + assert runner) → `test/fixtures/instagram-shapes.js` → `test/dom-mocks/instagram-mock.js` → a small inline `<script>` demo driver. No framework, no build step, no CDN.
    - The demo driver (plain ES5, textContent-only rendering — no innerHTML, matching the harness render idiom):
      1. Build a loggedIn-shape feed via `instaMocks.buildReelsFeed(instaShapes.SHAPES.loggedIn)` (role=main + videos with loop) plus `instaMocks.buildDialogRoot(...)` (D-03 dialog surface).
      2. Create a FakeWindow + FakeLocation(`/reels/`), FakeMutationObserver, FakeRAF, FakeClock; `SlowGram.init({clock, document, window, MutationObserver, requestAnimationFrame})` with the FakeDocument wrapping the built tree.
      3. Assert context becomes REELS; `observer.record([{type:'childList', addedNodes:[a video], target: feedRoot}])`; `raf.flush()`; read `SlowGram.getSelectorHealth()` and a registered-video count.
      4. Render a verdict panel as text: `Context: REELS`, `Registered videos: N`, `Selector health: ok`, `Drift threshold: 5` (values read live from SlowGram — getConfig().health.driftThreshold etc.).
      5. Route-flip demo: `setPathname('/direct/')` + `dispatchPopstate()` → verdict updates to `Context: SOCIAL` (observer disconnected).
    - Inline demo code must use the same containment pattern (try/catch) and the same APIs the tests use — demo.html is a user-facing proof, not a test.
    - Edge case: document.title + a `<div id="verdict">` rendered with textContent; no console errors (page must load clean under headless).

    Final two-host smoke (run both, record both results in the summary):
    - Node: `node test/slowgram.test.js` — exit 0, all suites green (count assertions, report the total).
    - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke2" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the Node assertion count (the dual-host parity contract from Phase 1).
    - demo.html smoke: same Edge headless dump against `file:///C:/Users/Usuario/Downloads/EcoInsta/demo.html`, asserting the verdict panel rendered (`Context: REELS` present in the dumped DOM).
    - If Edge is unavailable on the machine, document it and run the Node path only (Phase 1 already verified Edge works here — expect it available).

    test/slowgram.test.js — no new engine tests required in this task; add only a T-D39 assertion that demo.html exists and references the correct script order (read the file, assert the five script tags appear in order) so the deliverable is regression-guarded.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "demo.html exists at the project root and renders the live verdict (Context/Registered/Health) via textContent with the five scripts in documented order"
    - "Edge headless dump of harness.html shows TOTAL: N passed / N run with N === Node assertion count"
    - "Edge headless dump of demo.html shows 'Context: REELS' (and updates on the route-flip demo)"
    - "node test/slowgram.test.js exits 0 with the full phase suite green"
  </acceptance_criteria>
  <done>demo.html exists with the five scripts in documented order and renders Context: REELS (updating on route-flip); Edge headless shows TOTAL: N passed / N run on harness.html with N === Node assertion count, and Context: REELS in the demo.html dump; T-D39 passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page DOM structure → engine health | the presence/absence of anchors and videos is untrusted evidence; drift must never expand degradation scope beyond /reels/ |
| console / event bus → host | health signals are advisory; they must never throw or break the host page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | selector drift causing silent scope misclassification | high | mitigate | N=5 zero-hit scans → drift event + getSelectorHealth (D-09/D-10); fail-loud dev warn; fixture-refresh runbook HARN-04 |
| T-02-13 | Tampering | class-selector drift (hashed CSS classes) | high | mitigate | CONFIG.selectors values asserted class-free; engine source scanned for class-based query APIs (DETC-06); fixtures document the obfuscated classes as forbidden |
| T-02-14 | Denial of Service | drift fallback over-breadth (document-scoped query) | medium | mitigate | fallbackScope is bounded: only while pathname is /reels/ or /reels/<id>, only <video> nodes, never body-wide observation (D-08) |
| T-02-15 | Spoofing | fake health signals misleading the host | low | mitigate | health state derives only from real queries of the 3 registered selectors; event payload carries pathname for auditability |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump, `Context: REELS` in demo.html dump
- Drift lifecycle (declare at 5, recover at hit) proven by T-D33/T-D34; dev/prod split proven by T-D35/T-D36
- Class-free selector contract proven by T-D32 + source scans
</verification>

<success_criteria>
- Drift is loud (event + handle + dev warn) and its prod fallback is bounded to /reels/
- demo.html proves the full spine in a browser: detect → register → health → route-flip
- Phase 2 closes with all 8 DETC requirements implemented and verified in both hosts
</success_criteria>

<output>
Create `.planning/phases/02-dom-detection-scoping/02-04-SUMMARY.md` when done
</output>