---
phase: 02-dom-detection-scoping
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/slowgram.js
  - test/harness.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - DETC-04
  - DETC-05
  - DETC-08
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "DomWatcher observes ONLY the two roots — [role=main] and any [role=dialog] containing a video — with {childList:true, subtree:true, attributeFilter:['src','loop','autoplay','role']} (D-11)"
    - "mutation records are drained once per rAF batch via takeRecords(); the batch callback performs zero synchronous DOM queries (D-09)"
    - "self-mutations (engine's own writes) are filtered by the mutating flag + overlay-host subtree exclusion (D-14)"
    - "the observer connects ONLY when context is REELS, disconnects on SOCIAL/UNKNOWN (DETC-08), and reconnects with re-sync on return to /reels/ (D-12, D-07)"
    - "VideoRegistry keeps per-video state in a WeakMap, resets lifecycle on loadstart/emptied (DETC-05), and is NOT cleared on disconnect (D-07)"
    - "node test/slowgram.test.js exits 0 with DETC-04/05/08 suites green"
  artifacts:
    - src/slowgram.js
    - test/harness.js
    - test/slowgram.test.js
  key_links:
    - "takeRecords() → batchCallback → VideoRegistry.register is the only registration path; nothing else adds registry entries"
    - "setContext is the only thing that connects/disconnects the observer (D-12) — RouteGuard never touches the observer directly"
    - "FakeMutationObserver.record() + raf.flush() drive every observer test deterministically"
  prohibitions:
    - statement: "The batch callback never calls querySelector/querySelectorAll (Anti-Pattern, RESEARCH.md:291) — targets come from the mutation records alone."
      status: unverified
      flagged: true
    - statement: "VideoRegistry state survives disconnect — registry is never cleared on SOCIAL/UNKNOWN routes (D-07 keep-registry)."
      status: unverified
      flagged: true
---

<objective>
Wave 3 expansion: the full DomWatcher and VideoRegistry contracts — the two-root observer set with the locked attributeFilter, rAF-batched takeRecords() draining, self-mutation filtering (mutating flag + overlay-host subtree exclusion), connect-on-REELS-only / disconnect-on-SOCIAL/UNKNOWN lifecycle (DETC-08) with reconnect re-sync, and the WeakMap VideoRegistry with loadstart/emptied lifecycle reset.

Purpose: prove the observer architecture is narrow (Pitfall 2), feedback-loop-free (Pitfall 4), and lifecycle-correct for a virtualized feed (Pitfall 5), and that the disconnect-on-social contract (DETC-08) is real and tested. This is the layer Phase 3 levers will consume, so its record/register semantics must be exact.
Output: complete DomWatcher + VideoRegistry with their full test suites.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dom-detection-scoping/02-RESEARCH.md
@.planning/phases/02-dom-detection-scoping/02-PATTERNS.md
@.planning/phases/02-dom-detection-scoping/02-CONTEXT.md
@.planning/phases/02-dom-detection-scoping/02-01-SUMMARY.md
@.planning/phases/02-dom-detection-scoping/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DomWatcher full contract — two-root set, attributeFilter, rAF batch draining, self-mutation filter, connect/disconnect lifecycle</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — Pattern 2 (two-root observer set, 209-260), D-11 locked config (02-CONTEXT.md:33), D-14 self-mutation filter, Anti-Patterns (290-296), Pitfall 2 (323-327), Pitfall 4 (335-339), Common Operation 2 healthScan (382-398)
    - 02-PATTERNS.md — rAF poll batch carrier (122), teardown discipline (151), FakeMutationObserver spec (183-188), source-scan idiom (245-250)
    - 02-CONTEXT.md — D-03, D-07, D-09, D-11, D-12, D-14
  </read_first>
  <action>
    Complete DomWatcher to its locked contract. Plan 01 wired connect/disconnect + batch drain minimally; this task adds the second root (dialog), the self-mutation filter, connect/disconnect lifecycle rules, and reconnect re-sync, and hardens the batch against the documented anti-patterns.

    src/slowgram.js:
    - Two-root observer set (Pattern 2, D-11 + D-03):
      - `feedRoot()` — `env.document.querySelector(CONFIG.selectors.roleMain)` (already exists).
      - `dialogRoot()` — `env.document.querySelector(CONFIG.selectors.roleDialog)` that CONTAINS a video (A2/A3: CITED dialog-as-sibling; null when absent — logged-out dump has zero dialogs, so the R2 root is a no-op there but must be wired for the logged-in fixture). Implement `dialogRoot()` as: query the roleDialog selector; if the result is null → null; if it contains a video element → return it; else null.
      - `connect()` — only when `state.context === 'REELS'` (D-12 connect-on-REELS-only): create ONE `MutationObserver(batchCallback)`, call `observer.observe(feedRoot(), config)` AND `observer.observe(dialogRoot(), config)` for each non-null root, with `config = { childList: true, subtree: true, attributeFilter: ['src','loop','autoplay','role'] }` (D-11 literal — asserted in tests). `connected = true`. Null roots are skipped (no body-wide observation ever — Pitfall 2).
      - `disconnect()` — `observer.disconnect(); observer = null; connected = false;` — called on SOCIAL/UNKNOWN (DETC-08/D-12). Registry NOT touched (D-07 keep-registry).
      - Wire into setContext: on change to REELS → connect(); on change to SOCIAL/UNKNOWN → disconnect(). (Already stubbed in Plan 01; now complete with dialog root + guards.)
    - rAF batch (D-09):
      - Each frame while running: `var records = watcher.observer ? watcher.observer.takeRecords() : []; if (records.length) batchCallback(records); healthScan();` — takeRecords() drains the observer queue once per frame (Phase 1 poll loop pattern; the batch is the existing rAF carrier).
      - `batchCallback(records)` — for each record: if `record.type === 'childList'`, for each addedNode that is a VIDEO element → `VideoRegistry.register(node)`; if `record.type === 'attributes'` and `record.target` is a VIDEO element → `VideoRegistry.register(record.target)` (attribute change = possible src swap → re-register is idempotent). **Zero querySelector/querySelectorAll in this function** (Anti-Pattern line 291) — targets come only from records.
    - Self-mutation filter (D-14):
      - Module-var `mutating` flag: `setMutating(true)` before ANY engine write to the DOM (Phase 3 levers will use it; this phase only defines the flag and the filter), cleared after. In `batchCallback`, if `mutating === true` → skip processing (the records are the engine's own writes; Pitfall 4).
      - Overlay-host subtree exclusion: records whose `target` or addedNodes are inside the overlay host (Phase 4's overlay container) are skipped. Implement `isOverlayHost(node)` as a contains()-based check against a module-var `overlayHost` (null in Phase 2 — the exclusion path exists and is tested with a fake host injected via a test handle `_setOverlayHostForTest(node)`; the real overlay host lands in Phase 4). This is a function-level fill, not an architectural gap.
    - Reconnect re-sync (D-07 tail): on connect() when returning to REELS, run one immediate `healthScan()` + `takeRecords()` drain (records queued while disconnected are not lost — takeRecords on a fresh observer returns [] anyway; the re-sync = one health scan + one batch pass on the current DOM state). Keep simple: connect() triggers `processBatch()` once synchronously after observing.
    - `_registrySize()` test handle (already added in Plan 01) and a new `_getWatcherState()` handle exposing `{ connected, roots: [feedRoot, dialogRoot] }` for lifecycle assertions (follows `_phaseFor`/`_classifyPathname` precedent).

    test/harness.js:
    - FakeMutationObserver already records `{target, config}` + `lastObserved` (Plan 01). Add `recordAttributeMutation(target, attributeName)` helper and ensure `record()` accepts `{type:'attributes', target, attributeName}`.
    - FakeElement: `contains()` already recursive (Pattern 5). Add `appendChild(child)` / `removeChild(child)` helpers that maintain parentNode + children (needed to simulate feed node recycling in DETC-05 tests).

    test/slowgram.test.js — DETC-04/08 suite (T-D17+):
    - T-D17: two-root observe — REELS context with a loggedIn fixture (role=main + role=dialog with video): assert `_getWatcherState().connected === true`, `lastObserved` called for BOTH roots with the exact locked config `{childList:true, subtree:true, attributeFilter:['src','loop','autoplay','role']}`.
    - T-D18: logged-out shape (no dialog): only the role=main root is observed (dialog root skipped, never body-wide).
    - T-D19: batch drains once per frame — record 2 childList mutations with videos, `raf.flush()` once → `_registrySize() === 2`; record 2 more, flush once → 4 (idempotent re-register keeps it 4 if same elements; use distinct elements to prove draining, then same elements to prove idempotence).
    - T-D20: attribute mutation — `recordAttributeMutation(video, 'src')` + flush → registered (src swap signal, blob URL per State of the Art line 419).
    - T-D21: self-mutation filter — set `mutating` true via a test handle (`_setMutatingForTest(true)`), record a video mutation, flush → `_registrySize()` unchanged; reset flag, record again, flush → grows. (This proves Pitfall 4 protection.)
    - T-D22: overlay-host exclusion — `_setOverlayHostForTest(overlayEl)`, record a video mutation inside overlayEl, flush → not registered; outside overlayEl → registered.
    - T-D23: disconnect on SOCIAL (DETC-08/D-12) — REELS → pushState `/direct/` (RouteGuard from Plan 02) → context SOCIAL → assert observer.disconnect() called (`_getWatcherState().connected === false`) and `_registrySize()` UNCHANGED (D-07 keep-registry).
    - T-D24: disconnect on UNKNOWN — pushState `/` → UNKNOWN → disconnected.
    - T-D25: reconnect re-sync — back to `/reels/` → connected again, one batch processed synchronously, healthScan ran.
    - Cross-cutting source scan: assert the mutation-callback region of src/slowgram.js contains no querySelector/querySelectorAll calls (region-scoped negative gate: read the file, strip comments, isolate the batchCallback function body, assert absent), and assert the observe config literal `['src','loop','autoplay','role']` appears exactly as the attributeFilter argument.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "observer.observe is called with the exact locked config on the feed root and (when present) the dialog root — never on body"
    - "records drain once per rAF flush; the batch callback performs zero DOM queries (region-scoped source scan)"
    - "self-mutations and overlay-host subtree mutations are filtered; external mutations register"
    - "disconnect happens on SOCIAL and UNKNOWN; the registry survives; reconnect re-syncs within one frame"
  </acceptance_criteria>
  <done>The observer observes exactly the two roots with the locked config (never body), records drain once per frame with zero DOM queries in the callback, self-mutations and overlay-host subtrees are filtered, and the connect/disconnect/reconnect lifecycle works per D-12/D-07.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VideoRegistry full contract — WeakMap state, loadstart/emptied lifecycle reset, idempotent register</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 02-RESEARCH.md — Pattern 3 (WeakMap VideoRegistry, 235-260), Pitfall 5 (stale state on node recycling, 341-345), State of the Art blob src swap (419), DETC-05 requirement text (REQUIREMENTS.md:25)
    - 02-PATTERNS.md — WeakMap rule (348), FakeVideoElement spec (180-182), fixture shapes (loggedIn has loop)
    - 02-CONTEXT.md — D-05 lifecycle-reset context (DETC-05), D-07 keep-registry
  </read_first>
  <action>
    Complete VideoRegistry to its locked contract: per-video WeakMap state, loadstart/emptied lifecycle reset, idempotent registration, registry retained across disconnect.

    src/slowgram.js:
    - Keep the module-var `WeakMap` `videoStates` (Plan 01). State shape per-video: `{ registeredAt: <ms>, src: <string|null>, started: false, ended: false, appliedLevers: null }` — `appliedLevers` reserved for Phase 3 (null until then; Phase 2 holds registration + lifecycle only, per CONTEXT.md:39 discretion note).
    - `register(video)` — idempotent: if absent, `videoStates.set(video, { registeredAt: env.clock.now(), src: readSrc(video), started: false, ended: false, appliedLevers: null })`; then ALWAYS bind lifecycle listeners ONCE (guard with a `video.__slowgramLifecycleBound` marker or a Set of bound elements — WeakMap values can carry the flag; pick the WeakMap-value flag: `entry._bound`): `video.addEventListener('loadstart', onLoadStart)` and `video.addEventListener('emptied', onEmptied)` where:
      - `onLoadStart(ev)` — `var v = ev.target; var entry = videoStates.get(v); if (entry) { entry.started = true; entry.ended = false; entry.src = v.src || null; }` (reset on loadstart — new content loading, Pitfall 5).
      - `onEmptied(ev)` — `entry.ended = true; entry.started = false; entry.src = null;` (feed recycling the node — state reset).
    - `readSrc(video)` — `video.src || video.getAttribute && video.getAttribute('src') || null` (FakeVideoElement exposes `src`; real elements expose `.src` — blob URL per State of the Art).
    - `getRegistryState(video)` — returns a copy of the entry or null (Phase 3 consumer interface; test handle `_registrySize()` stays).
    - NOT cleared on disconnect (D-07) — no code path clears videoStates; assert via tests.
    - WeakMap GC-safety note (document in a comment): entries die with the element — the virtualized feed dropping nodes cannot leak.

    test/harness.js:
    - FakeVideoElement: extend to expose a settable `src` property that mirrors into attributes (getAttribute('src') reads it) so `readSrc` works identically on fake and real.
    - No other fakes needed — lifecycle events dispatch through the existing FakeElement listener map.

    test/slowgram.test.js — DETC-05 suite (T-D26+):
    - T-D26: register creates state — register a FakeVideoElement → `getRegistryState(video).registeredAt` is a number; `_registrySize() === 1`.
    - T-D27: idempotent — register the same element 10x → `_registrySize() === 1`; lifecycle listeners bound ONCE (dispatch loadstart once → assert the internal `started` flips but listener count for the event on the element === 1).
    - T-D28: loadstart reset — set `entry.src = 'blob:A'`; dispatch `loadstart` with `video.src = 'blob:B'` → `entry.src === 'blob:B'`, `entry.started === true`, `entry.ended === false`.
    - T-D29: emptied reset — after loadstart, dispatch `emptied` → `entry.ended === true`, `entry.started === false`, `entry.src === null` (node recycled; Pitfall 5).
    - T-D30: registry survives disconnect — REELS, register video, pushState `/direct/` (SOCIAL, observer disconnected) → `_registrySize() === 1` still (D-07); pushState back `/reels/` → still 1 (reconnect does not reset registry).
    - T-D31: feed node recycling via FakeElement.append/remove — build feed, register v1, remove v1 from the tree (simulating recycle), append a new FakeVideoElement → batch registers the new one; v1's entry still exists in the WeakMap until GC (assert `_registrySize() === 2` — WeakMap holds both; the reset contract is about the SAME node's state being cleared on emptied, not about dropping entries).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "register is idempotent; lifecycle listeners bind exactly once per element"
    - "loadstart and emptied reset the per-video state correctly (src, started, ended)"
    - "the registry is never cleared by disconnect or reconnect"
    - "all prior suites remain green"
  </acceptance_criteria>
  <done>register is idempotent with listeners bound once; loadstart and emptied reset per-video state; the registry survives disconnect and reconnect; T-D26..T-D31 pass with all prior suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation records → engine | records come from the page's DOM (untrusted); targets are used only for registration identity, never executed |
| page DOM mutations → engine | the engine's own writes must not be re-processed as page activity (feedback loop) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Denial of Service | observer over-breadth / jank | high | mitigate | two-root observation only (feed + dialog), attributeFilter limited to 4 attrs, takeRecords() drained once per rAF batch, batch callback does zero DOM queries (Pitfall 2, D-11/D-09) |
| T-02-09 | Tampering | feedback loop from engine's own writes | high | mitigate | mutating flag around engine DOM writes + overlay-host subtree exclusion (D-14, Pitfall 4) |
| T-02-10 | Denial of Service | observer running on social routes | medium | mitigate | connect-on-REELS-only, disconnect on SOCIAL/UNKNOWN (D-12/DETC-08), reconnect re-sync (D-07) |
| T-02-11 | Tampering | stale per-video state on node recycling | medium | mitigate | loadstart/emptied lifecycle reset (DETC-05, Pitfall 5); WeakMap GC-safety (no leak) |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0
- Observer config literal asserted (T-D17); region-scoped source scan proves zero DOM queries in batchCallback
- Self-mutation + overlay exclusion proven (T-D21/T-D22); disconnect/reconnect lifecycle proven (T-D23..T-D25)
- Lifecycle reset proven (T-D28/T-D29); registry survives disconnect (T-D30)
</verification>

<success_criteria>
- The observer is narrow (two roots, 4 attributes), feedback-loop-free, and disconnected on every non-REELS context
- Per-video state resets on loadstart/emptied; registry is WeakMap-backed and survives route flips
- Phase 3 can consume `getRegistryState(video)` + register/lifecycle semantics unchanged
</success_criteria>

<output>
Create `.planning/phases/02-dom-detection-scoping/02-03-SUMMARY.md` when done
</output>