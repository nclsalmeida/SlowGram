---
phase: 03-degradation-levers
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - LEVR-01
  - LEVR-02
  - LEVR-03
  - LEVR-04
  - LEVR-05
  - LEVR-06
  - LEVR-07
  - LEVR-08
  - LEVR-09
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Full-stack integration: ONE video at phase 3 carries ALL matrix levers at once — saturate() on the wrapper, playbackRate 0.8, volume 0.5, loop removed — and revertAll() on SOCIAL restores every one (wrapper filter, rate, volume, loop presence) in a single round-trip"
    - "The stop point composes with the buffer: ended at phase 3 pauses; with _setBufferEnabled(true) the pause holds exactly stallFrames then resumes; a SOCIAL flip mid-stall cancels it — a cancelled stall never resumes the video later"
    - "Lifecycle reentrancy: destroy()/teardown reverts all levers to native; a destroy → re-init cycle starts a fresh registry that re-applies the current phase, with the ended listener bound at most once per element per instance (T-D27 discipline extended)"
    - "Fatigue reset path: resetSession → sync(0) → matrix['0'] empty → the reconcile reverts every lever (LEVR-07 free path), not just explicit revertAll"
    - "The applicators map keys == the union of degradationMatrix phase lists ('saturation','playbackRate','volume','autoplay') and 'buffer' is NOT among them — the T15 lock + D-26 standalone-buffer contract, proven by source scan"
    - "node test/slowgram.test.js green with the closure suite; Edge headless harness.html parity maintained; demo.html verdict intact"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "The ended listener binds in the entry._bound block (03-03) — the reentrancy suite asserts (video.listeners.ended || []).length <= 1 after every init/destroy cycle"
    - "revertAll() (03-01) clears bufferStall (03-03) and runs every applicator revert — the SOCIAL round-trip and the destroy path share it; the closure suite asserts the full native restore"
    - "The reconcile in applyToVideo (03-01) reverts levers out of the matrix — the fatigue-reset test drives it via resetSession, not revertAll, proving the free path"
    - "The matrix/applicator key-set scan reads src/slowgram.js source (Node-only, SCAN block) — the same scan style as the existing loop=false / stallFrames / muted-assignment scans"
  prohibitions:
    - statement: "No new lever values or engine paths — the closure proves composition and lifecycle, it does not extend behavior; the existing 509-assertion suite stays green."
      status: unverified
      flagged: true
    - statement: "No timers anywhere — the buffer stall remains frame-counted on the rAF carrier; the timer-API source scan stays green."
      status: unverified
      flagged: true
---

<objective>
Wave 4: close Phase 3 — prove the five-lever matrix composes on a single video and survives every lifecycle round-trip (phase transitions, SOCIAL/UNKNOWN revert, fatigue reset, destroy/re-init, feed churn), then lock the closure with source scans and the final two-host smoke.

Purpose: the phase's success criteria are about the SYSTEM, not individual levers — a lever that works alone but fights its siblings (e.g. revert order, wrapper conflicts, listener duplication across re-init) is a phase failure. This plan makes composition and reentrancy first-class, then hands Phase 3 to verify-work with proof.
Output: `test/slowgram.test.js` (LEVR 03-04 closure suite: T-L43..T-L51) + any engine fix the suite surfaces; source-scan lock of the matrix/applicator key-set invariant.
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-degradation-levers/03-RESEARCH.md
@.planning/phases/03-degradation-levers/03-PATTERNS.md
@.planning/phases/03-degradation-levers/03-CONTEXT.md
@.planning/phases/03-degradation-levers/03-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Full-stack integration — five levers on one video + round-trip</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-CONTEXT.md — D-28/D-29 (closure contracts)
    - src/slowgram.js — applyToVideo reconcile, revertAll, autoplayApp/bufferApp (03-03), saturationApp (03-01)
  </read_first>
  <action>
    Create the tracer slice: one path — `video (wrapper + loop attr) at /reels/ → phase 3 → ALL five levers applied → ended → buffer stall resumes → SOCIAL → everything native → back to REELS → re-applied`.

    test/slowgram.test.js — LEVR 03-04 suite (appended after T-L41):
    - T-L43: full-stack apply — register video (wrapper + loop attr) at /reels/, advance to phase 3, flush → assert simultaneously: `wrapper.style.filter === 'saturate(0.4)'`, `video.playbackRate === 0.8`, `video.volume === 0.5`, `video.hasAttribute('loop') === false`, and `getRegistryState(video).appliedLevers` carries all four keys {saturation:3, playbackRate:3, volume:3, autoplay:3}.
    - T-L44: stop point + buffer compose — at phase 3, `_setBufferEnabled(true)`, dispatch ended → `paused === true`; flush → still paused; flush → `paused === false` (resumed). Flip the flag back off.
    - T-L45: SOCIAL round-trip restores everything — setContext('SOCIAL') → assert `wrapper.style.filter === ''`, `video.playbackRate === 1`, `video.volume === 1`, `video.hasAttribute('loop') === true` (origHadLoop restore), and no buffer stall pending. Then setContext('REELS') + advance already at 720000 → one flush → assert all five re-applied (register-time apply path on the surviving registry).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "One video carries all four matrix levers simultaneously at phase 3 (T-L43)"
    - "The stop point and the buffer compose in sequence: pause → stallFrames → resume (T-L44)"
    - "SOCIAL reverts every lever to native in one revertAll round-trip; REELS re-applies (T-L45)"
    - "All prior suites (509 assertions) stay green"
  </acceptance_criteria>
  <done>Full-stack composition proven: five levers apply/revert/re-apply as one system on a single video.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lifecycle reentrancy, fatigue reset, churn + matrix-lock scan + final two-host smoke</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-CONTEXT.md — D-28/D-29
    - src/slowgram.js — teardown, init (re-init path), resetSession, processBatch removedNodes pruning, the SCAN block tail in test/slowgram.test.js
  </read_first>
  <action>
    Close the lifecycle edge cases and lock the closure.

    test/slowgram.test.js — T-L46..T-L51:
    - T-L46: fatigue reset via the free path — video at phase 3 with all levers applied; `SlowGram.emit('reset')`-style resetSession → sync(0) → assert every lever reverted WITHOUT setContext (the reconcile's empty matrix['0'], not explicit revertAll).
    - T-L47: destroy/re-init reentrancy — at phase 3 (all levers applied), SlowGram.destroy() → assert wrapper filter ''/rate 1/volume 1/loop restored (teardown revertAll hygiene) AND `(video.listeners.ended || []).length === 1` (no listener growth); re-init with the same env → register-time state fresh (getRegistryState null or appliedLevers null) and re-applying the current phase re-degrades the video.
    - T-L48: feed churn — video registered + levers applied at phase 3; batch removedNodes → dropFromRegistry; re-add to the DOM via a new observer record → re-registers fresh (idempotent WeakMap entry, appliedLevers reset by the remove/emptied semantics) and re-applies; the registryElements list stays bounded (no duplicate entries — indexOf-guard).
    - T-L49: cancelled stall never resumes — at phase 3 with `_setBufferEnabled(true)`, ended starts a stall; BEFORE the frames elapse, setContext('SOCIAL') → revertAll clears bufferStall; return to REELS + two flushes → assert `video.paused` stays false-change-free... assert the video was never played (spy play() count === 0 after the flushes).
    - T-L50: no-loop shape full-stack — loggedOut video (no loop attr) at phase 3 → all four matrix levers apply without crash, no attribute written (`hasAttribute('loop')` false, getAttribute null), appliedLevers {saturation, playbackRate, volume, autoplay} all 3; SOCIAL revert clean.
    - T-L51: closure source scans (Node-only, extend the SCAN block): (a) matrix/applicator lock — `code.match(/applicators\.[a-zA-Z]+ =/g)` yields exactly the 4 matrix keys and NOT 'buffer' (assert the applicators assignments are a subset of the degradationMatrix phase-3 keys and 'buffer' never appears as an applicator); (b) existing timer/loop=false/stallFrames/muted scans stay green.
    - demo.html: re-run the Edge headless demo verdict dump — assert the verdict panel still renders (Context: REELS / Registered videos / Selector health) with the five-lever engine.

    Two-host smoke (run both, record both in the summary):
    - Node: `node test/slowgram.test.js` — exit 0, report the new total.
    - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke11" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` → `TOTAL: N passed / N run` parity (Node minus Node-only source-scan asserts).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Fatigue reset reverts all levers through the reconcile free path (T-L46)"
    - "destroy/re-init restores native, never duplicates the ended listener, and re-applies fresh (T-L47)"
    - "Feed churn keeps the registry bounded and re-applies cleanly (T-L48); a cancelled stall never resumes (T-L49)"
    - "The matrix/applicator key-set lock scan passes; the demo verdict renders with the five-lever engine"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html parity holds"
  </acceptance_criteria>
  <done>Phase 3 closed: composition + reentrancy + churn proven, matrix lock scanned, both hosts green, demo intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lever writes ↔ video native state | the full-stack round-trip must restore EXACTLY the captured originals (origFilter/origPlaybackRate/origVolume/origHadLoop) — a partial restore is a silent product leak |
| registry ↔ lifecycle | destroy/re-init and feed churn must not duplicate listeners, leak elements, or carry stale appliedLevers across instances |
| buffer stall ↔ context | a stall started at the stop point must never outlive the REELS surface — SOCIAL/reset cancels it or it resumes off-surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Tampering | partial revert — one lever restored, others left degraded after SOCIAL/reset | high | mitigate | T-L45/T-L46 assert the FULL native set in one round-trip |
| T-03-21 | Tampering | listener duplication across init/destroy cycles (ended/loadstart/emptied bound twice) | high | mitigate | `_bound` flag + T-L47 assert (video.listeners.ended length 1) |
| T-03-22 | Denial of Service | a cancelled buffer stall resumes a video after SOCIAL (off-surface playback) | medium | mitigate | revertAll clears bufferStall; T-L49 spies play() and asserts zero calls |
| T-03-23 | Information Disclosure | an applicator map key drifting from the matrix (lever applied outside its phase or buffer entering the map) | medium | mitigate | T-L51a source scan locks the 4-key set and bans 'buffer' in the map |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 (prior 509 + new LEVR 03-04 closure suite)
- Edge headless smoke: `TOTAL: N passed / N run` parity; demo.html verdict renders
- Composition proven by T-L43/T-L44/T-L45; lifecycle by T-L46/T-L47/T-L48/T-L49; lock by T-L51a
</verification>

<success_criteria>
- Phase 3's five success criteria are all provable against the shipped engine: composition (SC1-4), clamp spec (SC5), stop point + gated buffer (SC3), hub reconcile + revertAll (SC4)
- The closure suite adds ~30 assertions; Node + Edge both green; demo intact
- Phase 3 is ready for /gsd-verify-work — no open lever, lifecycle, or composition question remains
</success_criteria>

<output>
Create `.planning/phases/03-degradation-levers/03-04-SUMMARY.md` when done
</output>
