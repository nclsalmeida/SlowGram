---
phase: 03-degradation-levers
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - LEVR-04
  - LEVR-05
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Autoplay lever (LEVR-04): at phase 3 REMOVES the loop attribute (never loop=\"false\"), captures origHadLoop, restores it on revert; an ended listener bound once per element pauses the video while the lever is applied = the stop point"
    - "Buffer lever (LEVR-05): gated behind CONFIG.buffer.enabled (default FALSE), applicable only at the autoplay stop point, stalls sub-200ms (CONFIG.buffer.stallFrames = 2 rAF frames ≈ 33ms), frame-counted on the rAF carrier — NO timers"
    - "The buffer is NOT matrix-driven and NOT in the applicators map (T15 lock + the reconcile revert loop would cancel it) — a standalone applicator-shaped helper driven from onEnded (D-26)"
    - "The buffer flag is a module var seeded from CONFIG at init with a _setBufferEnabled test handle (precedent _setDevMode) — frozen CONFIG stays the production default"
    - "node test/slowgram.test.js green with the new suite; Edge headless harness.html parity maintained"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "The ended listener binds in the same entry._bound block as loadstart/emptied (once per element — T-D27 counts stay green)"
    - "The loop attribute is in the D-11 attributeFilter — the removal write rides the mutating flag (D-14 discipline)"
    - "The buffer stall count decrements in processBatch (per rAF frame on REELS) after healthScan, gated on the flag — the poll loop is the only carrier (no timers)"
    - "revertAll clears pending buffer stalls so a cancelled stall never resumes a video later (social/reset cleanliness)"
  prohibitions:
    - statement: "No timer-based stall — the buffer hold is frame-counted on the rAF carrier (Phase 1 timer ban); the timer-API source scan stays green."
      status: unverified
      flagged: true
    - statement: "The loop attribute is removed via removeAttribute('loop') — never set to loop=\"false\" (a present attribute keeps looping); a source scan proves no loop=false write."
      status: unverified
      flagged: true
    - statement: "The buffer never applies outside the phase-3 autoplay stop point (gated on appliedLevers.autoplay), and never without CONFIG.buffer.enabled."
      status: unverified
      flagged: true
---

<objective>
Wave 3: the Autoplay lever (LEVR-04 — loop-removal + pause-on-ended = the stop point) and the flagged Buffer capstone (LEVR-05 — sub-200ms stall at the stop point, default off), completing the five-lever matrix.

Purpose: prove the stop point works end-to-end — a looping reel at phase 3 loses its loop attribute and pauses at the end instead of restarting — and the gated buffer hiccup stays off by default and behaves when flipped. A dead end in the loop-removal/ended contract or the frame-counted stall costs one commit, not two.
Output: `src/slowgram.js` (autoplayApp + onEnded + processStalls + CONFIG.buffer + _setBufferEnabled), `test/slowgram.test.js` (LEVR 03-03 suite).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-degradation-levers/03-RESEARCH.md
@.planning/phases/03-degradation-levers/03-PATTERNS.md
@.planning/phases/03-degradation-levers/03-CONTEXT.md
@.planning/phases/03-degradation-levers/03-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Autoplay lever — loop removal + pause-on-ended stop point</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-CONTEXT.md — D-25 (autoplay contract), the T15 matrix lock note
    - src/slowgram.js — registerVideo (entry._bound block), onLoadStart/onEmptied, applyToVideo reconcile
  </read_first>
  <action>
    Create the tracer slice: one path — `video with loop attr at phase 3 → removeAttribute('loop') → dispatch ended → video.pause() (stop point) → revert on SOCIAL → loop attr restored`.

    src/slowgram.js:
    - `onEnded(ev)` — entry = videoStates.get(ev.target); if entry.appliedLevers && entry.appliedLevers.autoplay && typeof ev.target.pause === 'function' → `ev.target.pause()` (LEVR-04 stop point).
    - registerVideo: bind `video.addEventListener('ended', onEnded)` inside the existing `entry._bound` once-block (next to loadstart/emptied).
    - `autoplayApp = { key: 'autoplay', apply(phase, video), revert(video) }`:
      - apply: entry guard; idempotence (appliedLevers.autoplay === phase); if `typeof video.hasAttribute === 'function' && video.hasAttribute('loop')` → capture `entry.origHadLoop = true` (first touch only) and `mutating` write `video.removeAttribute('loop')` (NEVER loop="false"); else `entry.origHadLoop = false`. Set appliedLevers.autoplay = phase. Contained try/catch.
      - revert: entry guard; no appliedLevers.autoplay → no-op; if entry.origHadLoop && typeof video.setAttribute === 'function' → `mutating` write `video.setAttribute('loop', '')` (restore presence); delete appliedLevers.autoplay.
    - `applicators.autoplay = autoplayApp;` — the reconcile routes it at phase 3 (matrix key 'autoplay' exists) and reverts it below phase 3.

    test/slowgram.test.js — LEVR 03-03 suite (appended after T-L31):
    - T-L33: CONFIG.buffer exists, frozen, enabled === false (default off — LEVR-05) and stallFrames === 2 (locked).
    - T-L34: autoplay end-to-end — video WITH loop attr, advance to phase 3 + flush → hasAttribute('loop') === false, getAttribute('loop') === null (removed, not loop="false"), appliedLevers.autoplay === 3.
    - T-L35: stop point — at phase 3, dispatch ended → video.paused === true (pause on ended — LEVR-04).
    - T-L36: revert restores — /direct/ → hasAttribute('loop') === true again (origHadLoop restored).
    - T-L37: no-loop video (loggedOut shape) — apply at phase 3 → no crash, appliedLevers.autoplay === 3, no attribute touched.
    - T-L38: below phase 3 no autoplay — at phase 2, hasAttribute('loop') still true (matrix gate).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The loop attribute is REMOVED at phase 3 (never loop=\"false\") and restored on revert"
    - "An ended video pauses at the stop point while the lever is applied"
    - "All prior suites (485 assertions) stay green"
  </acceptance_criteria>
  <done>Autoplay lever works end-to-end: loop removed at phase 3, ended pauses (stop point), revert restores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Buffer capstone — gated flag, frame-counted stall, revertAll cleanliness + two-host smoke</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-CONTEXT.md — D-26/D-27 (buffer contract), FEATURES.md:47 note in 03-RESEARCH.md
    - src/slowgram.js — the Task 1 additions, processBatch (healthScan tail), revertAll, teardown
  </read_first>
  <action>
    Add the flagged buffer capstone and close with the smoke.

    src/slowgram.js:
    - initConfig: add `buffer: { enabled: false, stallFrames: 2 }` (D-27, frozen).
    - Module var `bufferEnabled = false` — seeded at init from `CONFIG.buffer.enabled`; test handle `SlowGram._setBufferEnabled(flag)` sets it (precedent: _setDevMode).
    - `bufferApp = { key: 'buffer', apply(phase, video), revert(video) }` — the standalone applicator-shaped helper (D-26, NOT in the applicators map):
      - apply: start a stall — `entry.bufferStall = CONFIG.buffer.stallFrames` (only meaningful at the stop point; the gate lives in onEnded).
      - revert: cancel any pending stall (`entry.bufferStall = 0`).
    - `onEnded` — extend: if `bufferEnabled && entry.appliedLevers && entry.appliedLevers.autoplay` → `bufferApp.apply(state.phase, ev.target)` (start the sub-200ms stall at the stop point). The autoplay pause already ran above it.
    - `processStalls()` — for each registryElement: entry with `entry.bufferStall > 0` → decrement; at 0 → `if (typeof v.play === 'function') v.play()` (resume — the capstone hiccup resolves). Called from processBatch after healthScan, gated on `bufferEnabled`.
    - `revertAll()` — clear pending stalls (`entry.bufferStall = 0`) so a cancelled stall never resumes later (social/reset cleanliness); loop the registryElements.
    - init: `bufferEnabled = CONFIG.buffer.enabled;` (fresh per init — a _setBufferEnabled(true) resets on re-init).

    test/slowgram.test.js — T-L39..T-L42:
    - T-L39: buffer OFF by default — at phase 3 (loop video), dispatch ended → paused true; two flushes → still paused (no stall resume — flag off).
    - T-L40: buffer ON flow — `_setBufferEnabled(true)`; at phase 3, ended → paused true; flush #1 → still paused (stallFrames-1); flush #2 → paused false (resume — sub-200ms, frame-counted).
    - T-L41: buffer never outside the stop point — `_setBufferEnabled(true)` at phase 2 (no autoplay applied), dispatch ended → no stall (a flush does not play anything; paused stays as the platform left it — assert bufferStall not started via a flush no-op... assert `video.paused` unchanged and a flush does not call play).
    - T-L42: source scans (Node-only, extend the SCAN block): (a) no `loop="false"` / `loop = false` write (`code.match(/loop\s*=\s*(?:false|"false")/g)` → 0); (b) stallFrames literal appears only in CONFIG (`code.match(/stallFrames:/g)` → 1); (c) existing timer/class/muted scans stay green.

    Two-host smoke (run both, record both in the summary):
    - Node: `node test/slowgram.test.js` — exit 0, report the new total.
    - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke9" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` → `TOTAL: N passed / N run` parity.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The buffer stays OFF by default (production CONFIG) and, when flipped, stalls sub-200ms at the stop point via frame counting — no timers"
    - "revertAll clears pending stalls; a cancelled stall never resumes a video later"
    - "Source scans prove no loop=false write and no out-of-CONFIG stall literal"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html parity holds"
  </acceptance_criteria>
  <done>Buffer capstone complete: flag-gated, frame-counted, stop-point-only; the full five-lever matrix reconciles; both hosts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| video attribute state → autoplay behavior | the loop attribute is untrusted evidence; removal must be exact (removeAttribute, never loop=false) and reversible (origHadLoop) |
| ended event → stop point | the ended listener must only act when the lever is applied (phase 3) — never pause outside the stop point |
| buffer flag → stall behavior | the stall must be default-off, frame-counted (no timers), and cancelled cleanly on revertAll |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | loop=\"false\" instead of removal (attribute presence keeps looping) | high | mitigate | removeAttribute('loop') only; source scan forbids loop=false (T-L42a) |
| T-03-11 | Tampering | ended-pause firing outside the stop point (breaks autoplay below phase 3) | medium | mitigate | the ended listener gates on appliedLevers.autoplay (T-L38/T-L41) |
| T-03-12 | Tampering | timer-based buffer stall (violates the Phase 1 ban) | high | mitigate | frame-counted on the rAF carrier; timer scan stays green (T-L42) |
| T-03-13 | Denial of Service | a cancelled stall resumes a video after revertAll (social leak) | low | mitigate | revertAll clears bufferStall (T-03-13 in tests) |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 (prior 485 + new LEVR 03-03 suite)
- Edge headless smoke: `TOTAL: N passed / N run` parity
- Stop point proven by T-L34/T-L35/T-L36; buffer gate by T-L39/T-L40/T-L41; source scans by T-L42
</verification>

<success_criteria>
- The five-lever matrix is complete: saturation + playbackRate + volume + autoplay (stop point) + gated buffer, all reconciled by the hub
- The stop point works: a phase-3 looping reel loses its loop and pauses at the end; the buffer hiccup stays off by default and is frame-counted when flipped
- LEVR-04/05 covered; Phase 3 needs only the 03-04 closure plan (integration edge cases + final smoke)
</success_criteria>

<output>
Create `.planning/phases/03-degradation-levers/03-03-SUMMARY.md` when done
</output>
