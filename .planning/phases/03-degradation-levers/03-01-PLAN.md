---
phase: 03-degradation-levers
plan: 01
type: execute
wave: 1
depends_on:
  - 02-04
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - LEVR-01
  - LEVR-06
  - LEVR-07
  - LEVR-09
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The D-15 gate is resolved toward PITFALLS: saturation targets a static, non-transformed ancestor wrapper — never the <video> itself, never a transformed/filtered element, never BODY/HTML; the walk is bounded (null → skip) and values are static (03-CONTEXT.md D-15)"
    - "CONFIG.degradationMatrix keys are unchanged (T15 lock) and the saturation applicator's key matches the matrix ('saturation'); per-phase lever values live ONLY in CONFIG.leverParams — no magic literals in module bodies (CORE-05)"
    - "applyAll reconciles per video (apply levers in matrix[phase], revert levers applied but out of matrix) so phase 0 / fatigue reset reverts everything automatically; revertAll() is the explicit LEVR-07 handle and is context-agnostic"
    - "The hub guards context === 'REELS' for apply (never degrades outside the Reels surface); revertAll restores native on SOCIAL/UNKNOWN and on reset"
    - "registryElements (pushed on register, pruned on batch removedNodes VIDEO) is the only iteration mechanism — WeakMap/registryCount semantics unchanged (T-D26/T-D31 stay green)"
    - "node test/slowgram.test.js green with the new LEVR suite; Edge headless harness.html parity maintained (Node count − NodeOnly scans == Edge count)"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "syncPhase hooks applyAll AFTER emit('phasechange') — the hub emits nothing itself (T22/T23 counts stay green)"
    - "onLoadStart clears entry.appliedLevers and re-applies current phase (apply-after-load, ARCHITECTURE Pattern 2) — playbackRate-style resets stay degraded"
    - "registerVideo applies the current phase immediately — a video appearing mid-phase-2 is degraded on registration, not on the next transition"
    - "setContext REELS → connectWatcher + applyAll; SOCIAL/UNKNOWN → disconnectWatcher + revertAll (re-apply on return to /reels/)"
    - "style.filter is NOT in the D-11 attributeFilter, but the write still rides the D-14 mutating flag (discipline, Anti-Pattern 4)"
  prohibitions:
    - statement: "No timer-based lever application — the hub rides phasechange (transition-guarded) + register/loadstart/batch events only (Phase 1 timer ban)."
      status: unverified
      flagged: true
    - statement: "No lever value (saturation numbers) appears as a literal in src/slowgram.js — all values come from CONFIG.leverParams (CORE-05)."
      status: unverified
      flagged: true
    - statement: "The engine never applies a lever while context is not REELS, and never writes to the <video> element itself or BODY/HTML for the filter lever."
      status: unverified
      flagged: true
---

<objective>
Wave 1 tracer: the thinnest end-to-end slice of the Phase 3 degradation spine — DegradationEngine hub (phase → CONFIG.degradationMatrix → applicator map, with the per-video reconcile), the saturation applicator implementing the D-15 ancestor-wrapper gate, revertAll() (LEVR-07), and the trust-contract wiring (apply on REELS only, revert on SOCIAL/UNKNOWN/reset, apply-after-load on loadstart, live-element iteration via a pruned array).

Purpose: prove the architecture (phasechange → applyAll → filterTarget walk → wrapper.style.filter write → revert on reset/social) works end-to-end before the rate/volume/autoplay/buffer levers expand it. A dead end in the wrapper walk, the reconcile loop, or the WeakMap-iteration companion costs one commit, not ten committed levers.
Output: `src/slowgram.js` (DegradationEngine hub + saturation applicator + CONFIG.leverParams + registryElements pruning + revertAll/live-registry handles), `test/slowgram.test.js` (LEVR tracer suite).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-degradation-levers/03-RESEARCH.md
@.planning/phases/03-degradation-levers/03-PATTERNS.md
@.planning/phases/03-degradation-levers/03-CONTEXT.md
@.planning/phases/02-dom-detection-scoping/02-03-SUMMARY.md
@.planning/phases/02-dom-detection-scoping/02-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: DegradationEngine hub + saturation applicator — phase→matrix→wrapper, end-to-end</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-RESEARCH.md — Patterns A/B/C, Pitfall 2 (ancestor wrapper rules), the D-15 gate resolution
    - 03-PATTERNS.md — CONFIG leverParams block, applicator map + hub assignment, saturation applicator spec
    - src/slowgram.js — initConfig degradationMatrix (lines 171-176), syncPhase (118-126), registerVideo/onLoadStart/onEmptied (Phase 2 block), setContext hook, batchCallback
  </read_first>
  <action>
    Create the tracer slice: one path — `video registered at phase 0 → advance(180000) → flush → phasechange 1 → applyAll → filterTarget(video) → wrapper.style.filter = 'saturate(0.85)'` — production-quality, committed for keeps.

    src/slowgram.js — extend the existing IIFE (same file, same idioms, all new code closure-private):
    - CONFIG addition in initConfig (deep-freeze covers it; T15 matrix unchanged): `leverParams: { saturation: { '1': 0.85, '2': 0.65, '3': 0.4 } }` (D-19/D-20). No other CONFIG change.
    - Module vars: `var applicators = {};` (lever-key → {key, apply(phase, video), revert(video)}), `var registryElements = [];` (D-18 live-element array).
    - `isTransformed(node)` — `var s = node.style || {}; return !!(s.transform || s.filter);` (inline-style detection; computed-style deferred to Phase 5 per D-15).
    - `filterTarget(video)` — D-15 walk: `var node = video.parentNode; while (node) { if (node.tagName === 'BODY' || node.tagName === 'HTML') { return null; } if (!isTransformed(node)) { return node; } node = node.parentNode; } return null;` — first non-transformed ancestor; bounded at BODY/HTML (Anti-Pattern 6); never the video itself; null → skip (fail-safe).
    - `saturationApp = { key: 'saturation', apply(phase, video), revert(video) }`:
      - apply: guard `state.context === 'REELS'`; entry = videoStates.get(video), skip if missing; if `entry.appliedLevers && entry.appliedLevers.saturation === phase` → no-op (idempotent); target = filterTarget(video), skip if null; `mutating = true;` capture `entry.origFilter = target.style.filter || ''` once (only if undefined); `target.style.filter = 'saturate(' + CONFIG.leverParams.saturation[String(phase)] + ')';` restore `mutating = false;` set `entry.appliedLevers = entry.appliedLevers || {}; entry.appliedLevers.saturation = phase;`. Whole body try/catch contained (a lever failure logs, never breaks the host).
      - revert: entry = videoStates.get(video), skip if missing; if no appliedLevers.saturation → no-op; target = filterTarget(video); if target, restore `target.style.filter = entry.origFilter || ''` inside `mutating`; `delete entry.appliedLevers.saturation`.
    - `applyToVideo(video)` — guard REELS; `var keys = CONFIG.degradationMatrix[String(state.phase)] || [];` for each key → `applicators[key] && applicators[key].apply(state.phase, video)`; then for each applicator key NOT in matrix → `applicators[k].revert(video)` (the per-video reconcile, D-16 — de-escalation/reset revert automatically).
    - `applyAll()` — guard REELS; iterate `registryElements`, skip elements whose WeakMap entry is gone (`!videoStates.has(v)`), call applyToVideo. Called after emit('phasechange') in syncPhase (the hub emits nothing itself — T22/T23 green) and from setContext on REELS.
    - `registerVideo` — after the existing lifecycle binding, if `state.context === 'REELS'` → push the element into registryElements (once — guard by checking `videoStates.has(video)` before registering, which is the existing idempotence gate) and `applyToVideo(video)` (a video appearing mid-phase is degraded at registration).
    - `onLoadStart` — after the existing reset (started/src), clear `entry.appliedLevers = null;` and, if `state.context === 'REELS'`, `applyToVideo(v)` (Pattern 2 apply-after-load). `onEmptied` — clear `entry.appliedLevers = null;` only (no re-apply — no media).
    - `setContext` — REELS branch: after connectWatcher() call `applyAll()` (re-apply to surviving registry on return to /reels/); SOCIAL/UNKNOWN branch: after disconnectWatcher() call `revertAll()` (trust contract — never degrade social).
    - `resetSession` — call `revertAll()` before syncPhase(0) (LEVR-07 explicit; the phase-0 reconcile is the automatic backstop).
    - `revertAll()` — iterate registryElements, skip unregistered, call every applicator's revert (context-agnostic, LEVR-07).
    - Handles: `SlowGram.revertAll()` (public), `SlowGram._liveRegistrySize()` (test-only — registryElements.length, D-18 precedent: _registrySize).

    test/slowgram.test.js — LEVR tracer suite (appended after T-D39):
    - T-L1: CONFIG.leverParams exists, frozen, and `saturation['1']/['2']/['3']` are the locked escalation values (read via getConfig — never literals elsewhere).
    - T-L2: end-to-end — freshEnv with observer + root (plain wrapper div > video), register via record+flush at phase 0 → wrapper.style.filter stays '' (no lever at phase 0); advance(180000) + flush → phase 1 → wrapper.style.filter === 'saturate(0.85)' and getRegistryState(video).appliedLevers.saturation === 1.
    - T-L3: escalation — advance to 420000 (phase 2) + flush → 'saturate(0.65)'; advance to 720000 (phase 3) + flush → 'saturate(0.40)'.
    - T-L4: idempotence — repeated flush at the same phase → filter unchanged AND appliedLevers.saturation stays at the phase (no churn).
    - T-L5: revert on reset (LEVR-07) — at phase 2, `doc.setVisibility('hidden')` + visibilitychange + advance(300001) + resume signal → resetSession → phase 0, wrapper.style.filter restored to '' (and appliedLevers.saturation gone).
    - T-L6: revert on SOCIAL — pushState /direct/ → wrapper.style.filter '' (never degrade social).
    - T-L7: re-apply on return — pushState /reels/ → wrapper.style.filter back to 'saturate(0.65)' (current phase preserved across the social detour).
    - T-L8: wrapper selection (D-15) — video inside a transformed div inside a plain div → the plain wrapper gets the filter, the transformed div's style untouched.
    - T-L9: bounded walk — video whose ancestor chain is transformed up to BODY → no filter applied anywhere (null → skip).
    - T-L10: never the video itself — after applying, `video.style.filter` is '' (the lever wrote the wrapper, not the element).
    - T-L11: original preserved + restored — wrapper with a pre-existing `style.filter` ('brightness(1.1)') → after apply, wrapper filter is 'saturate(...)' (orig captured); after revert, restored to 'brightness(1.1)'.
    - T-L12: removedNodes pruning (D-18) — register v1 + v2, batch a removedNodes record for v1 → `_liveRegistrySize()` drops to 1 and revertAll touches only v2 (v1's wrapper stays untouched); WeakMap/registryCount semantics unchanged (`_registrySize()` still 2 — T-D31 contract).
    - T-L13: loadstart re-apply (Pattern 2) — at phase 2, dispatch loadstart → appliedLevers cleared then re-applied → wrapper.style.filter still 'saturate(0.65)'.
    - T-L14: source scans (Node-only) — extend the existing scan block: assert `leverParams` values appear only in initConfig (no `saturate(0.` literal outside CONFIG), no timer APIs (existing scan stays green), no new class-based queries.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "One registered video degrades end-to-end: phase 0 no-op → phase 1 saturate(0.85) on the non-transformed ancestor wrapper → escalation 0.65/0.40 → revert on reset/social"
    - "The D-15 gate holds: never the video itself, never a transformed ancestor, never BODY/HTML (T-L8/T-L9/T-L10)"
    - "revertAll() restores native on fatigue reset and social routes; return to /reels/ re-applies the current phase"
    - "Idempotent applies (no per-frame churn); lever values read only from CONFIG.leverParams"
    - "All prior suites (359 assertions) stay green; new LEVR suite adds ~30+ assertions"
  </acceptance_criteria>
  <done>The DegradationEngine hub + saturation lever prove phase→matrix→wrapper→revert end-to-end with the D-15 ancestor-wrapper gate, idempotent applies, automatic reset revert, and social trust preservation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hub trust wiring completion — register-time apply, apply-after-load, removedNodes pruning, two-host smoke</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-PATTERNS.md — hook points list (syncPhase, setContext, registerVideo, onLoadStart, batchCallback removedNodes)
    - src/slowgram.js — the Phase 3 block landed in Task 1
  </read_first>
  <action>
    Complete the trust wiring and close the plan with the dual-host smoke. Most of this task's behaviors were written in Task 1; this task adds the missing wiring, edge-cover tests, and the verification run.

    src/slowgram.js (additions/fixes only, if Task 1 left gaps):
    - batchCallback removedNodes pruning (if not already in Task 1): for each record, iterate `rec.removedNodes`, and for VIDEO nodes not in the overlay host, drop them from registryElements (indexOf + splice). D-14 filter applies (mutating skip is batch-level, unchanged).
    - Guard `applyToVideo`/`applyAll` re-entry: if the reconcile loop runs while a lever writes style (which the observer does NOT report — style is not in attributeFilter), no recursion is possible; still, keep the mutating flag discipline around writes (D-14).
    - Ensure `_liveRegistrySize()` and `revertAll()` are exported handles with doc comments following the _registrySize/_setMutatingForTest precedent.

    test/slowgram.test.js (edge coverage for the wiring):
    - T-L15: register-time apply — video registered AFTER reaching phase 2 (advance + flush first, then record+flush the video) → degraded immediately on registration (appliedLevers.saturation === 2 at register).
    - T-L16: onEmptied clears appliedLevers — after apply at phase 2, dispatch emptied → appliedLevers null; the next loadstart re-applies.
    - T-L17: multi-video reconcile — two videos, one at phase 2; reset → BOTH wrappers restored (reconcile iterates the live list).
    - T-L18: never outside REELS — with context SOCIAL and a video somehow in registryElements, applyAll is a no-op (guard) — drive via pushing to /reels/ at phase 2, then /direct/ → revert; assert the filter never appears on a SOCIAL context (T-L6 covers the main path; T-L18 covers the guard directly via _liveRegistrySize + filter state).

    Two-host smoke (run both, record both results in the summary):
    - Node: `node test/slowgram.test.js` — exit 0, all suites green (report the new total).
    - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke3" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count (Node count − NodeOnly scans; the parity contract from Phase 1/2).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Register-time apply, apply-after-load, emptied-clears, and multi-video reconcile are covered by tests"
    - "removedNodes pruning keeps the live list bounded; WeakMap semantics unchanged"
    - "node test/slowgram.test.js exits 0 with the full suite green; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The hub trust wiring is complete and covered; both hosts green with the full LEVR tracer suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page DOM structure → wrapper selection | parentNode chain is untrusted evidence; the walk must stay bounded (never BODY/HTML, never the video) or the filter lands on the wrong surface |
| engine style writes → host page | the lever writes wrapper.style.filter; a failure must log, never break the host; the write must not retrigger the observer (D-14) |
| context verdict → lever application | apply must be REELS-only; revert must be context-agnostic (social/reset trust contract) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | wrapper walk selecting a transformed/big ancestor | high | mitigate | D-15 bounded walk: skip transformed, stop at BODY/HTML (null → skip), never the video; T-L8/T-L9/T-L10 lock it |
| T-03-02 | Tampering | lever applied outside REELS (scope leakage) | high | mitigate | applyToVideo/applyAll guard `context === 'REELS'`; SOCIAL/UNKNOWN → revertAll (T-L6/T-L18); UNKNOWN never degrades |
| T-03-03 | Denial of Service | registryElements leak on feed virtualization (long session) | medium | mitigate | batch removedNodes pruning (T-L12); WeakMap still GC-safe for state |
| T-03-04 | Spoofing | stale appliedLevers after media reset (recycled node) | medium | mitigate | loadstart/emptied clear appliedLevers; apply-after-load re-applies (T-L13/T-L16) |
| T-03-05 | Tampering | saturation value drift (magic literals) | low | mitigate | values only in CONFIG.leverParams; T-L14 source scan forbids literals |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior 359 + new LEVR suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- D-15 gate proven by T-L8/T-L9/T-L10; reconcile/reset proven by T-L5/T-L17; social trust proven by T-L6/T-L7/T-L18
- Lever values from CONFIG only, proven by T-L1/T-L14
</verification>

<success_criteria>
- The flagship saturation lever renders on a safe ancestor wrapper, escalates imperceptibly, and reverts cleanly on reset/social — with the D-15 gate resolved and locked by tests
- The DegradationEngine hub is proven end-to-end (route → reconcile → apply → revert) and ready for the 03-02 levers (Playback/Volume + clamp tables)
- Phase 3 opens with a validated spine; LEVR-01/06/07/09 covered by the tracer, LEVR-02/03/08 (clamps) and LEVR-04/05 (autoplay/buffer) queued
</success_criteria>

<output>
Create `.planning/phases/03-degradation-levers/03-01-SUMMARY.md` when done
</output>
