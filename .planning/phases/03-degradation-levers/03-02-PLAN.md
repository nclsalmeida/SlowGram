---
phase: 03-degradation-levers
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/slowgram.js
  - test/harness.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - LEVR-02
  - LEVR-03
  - LEVR-08
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Clamp tables live in frozen CONFIG.clampTables (LEVR-08) — webkit { playbackRate {0.5,2}, volume {0,1} }, chromium { playbackRate {0.5,4}, volume {0,1} } (D-22); the engine clamps every lever value through the platform table before writing (clampForPlatform)"
    - "The platform comes from the DI seam (env.platform 'webkit'|'chromium', validated in resolveEnv, UA-sniffed default — D-21); SlowGram._clampForPlatform test handle (precedent _phaseFor)"
    - "Playback lever (LEVR-02): CONFIG.leverParams.playbackRate {2:0.9, 3:0.8}, inside 0.5–2.0, pitch preserved (preservesPitch), re-applied per video via the register/loadstart hooks (D-23)"
    - "Volume lever (LEVR-03): CONFIG.leverParams.volume {3:0.5} relative factor; feature-detects typeof volume === 'number'; gates on muted !== true && volume > 0; NEVER assigns video.muted (Anti-Pattern 2); revert restores origVolume (D-24)"
    - "No lever values or clamp limits as literals in module bodies — all from CONFIG (CORE-05); a source scan proves no `.muted =` assignment anywhere"
    - "node test/slowgram.test.js green with the new LEVR 03-02 suite; Edge headless harness.html parity maintained"
  artifacts:
    - src/slowgram.js
    - test/harness.js
    - test/slowgram.test.js
  key_links:
    - "clampForPlatform reads CONFIG.clampTables[env.platform][leverKey] — single source of truth for limits"
    - "The hub's per-video reconcile already routes 'playbackRate'/'volume' (matrix keys exist since Phase 1) — the 03-02 levers register into applicators with the same {key, apply, revert} shape"
    - "FakeVideoElement gains the STACK.md:40 media stubs (playbackRate/volume/muted/paused/currentTime/duration/play/pause/preservesPitch) — additive, existing src-mirror behavior unchanged"
    - "loadstart clears appliedLevers + re-applies (03-01 wiring) → playbackRate resets to 1.0 on source change and is re-applied per video (LEVR-02)"
  prohibitions:
    - statement: "No code path assigns video.muted (source-scan proven) — the volume lever only READS muted as a gate (Anti-Pattern 2, WebKit pause-on-unmute)."
      status: unverified
      flagged: true
    - statement: "No lever value or clamp limit appears as a literal outside initConfig — the engine clamps through CONFIG.clampTables and reads values from CONFIG.leverParams."
      status: unverified
      flagged: true
    - statement: "No timer-based lever application — the 03-02 levers ride the same phasechange/register/loadstart hooks (Phase 1 timer ban)."
      status: unverified
      flagged: true
---

<objective>
Wave 2: the Playback and Volume levers (LEVR-02/03) with the per-platform clamp tables (LEVR-08) — the DI platform seam, the frozen CONFIG.clampTables spec, the harness media stubs, and the two levers wired into the 03-01 hub with full trust/reconcile coverage.

Purpose: prove the two media-API levers (playbackRate, volume) degrade correctly under the platform clamp spec — never touching muted, never writing out-of-band values — before the autoplay/buffer capstone expands the matrix. A dead end in the platform seam, the clamp spec, or the muted gate costs one commit, not three levers.
Output: `src/slowgram.js` (env.platform seam + clampForPlatform + playback/volume applicators), `test/harness.js` (FakeVideoElement media stubs), `test/slowgram.test.js` (LEVR 03-02 suite).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-degradation-levers/03-RESEARCH.md
@.planning/phases/03-degradation-levers/03-PATTERNS.md
@.planning/phases/03-degradation-levers/03-CONTEXT.md
@.planning/phases/03-degradation-levers/03-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Platform seam + clamp tables + FakeVideoElement media stubs + Playback lever</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 03-RESEARCH.md — Clamp Tables section (PITFALLS:71-88 facts), Pattern A (applicator interface)
    - 03-CONTEXT.md — D-21/D-22/D-23 (platform seam, clamp spec, playback values)
    - test/harness.js — FakeVideoElement factory (lines ~380-408)
  </read_first>
  <action>
    Create the tracer slice: one path — `init({platform:'webkit'}) → video at phase 2 → playbackRate 0.9 (clamped through webkit's table) → preservesPitch true → revert on SOCIAL → 1.0` — plus the clamp spec it depends on.

    src/slowgram.js:
    - resolveEnv: add the `platform` override (D-21): validate — provided non-undefined must be null or 'webkit'|'chromium' (descriptive Error otherwise); default `(typeof navigator !== 'undefined' && navigator.userAgent) ? (userAgent.indexOf('Chrome') !== -1 ? 'chromium' : userAgent.indexOf('Safari') !== -1 ? 'webkit' : 'chromium') : 'chromium'`. The resolved `platform` joins the env object.
    - initConfig: add `clampTables` (D-22) — `{ webkit: { playbackRate: { min: 0.5, max: 2 }, volume: { min: 0, max: 1 } }, chromium: { playbackRate: { min: 0.5, max: 4 }, volume: { min: 0, max: 1 } } }` — frozen with the rest; extend `leverParams` with `playbackRate: { '2': 0.9, '3': 0.8 }` (D-23).
    - `clampForPlatform(leverKey, value)` — `var table = CONFIG.clampTables && CONFIG.clampTables[env.platform]; var limits = table && table[leverKey]; if (!limits) return value; return Math.min(Math.max(value, limits.min), limits.max);` — every lever value is clamped through the platform spec before writing (LEVR-08).
    - `playbackApp = { key: 'playbackRate', apply(phase, video), revert(video) }`:
      - apply: entry guard; idempotence (`appliedLevers.playbackRate === phase`); `value = CONFIG.leverParams.playbackRate[String(phase)]` (undefined → return); `value = clampForPlatform('playbackRate', value)`; capture `entry.origPlaybackRate = video.playbackRate || 1` on first touch; `mutating` write `video.playbackRate = value` + `if ('preservesPitch' in video) video.preservesPitch = true;` (LEVR-02 pitch); set appliedLevers.playbackRate = phase. Contained try/catch.
      - revert: entry guard; if no appliedLevers.playbackRate → no-op; `mutating` write `video.playbackRate = entry.origPlaybackRate !== undefined ? entry.origPlaybackRate : 1`; delete appliedLevers.playbackRate.
    - `applicators.playbackRate = playbackApp;` + `SlowGram._clampForPlatform = clampForPlatform;` (test-only handle, D-22).

    test/harness.js — FakeVideoElement: add the STACK.md:40 media stubs (additive; keep the src mirror): `playbackRate = 1; defaultPlaybackRate = 1; volume = 1; muted = false; loop = false; autoplay = false; paused = true; currentTime = 0; duration = 10; ended = false; preservesPitch = true; play() { paused = false; } pause() { paused = true; }` — plain properties/functions on the returned element, after the src mirror block.

    test/slowgram.test.js — LEVR 03-02 suite (appended after T-L18; freshEnv gains `platform` passthrough: `init({..., platform: opts.platform})`):
    - T-L19: CONFIG.clampTables frozen with the locked spec — webkit/chromium × playbackRate/volume min/max read via getConfig (LEVR-08).
    - T-L20: CONFIG.leverParams.playbackRate {2:0.9, 3:0.8} and volume {3:0.5}.
    - T-L21: playback end-to-end — register at phase 0/1 → playbackRate stays 1; advance to 420000 (phase 2) + flush → video.playbackRate === 0.9 and appliedLevers.playbackRate === 2; advance to 720000 (phase 3) → 0.8; preservesPitch stays true throughout.
    - T-L22: pitch preservation — after apply, `video.preservesPitch === true` explicitly (LEVR-02).
    - T-L23: platform seam — `SlowGram.init({platform:'not-an-engine'})` throws the descriptive Error; `freshEnv({platform:'webkit'})` init succeeds.
    - T-L24: clamp behavior — with platform webkit, `_clampForPlatform('playbackRate', 2.5) === 2`; chromium, `_clampForPlatform('playbackRate', 2.5) === 2.5`; `_clampForPlatform('volume', 1.5) === 1` both; `_clampForPlatform('playbackRate', 0.1) === 0.5`.
    - T-L25: playback revert on SOCIAL — at phase 2 rate 0.9 → pushState /direct/ → video.playbackRate === 1 (origPlaybackRate restored).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Playback lever applies CONFIG values clamped through the platform table, preserves pitch, and reverts to the captured original"
    - "Clamp tables + leverParams are frozen CONFIG data; clampForPlatform is proven by the test handle"
    - "The platform seam validates loudly and defaults deterministically"
    - "All prior suites (425 assertions) stay green"
  </acceptance_criteria>
  <done>Playback lever works end-to-end under the clamp spec (webkit cap 2.0), pitch preserved, revert clean; clamp tables are the frozen spec.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Volume lever (LEVR-03 muted gate) + reconcile/trust coverage + source scans + two-host smoke</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 03-CONTEXT.md — D-24 (volume gate), Anti-Pattern 2 in 03-RESEARCH.md
    - src/slowgram.js — the Phase 3 block from 03-01 (applyToVideo reconcile, loadstart re-apply)
  </read_first>
  <action>
    Add the volume lever and close with the full-stack coverage + smoke.

    src/slowgram.js:
    - leverParams: add `volume: { '3': 0.5 }` (D-24; matrix has volume at phase 3 only — T15 lock).
    - `volumeApp = { key: 'volume', apply(phase, video), revert(video) }`:
      - apply: entry guard; idempotence; LEVR-03 gate: `if (video.muted === true || typeof video.volume !== 'number' || !(video.volume > 0)) { return; }` (feature-detect + never-touch-muted); `factor = CONFIG.leverParams.volume[String(phase)]` (undefined → return); capture `entry.origVolume = video.volume` on first touch; `value = clampForPlatform('volume', entry.origVolume * factor)`; `mutating` write `video.volume = value`; set appliedLevers.volume = phase. Contained.
      - revert: entry guard; no appliedLevers.volume → no-op; `mutating` write `video.volume = entry.origVolume` (if defined); delete appliedLevers.volume.
    - `applicators.volume = volumeApp;`

    test/slowgram.test.js — T-L26..T-L32:
    - T-L26: volume lever — freshEnv + video `muted=false, volume=0.8`; advance to phase 3 + flush → video.volume === 0.4 (0.8 × 0.5, clamped); revert via /direct/ → 0.8 restored.
    - T-L27: muted gate — `video.muted = true` → phase 3 apply leaves volume unchanged AND appliedLevers.volume stays unset; `video.muted` itself never changes (no assignment anywhere).
    - T-L28: zero/unsupported gate — volume 0 → unchanged; `video.volume = undefined` (unsupported feature-detect) → unchanged.
    - T-L29: full-stack reconcile — at phase 3 (unmuted): saturation + playbackRate + volume ALL applied (wrapper filter, rate 0.8, volume × 0.5); revertAll → all three restored.
    - T-L30: escalation profile — phase 1: only saturation (rate 1, volume untouched); phase 2: saturation + playbackRate (volume untouched); phase 3: + volume.
    - T-L31: loadstart re-apply for rate — at phase 2, set `video.playbackRate = 1` (simulating the browser reset on source change) then dispatch loadstart → rate re-applied 0.9 (Pattern 2 via the 03-01 hook).
    - T-L32: source scans (Node-only, extend the SCAN block): (a) no `.muted =` assignment anywhere in the engine (`code.match(/\.muted\s*=/g)` → 0); (b) no clamp/lever literals outside initConfig (playbackRate values 0.9/0.8 and clamp 2/4 appear only in CONFIG — scan `code.match(/playbackRate:\s*\{\s*'2':/)` presence + the saturate literal scan stays); (c) existing timer/class scans stay green.

    Two-host smoke (run both, record both in the summary):
    - Node: `node test/slowgram.test.js` — exit 0, report the new total.
    - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke7" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` → `TOTAL: N passed / N run` parity.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Volume lever gates on !muted && volume > 0 with feature-detect; never assigns muted; reverts the original volume"
    - "The full phase-3 stack (saturation + playbackRate + volume) applies and reverts together via the reconcile"
    - "Source scans prove no muted assignment and no out-of-CONFIG lever/clamp literals"
    - "node test/slowgram.test.js exits 0; Edge headless harness.html parity holds"
  </acceptance_criteria>
  <done>Volume lever complete under the LEVR-03 gate; the phase-3 full stack reconciles; both hosts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform verdict → lever limits | env.platform is untrusted input; the clamp tables must be the single source of limits and clamping must never throw |
| video media state → lever application | muted/volume/playbackRate are untrusted evidence; the volume lever must never write muted (WebKit pause) and never write out-of-band values (silent no-ops) |
| lever writes → host page | media-API writes must be contained and must not retrigger the observer (D-14 mutating flag) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | out-of-band playbackRate silently no-ops on WebKit | high | mitigate | clampForPlatform through CONFIG.clampTables (webkit max 2.0); T-L24 proves the clamp |
| T-03-07 | Tampering | programmatic muted write pauses iOS playback | high | mitigate | volume lever never assigns muted (gate + source scan); T-L27/T-L32a |
| T-03-08 | Tampering | volume applied to muted/zero-volume videos is inert or wrong | medium | mitigate | LEVR-03 gate (muted !== true && volume > 0) + feature-detect (T-L27/T-L28) |
| T-03-09 | Tampering | platform seam misdetection | low | mitigate | DI override validated loudly; UA sniff is a default only; tests pass explicit platforms |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 (prior 425 + new LEVR 03-02 suite)
- Edge headless smoke: `TOTAL: N passed / N run` parity
- Clamp spec proven by T-L19/T-L24; muted gate by T-L27/T-L32a; full stack by T-L29/T-L30
- Values/limits from CONFIG only, proven by T-L32b
</verification>

<success_criteria>
- Playback and Volume degrade correctly under the per-platform clamp spec — pitch preserved, muted never touched, out-of-band values never written
- The clamp tables are frozen CONFIG data (LEVR-08); the platform seam is deterministic and testable
- Phase 3 now has 3 levers (saturation, playbackRate, volume) reconciled by the hub; LEVR-02/03/08 covered, LEVR-04/05 (autoplay/buffer) queued for 03-03
</success_criteria>

<output>
Create `.planning/phases/03-degradation-levers/03-02-SUMMARY.md` when done
</output>
