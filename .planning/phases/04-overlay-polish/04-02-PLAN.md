---
phase: 04-overlay-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - OVER-01
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The overlay subscribes to the existing bus via SlowGram.on — 'elapsed' (src/slowgram.js:425), 'phasechange' (:142), 'reset' (:483) — and re-renders ONLY from bus values (state.elapsedMs), never from a locally accumulated counter (D-3: stays aligned with the fatigue clock)"
    - "Re-render is value-throttled: the text node updates only when the FLOORED session minutes change (Math.floor(elapsedMs / 60000)) — inherently ≤1/s (OVER-01), zero timer APIs (Phase 1 ban, SCAN stays green)"
    - "The rendered text is exactly `{minutes} {unitLabel}` — floored minutes + CONFIG.overlay.unitLabel ('min'), single space, no seconds, no label prefix, no explanation text (D-5/D-6/D-7); the unit comes from CONFIG, never a literal (CORE-05)"
    - "The injected CSS declares font-variant-numeric: tabular-nums and white-space: nowrap so digit changes don't shift pill width (UI-SPEC typography contract); pill width is stable across updates"
    - "The first rendered value at appearance is the REAL session time — at phase 1 (elapsed ≈ 3:00) the text reads '3 min', never '0 min' (D-3)"
    - "On 'reset' (fatigue reset → phase 0) the overlay fades out (opacity → 0 via the CSS transition); it never shows a zeroed counter while hidden"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "emit('elapsed', state.elapsedMs) at src/slowgram.js:425 — the counter's data source (Pattern A: rides the bus, never re-derives from the DOM)"
    - "SlowGram.on (:1693) — the public subscription API the overlay uses; subscribers are PRESERVED across destroy/re-init (teardown contract :1420)"
    - "emit('reset') at :483 — the fade-out trigger; phaseMachine.sync(0) emits phasechange 0 as the backstop"
    - "CONFIG.overlay (wave 1) — unitLabel and fadeMs are read here, never re-declared"
  prohibitions:
    - statement: "No timer API anywhere in the overlay update path — the ≤1/s update is value-throttling on the rAF/elapsed stream, never setTimeout/setInterval (Phase 1 ban, SCAN green)."
      status: unverified
      flagged: true
    - statement: "The counter never re-derives elapsed from a local accumulator — all renders read the bus value (D-3); a locally counted time diverges from the fatigue clock."
      status: unverified
      flagged: true
    - statement: "No 'min' / seconds / label / explanation literal in module bodies — text is `{floored} {CONFIG.overlay.unitLabel}` only (D-5/D-6/D-7, CORE-05)."
      status: unverified
      flagged: true
---

<objective>
Wave 2: the counter's data flow — the invisible engine's clock becomes the visible "N min". The overlay subscribes to the existing bus (`elapsed`, `phasechange`, `reset`), renders floored session minutes + `CONFIG.overlay.unitLabel`, and throttles by value so the ≤1/s constraint (OVER-01) is satisfied without a single timer API.

Purpose: prove the data half of OVER-01 — the text is a faithful, throttled reflection of the fatigue clock. The wave-1 host is currently an empty pill; this wave gives it life.
Output: `src/slowgram.js` (bus subscription + value-throttled render + text node + reset fade-out), `test/slowgram.test.js` (T-O11..T-O16 data-flow suite + SCAN extensions).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overlay-polish/04-CONTEXT.md
@.planning/phases/04-overlay-polish/04-UI-SPEC.md
@.planning/phases/04-overlay-polish/04-RESEARCH.md
@.planning/phases/04-overlay-polish/04-VALIDATION.md
@.planning/phases/04-overlay-polish/04-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Bus subscription + value-throttled render — the ≤1/s counter, end-to-end</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 04-RESEARCH.md — Pattern A (rides the bus), Pattern B (no-timer 1/s = value-throttle the stream)
    - src/slowgram.js — emit(:1404)/SlowGram.on(:1693); emit('elapsed') call site (:425); emit('phasechange') (:142); emit('reset') (:483); the wave-1 overlay block (ensureOverlayHost/buildOverlayCss/overlaySync/_overlayState)
    - test/slowgram.test.js — the freshEnv pattern + how existing suites drive elapsed (advance + raf.flush)
  </action>
  Create the data-flow slice: one path — `phase 1 reached → host created → elapsed events flow → text reads "3 min" → minute boundary → "4 min" → reset → fade out`. Production-quality, committed for keeps.

  src/slowgram.js — extend the wave-1 overlay block:
  - Subscribe ONCE (module init, before the first init() — listeners are preserved across destroy/re-init by the teardown contract): `SlowGram.on('elapsed', onOverlayElapsed); SlowGram.on('phasechange', onOverlayPhase); SlowGram.on('reset', onOverlayReset);`. Contained handlers (try/catch — a subscriber error logs, never breaks the engine; emit already contains per-callback errors, but keep overlay handlers defensive anyway).
  - Module vars: `var overlayText = null;` (the text node), `var overlayLastMinutes = -1;` (the value-throttle latch).
  - `overlayMinutes(ms)` — pure: `Math.floor(ms / 60000)` (the ONLY place session ms become minutes; no literal drift).
  - `overlayRender()` — if no host yet, no-op. If `overlayShouldShow()`: `var mins = overlayMinutes(state.elapsedMs);` if `mins !== overlayLastMinutes` → set `overlayText.nodeValue = mins + ' ' + CONFIG.overlay.unitLabel; overlayLastMinutes = mins;` (value-throttle — the ≤1/s guarantee: minute boundaries are ≥60s apart, so at most one write per minute). Guard the text node exists (created in Task 2 of this plan; render no-ops until then — defensive).
  - `onOverlayElapsed(ms)` — `overlayRender()` (value-throttle inside; the write is skipped unless the minute changed).
  - `onOverlayPhase(next)` — if `overlayShouldShow()` and !overlayCreated → ensureOverlayHost + append (wave-1 sync path already does this — keep overlaySync as the single creation point, called from syncPhase after emit('phasechange'); onOverlayPhase only renders, creation stays in overlaySync). If `next < 1` → set fade-out marker (opacity 0 via the CSS transition — no timers).
  - `onOverlayReset()` — same fade-out path (fatigue reset → phase 0; the phasechange(0) backstop also fires — idempotent).
  - Fade mechanics (D-2): the host's injected CSS already declares `transition: opacity <fadeMs>ms`; show/hide toggles `host.style.opacity` between '0' and '1' (no timers — the CSS transition animates). The wave-1 host style must not conflict: the pill element inside the shadow root carries the transition; the host div carries pointer-events/z-index.
  - Test handle extension: `_overlayState()` gains `{ text: overlayText ? overlayText.nodeValue : null, lastMinutes: overlayLastMinutes }`.

  test/slowgram.test.js — T-O data-flow suite:
  - T-O11: real session time at first appearance (D-3) — freshEnv REELS, advance(180000) + flush → phase 1, host exists, `_overlayState().text === '3 min'` (never '0 min').
  - T-O12: minute-boundary update — continue to elapsed 210000 (still floor 3) → flush → text STILL '3 min' (throttled — no write on same minute); advance to 240000 → flush → text '4 min'.
  - T-O13: no local accumulation — the text tracks the BUS: after a reset (visibility hidden + advance > fatigueWindow + resume → resetSession → elapsed 0 → phase 0), `_overlayState().lastMinutes === -1` and text is cleared/null — the overlay never shows a counter while hidden, and a subsequent phase-1 re-entry re-renders from the fresh elapsed (e.g. after re-accumulating 180000 → '3 min' again).
  - T-O14: unit comes from CONFIG — flip CONFIG via a second init with a different config? NO — CONFIG is frozen; instead assert the text is built from getConfig().overlay.unitLabel by asserting `_overlayState().text` ends with ' ' + SlowGram.getConfig().overlay.unitLabel, and that the string 'min' appears in source ONLY inside initConfig (SCAN in Task 2). Here: assert text === '3 ' + unitLabel (contract shape: floored minutes, single space, unit — D-5/D-6).
  - T-O15: fade-out on reset — from phase 2 (elapsed 420000+, text '7 min'), force reset → host.style.opacity === '0' (fade-out marker set), text cleared; no timers involved (opacity is a CSS transition).
  - T-O16: subscriber containment — a throwing overlay handler must not break emit: register a throwing 'elapsed' subscriber AFTER the overlay, advance, flush → engine still healthy (getState().elapsedMs correct); console.error captured, no throw escapes.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The counter renders the REAL session time at appearance ('3 min' at phase 1) and tracks the bus, never a local counter (T-O11/T-O13)"
    - "Updates are value-throttled: no text write within the same minute; ≤1/s guaranteed structurally (T-O12)"
    - "Text is exactly `{floored minutes} {unitLabel}` with the unit from CONFIG.overlay (T-O11/T-O14)"
    - "Fatigue reset fades the overlay out (opacity 0, CSS transition, no timers) and clears the counter (T-O15)"
    - "Subscriber containment holds — a throwing overlay consumer never breaks the engine (T-O16)"
  </acceptance_criteria>
  <done>The counter lives: real session minutes render on the bus, throttle by value to ≤1/s with zero timers, and fade out on fatigue reset.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Text node + tabular-nums typography + SCAN proof — the stable pill</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 04-UI-SPEC.md — typography row (13px/500/1.4, tabular-nums), the populated/overflow/long-text UI-consideration rows (bounded copy, max-width 200px, no wrap)
    - src/slowgram.js — the wave-1 buildOverlayCss + Task 1's landed block
  </action>
  Complete the data flow: the text node creation, the typography contract, and the source-scan proof that the whole overlay honors CORE-05 and the no-timers ban.

  src/slowgram.js (additions/fixes only, if Task 1 left gaps):
  - In `ensureOverlayHost()`: create the text node (`env.document.createElement` is overkill — use `env.document.createTextNode` if the injected document provides it, else a plain `{ nodeValue: '', data: '' }` object; guard either way) inside the shadow pill element; initial `nodeValue = ''` — the first overlayRender() sets the real value.
  - buildOverlayCss additions (UI-SPEC typography contract): `font-variant-numeric: tabular-nums; white-space: nowrap;` (stable pill width across digit changes — the populated/overflow UI-consideration resolution), plus `overflow: hidden; text-overflow: clip;` behind the pill's max-width 200px (bounded copy — "999+ min" is ~7 chars; pathological overflow clips, never wraps, never affects host layout).
  - Ensure overlayRender's throttle latch resets on reset (overlayLastMinutes = -1) so a re-entry re-renders even at the same minute value (T-O13 covers the behavior).
  - No new timers, no new literals — SCAN below must pass.

  test/slowgram.test.js — typography + SCAN block:
  - T-O17: typography contract — the injected shadow CSS contains 'font-variant-numeric: tabular-nums', 'white-space: nowrap', 'max-width: 200px', and the 13px/500/1.4 font values (assert the concatenated CSS from the shadowRoot style element).
  - T-O18: width stability — render '3 min' then '4 min' (same character count, digit change): the text node value changes but the pill's CSS (tabular-nums + nowrap + fixed max-width) is unchanged — assert the style element textContent is byte-identical across the update (no per-update style churn).
  - T-O19 (SCAN, Node-only — extend the existing scan block): `'min'` appears in the engine source EXACTLY once (inside initConfig's CONFIG.overlay.unitLabel) — count occurrences of `'min'`; `2147483000` exactly once (zIndex); `rgba(12, 12, 14, 0.42)` exactly once; `#F5F5F7` exactly once. The no-timer scan (setTimeout/setInterval === 0) and the no-bare-Date.now scan must stay green with the overlay code present.
  - T-O20 (SCAN): the overlay functions contain no DOM-query API — extractBody('function overlayRender') and extractBody('function onOverlayElapsed') contain no 'querySelector' (records/state carry the data; the overlay never scans the DOM — Pattern A).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The pill renders the counter with tabular-nums + nowrap + bounded max-width — stable width across updates (T-O17/T-O18)"
    - "SCAN proves overlay values live only in CONFIG.overlay, no timer API entered the engine, and the overlay never queries the DOM (T-O19/T-O20)"
    - "node test/slowgram.test.js exits 0 with the full suite green"
  </acceptance_criteria>
  <done>The counter's data flow is complete and provable: bus-fed, value-throttled, stable-width, CONFIG-sourced, timer-free — ready for wave-3 context/fullscreen gating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine clock → overlay text | the rendered number must be the fatigue clock's truth; a locally accumulated counter diverges (D-3) and breaks the product's honesty |
| update cadence → page performance | the ≤1/s constraint (OVER-01) is structural — value-throttling, not best-effort; a timer-based update violates the Phase 1 ban |
| bus consumers → engine | a throwing overlay subscriber must never propagate through emit into the host frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | counter shows wrong time (local accumulator drift, stale minute latch) | high | mitigate | renders read state.elapsedMs only; throttle latch resets on reset; T-O11/T-O12/T-O13 lock it |
| T-04-02 | Denial of Service | timer-based update path (setInterval/setTimeout in overlay) | high | mitigate | value-throttle on the rAF/elapsed stream; SCAN forbids timer APIs (T-O19); the Phase 1 ban stays green |
| T-04-05 | Tampering | unit/label literal drift ('min' hardcoded, label prefix added) | medium | mitigate | text = `{floored} {CONFIG.overlay.unitLabel}`; SCAN: 'min' appears once, in initConfig (T-O19) |
| T-04-06 | Tampering | overlay DOM queries (scans the page for state) | low | mitigate | Pattern A: overlay reads bus/state only; extractBody scans prove zero querySelector (T-O20) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior 560 + T-O suite
- The ≤1/s guarantee is structural (minute-boundary throttle), proven by T-O12; the bus-truth proven by T-O11/T-O13
- CORE-05/no-timers proven by T-O19/T-O20 scans
</verification>

<success_criteria>
- The counter faithfully reflects the fatigue clock — real session minutes, value-throttled to ≤1/s with zero timers, unit from CONFIG, stable-width pill
- OVER-01's data half is locked; the wave-3 context/fullscreen gating (OVER-02/OVER-03) builds on the same bus
- Phase 4's visible face is honest, calm, and timer-free — matching the "sem culpa" core promise
</success_criteria>

<output>
Create `.planning/phases/04-overlay-polish/04-02-SUMMARY.md` when done
</output>
