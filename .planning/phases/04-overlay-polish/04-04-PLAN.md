---
phase: 04-overlay-polish
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - OVER-01
  - OVER-02
  - OVER-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "When the document becomes hidden (visibilitychange), the overlay hides — instantly (no fade); when it becomes visible again it fades back in (D-4) — presence aligns with the REELS && visible gate the clock already uses"
    - "destroy() removes the overlay host from the DOM entirely and clears the D-14 seam (overlayHost back to null); a later init() recreates a SINGLE host lazily on the next phase >= 1 (D-16) — no dead nodes after teardown, no duplicate hosts, matching the Phase 1 destroy contract"
    - "Overlay DOM writes never re-trigger the engine's MutationObserver — the D-14 seam exclusion (isInsideOverlayHost, src/slowgram.js:677-678) is PROVEN: mutation records targeting nodes inside the host subtree are skipped by the batch (no re-apply, no observer echo, no feedback loop)"
    - "Overlay lifecycle listeners (visibilitychange, fullscreenchange/webkitfullscreenchange) are registered in an overlay-handlers list removed on teardown — no listener stacking across init/destroy cycles (lifecycleHandlers precedent)"
    - "The overlay subscriptions to the bus (SlowGram.on) are preserved across destroy/re-init (teardown contract) — the counter re-renders correctly after a destroy→init cycle"
    - "node test/slowgram.test.js green with the full T-O suite; Edge headless harness.html shows TOTAL parity (Node count − NodeOnly scans == Edge count)"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "teardown() at src/slowgram.js:1420 — the single destroy path; overlay host removal + overlay-handlers cleanup hook in here; listeners preserved by design"
    - "SlowGram.destroy (:1710) / SlowGram.init (:1483) — the D-16 lifecycle contract"
    - "isInsideOverlayHost (:677-678) + the D-14 mutating-flag batch filter — the feedback-loop proof target"
    - "onHidden (:432) / onResume (:462) — state.visible transitions the overlay mirrors via its own visibilitychange listener (no new engine event needed)"
  prohibitions:
    - statement: "destroy() leaves no overlay host in the DOM and no overlay-handlers listener — teardown is clean (D-16); a hidden-but-present node after destroy is a regression."
      status: unverified
      flagged: true
    - statement: "Re-init after destroy never creates a second host — exactly one overlay exists at any time (D-16 single-instance)."
      status: unverified
      flagged: true
    - statement: "No overlay DOM write feeds back into the MutationObserver — the D-14 seam exclusion is the ONLY mechanism; a mutating-flag bypass in overlay code is a regression."
      status: unverified
      flagged: true
---

<objective>
Wave 4: the lifecycle edges that make the overlay bulletproof — tab-hide alignment (D-4), clean destroy/re-init (D-16), and the D-14 feedback-loop proof (overlay DOM writes never re-trigger the observer). Closes the phase with the two-host smoke.

Purpose: prove the overlay's end-state guarantees: it never lingers in a background tab, never survives teardown, never duplicates, and never talks back to the engine that hosts it.
Output: `src/slowgram.js` (visibilitychange wiring + overlay teardown hook + D-14 seam verification), `test/slowgram.test.js` (T-O31..T-O36 lifecycle suite + two-host smoke).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overlay-polish/04-CONTEXT.md
@.planning/phases/04-overlay-polish/04-UI-SPEC.md
@.planning/phases/04-overlay-polish/04-RESEARCH.md
@.planning/phases/04-overlay-polish/04-VALIDATION.md
@.planning/phases/04-overlay-polish/04-01-SUMMARY.md
@.planning/phases/04-overlay-polish/04-02-SUMMARY.md
@.planning/phases/04-overlay-polish/04-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: visibilitychange + destroy/re-init — D-4 and D-16 lifecycle, end-to-end</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 04-CONTEXT.md — D-4 (hide on hidden tab, fade on return), D-16 (destroy removes host, re-init recreates lazily)
    - src/slowgram.js — onHidden (:432) / onResume (:462) / teardown (:1420) / SlowGram.destroy (:1710) / SlowGram.init (:1483); the wave-1..3 overlay block (subscriptions, overlaySync, overlayHideInstant/overlayShow, _overlayState)
    - test/slowgram.test.js — the destroy/re-init suite precedent from Phase 1 (T-01-10 destroy + re-init idempotence) and the visibility driver (doc.setVisibility + dispatch visibilitychange)
  </action>
  Create the lifecycle slice: one path — `counter visible → doc hidden → overlay hidden instantly → doc visible → fades back → destroy() → host gone, seam null → re-init → phase 1 → single new host`. Production-quality, committed for keeps.

  src/slowgram.js — extend the overlay block:
  - Visibility listener: register `visibilitychange` on `env.document` in an `overlayHandlers` list (same shape as lifecycleHandlers). Handler: if `env.document.visibilityState === 'hidden'` → `overlayHideInstant()` (D-4 instant); if 'visible' → `overlayShow()` (fade in). No new engine event — the overlay listens directly (the engine's onHidden/onResume already gate the clock; the overlay mirrors via its own listener).
  - Teardown hook: in `teardown()` (the single destroy path), call `overlayTeardown()`: remove every overlayHandlers listener from env.document (visibilitychange, fullscreenchange, webkitfullscreenchange), remove the host node from its parent (`overlayHostEl.parentNode.removeChild(overlayHostEl)` if present), set `overlayHostEl = null; overlayCreated = false; overlayLastMinutes = -1; overlayText = null; overlayHost = null;` (D-16: clean slate; the D-14 seam clears with it). The BUS subscriptions are NOT removed (teardown preserves subscribers — they re-create the host on the next phase >= 1).
  - Re-init: since init() calls teardown() on an already-initialized engine, the overlay restarts clean; overlaySync (post-phasechange) recreates the single host on the next phase >= 1. No duplicate-host path: overlayCreated is the latch, and teardown resets it.
  - Ensure `overlaySync`'s creation path is reachable after re-init (nothing caches the old host).
  - Test handle extension: `_overlayState()` gains `{ parentNode: overlayHostEl ? overlayHostEl.parentNode : null }`.

  test/slowgram.test.js — lifecycle suite:
  - T-O31: hidden tab hides instantly — REELS at phase 2 with visible overlay ('7 min'); `e.doc.setVisibility('hidden')` + dispatch visibilitychange → `_overlayState().opacity === '0'` (instant — transition disabled).
  - T-O32: return fades back — setVisibility('visible') + dispatch visibilitychange → opacity '1', text still '7 min' (elapsed preserved by the engine's catch-up), single host.
  - T-O33: destroy removes host — at phase 2 with host present, `SlowGram.destroy()` → `_overlayState().hostExists === false`, `parentNode === null`, `_overlayState().created === false`, and the D-14 seam is cleared (assert via the exclusion helper: overlayHost is null — `_overlayState().seamRegistered === false`).
  - T-O34: re-init recreates single host — after destroy, `SlowGram.init(...)` fresh + REELS + advance(180000) + flush → phase 1 → exactly ONE host in document.body (count created hosts === 1), `_overlayState().text === '3 min'` (bus subscriptions survived — the counter re-renders post-re-init).
  - T-O35: destroy at phase 0 creates nothing — freshEnv REELS phase 0, destroy → no host ever existed, re-init + phase 0 → still none; destroy is idempotent (double destroy no throw).
  - T-O36: listener hygiene — after one init, doc has exactly one visibilitychange and one fullscreenchange (+ one webkitfullscreenchange if registered) listener from the overlay; after destroy → zero (no stacking across cycles).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Tab hidden → overlay gone instantly; tab visible → fades back with time preserved (T-O31/T-O32)"
    - "destroy() removes the host and clears the seam; re-init recreates a single host lazily (T-O33/T-O34)"
    - "No host at phase 0; destroy idempotent (T-O35)"
    - "No listener stacking across destroy/re-init (T-O36)"
  </acceptance_criteria>
  <done>The overlay's lifecycle is clean: aligned with tab visibility, fully removed on destroy, single-instance on re-init, and listener-leak-free.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-14 feedback-loop proof + two-host smoke — the phase-closing verification</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js — isInsideOverlayHost (:677-678), the batchCallback self-mutation filter, the D-14 mutating flag; the full overlay block as landed
    - test/slowgram.test.js — how Phase 3 suites connect a real observer (freshEnv with observer + record/recordAttributeMutation helpers) and the source-scan block tail
  </action>
  Close the phase: prove the feedback loop is dead (the overlay's own DOM writes can never re-trigger the observer), extend the scans for the wave-4 code, and run the two-host smoke.

  src/slowgram.js (additions/fixes only, if Task 1 left gaps):
  - Verify the D-14 integration end-to-end: when the overlay host exists and a mutation record's target is INSIDE the host subtree, the batch filter skips it via isInsideOverlayHost (no re-apply, no observer echo). The host was seam-registered at creation (wave 1); this task only confirms the exclusion path with real records.
  - The overlay's own style/text writes must NOT set the engine's mutating flag unless required — the seam exclusion is the mechanism; document with a comment that overlay writes are excluded by isInsideOverlayHost (never a mutating-flag bypass in overlay code).
  - Final scan discipline: ensure no wave-4 code added timers or literals.

  test/slowgram.test.js — D-14 proof + scans + smoke:
  - T-O37: observer connected, record targeting host subtree is skipped — freshEnv with a real FakeMutationObserver + root; register a video + apply a lever at phase 2 (saturation on wrapper) so the engine is live; create the overlay host (phase >= 1, REELS); record an attribute mutation whose target is a node INSIDE the host subtree; flush → the batch skips it: no lever re-apply, no registry churn (assert _liveRegistrySize()/_registrySize() unchanged and the lever's appliedLevers untouched).
  - T-O38: overlay render writes do not echo — while the observer is connected, drive the overlay's own update (elapsed boundary → text write inside the shadow root); flush → zero records processed from the host subtree (the observer received none for those writes); the engine's self-mutation filter + seam hold together.
  - T-O39 (SCAN, Node-only — extend the scan block): `overlayHost` appears in source at most at the var declaration + the seam assignment + the teardown clear (count `overlayHost =` assignments — exactly 2: creation + teardown-clear... assert the specific count that the implementation lands on; the POINT is: the seam is only ever set at creation and cleared at teardown — no third writer); and the wave-4 code added no setTimeout/setInterval and no new 'min'/zIndex/color literals (existing scans stay green).
  - T-O40: full-suite containment — with the complete overlay code present, `node test/slowgram.test.js` exits 0 with the prior 560 + full T-O suite (report the new total).

  Two-host smoke (run both, record both results in the summary):
  - Node: `node test/slowgram.test.js` — exit 0, all suites green.
  - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke4" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count (Node count − NodeOnly scans; the parity contract from Phases 1-3).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "The D-14 seam is proven: mutation records targeting the host subtree are skipped; overlay writes never echo back (T-O37/T-O38)"
    - "Scans prove the seam has exactly two writers (creation/teardown) and no wave-4 timer/literal drift (T-O39)"
    - "node test/slowgram.test.js exits 0 with the full suite green; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The overlay is feedback-loop-free and phase-complete: seam-proven, scan-clean, both hosts green with the full T-O suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tab visibility → overlay presence | a frozen counter on a hidden tab is noise; presence must mirror the clock's REELS && visible gate |
| destroy/re-init → DOM state | a dead host or stacked listeners after teardown breaks the Phase 1 destroy contract and leaks memory |
| overlay subtree → engine observer | the overlay's own mutations must never feed the MutationObserver batch (feedback loop / infinite apply) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Denial of Service | overlay DOM writes re-trigger the MutationObserver (feedback loop) | high | mitigate | D-14 seam exclusion proven with real records: host-subtree records skipped, no echo (T-O37/T-O38) |
| T-04-08 | Denial of Service | listener/host leak across destroy/re-init cycles | medium | mitigate | overlayHandlers removed on teardown; host removed + seam cleared (D-16); T-O33/T-O36 lock it |
| T-04-09 | Tampering | duplicate overlay instance after re-init (two pills) | medium | mitigate | overlayCreated latch reset only in teardown; T-O34 asserts exactly one host |
| T-04-10 | Spoofing | stale counter after re-init (bus subscription lost) | low | mitigate | bus subscriptions preserved by teardown contract; T-O34 re-renders post-re-init |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior 560 + full T-O suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- D-14 feedback-loop proof: T-O37/T-O38; lifecycle: T-O31..T-O36; scans: T-O39
</verification>

<success_criteria>
- The overlay's end-state guarantees hold: hidden with the tab, fully removed on destroy, single-instance on re-init, and provably unable to feedback into the engine
- Phase 4 is implementation-complete: OVER-01 (Shadow DOM, pointer-events none, z-index, ≤1/s), OVER-02 (social hidden), OVER-03 (fullscreen hidden) all locked by tests on both hosts
- The visible face of the product is neutral, honest, and invisible until it must speak — the "sem culpa" promise delivered in code
</success_criteria>

<output>
Create `.planning/phases/04-overlay-polish/04-04-SUMMARY.md` when done
</output>
