---
phase: 04-overlay-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
autonomous: true
requirements:
  - OVER-02
  - OVER-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "on SOCIAL/UNKNOWN context change the overlay hides INSTANTLY — no fade, no lingering frame (D-14, OVER-02 trust contract): the hide path for context sets host opacity to 0 with the transition disabled for that write (never a fading counter bleeding into a social page)"
    - "On return to REELS the overlay fades back in (opacity transition, ~400ms) with the current phase/time preserved — no re-creation, no time jump (D-14 second half)"
    - "The overlay NEVER appears on preservedRoutes (/direct/, /messages/, /p/, /explore/, /accounts/, /stories/) or any UNKNOWN context — the predicate's REELS gate is the single source of truth (reuses classifyContext, does not re-derive)"
    - "The overlay is hidden whenever a live registered video has webkitDisplayingFullscreen === true OR document.fullscreenElement / document.webkitFullscreenElement is set (OVER-03, D-15) — fullscreen entry hides INSTANTLY, exit fades back in"
    - "Fullscreen detection is poll-free and timer-free: the fullscreen predicate is checked on the rAF/elapsed carrier + a fullscreenchange/webkitfullscreenchange listener (RESEARCH Pattern D) — no new timers (Phase 1 ban)"
    - "Picture-in-Picture needs NO handling — the PiP window is a separate DOM the page cannot render into; the overlay naturally is not there (D-15)"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
  key_links:
    - "emit('contextchange', context) at src/slowgram.js:1673 — the overlay's instant-hide trigger; setContext SOCIAL/UNKNOWN also reverts degradation (Phase 3 trust contract — the overlay rides the same gate)"
    - "classifyContext + CONFIG.preservedRoutes (Phase 2) — the REELS/SOCIAL/UNKNOWN verdict the predicate reuses; never re-derived"
    - "state.context / state.phase — the predicate inputs (getState contract)"
    - "The live registry (registryElements / videoStates) — fullscreen check iterates registered videos for webkitDisplayingFullscreen (or reads document.fullscreenElement — cheaper, no iteration)"
  prohibitions:
    - statement: "The overlay never lingers on a SOCIAL/UNKNOWN route — instant hide with transition disabled; a fading counter visible on social is a trust-contract violation (D-14)."
      status: unverified
      flagged: true
    - statement: "No overlay element is ever visible while webkitDisplayingFullscreen or document.fullscreenElement is set (OVER-03) — fullscreen entry hides instantly."
      status: unverified
      flagged: true
    - statement: "No timer-based fullscreen polling — the check rides the rAF/elapsed carrier and the fullscreenchange listener only (Phase 1 ban)."
      status: unverified
      flagged: true
---

<objective>
Wave 3: the trust-contract gates — OVER-02 (hidden on social routes) and OVER-03 (hidden during video fullscreen). The counter must never be seen on a social page for even one frame, and must vanish the instant the user goes fullscreen.

Purpose: prove the two hiding requirements that make the overlay trustworthy — the "never on social" promise from Phase 2 and the immersive-fullscreen cleanliness from OVER-03 — with instant-hide semantics and poll-free, timer-free detection.
Output: `src/slowgram.js` (context-change instant hide/fade-in + fullscreen predicate + fullscreenchange listener), `test/slowgram.test.js` (T-O21..T-O26 gating suite).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overlay-polish/04-CONTEXT.md
@.planning/phases/04-overlay-polish/04-UI-SPEC.md
@.planning/phases/04-overlay-polish/04-RESEARCH.md
@.planning/phases/04-overlay-polish/04-VALIDATION.md
@.planning/phases/04-overlay-polish/04-01-SUMMARY.md
@.planning/phases/04-overlay-polish/04-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Context gating — instant hide on SOCIAL/UNKNOWN, fade in on REELS return</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 04-CONTEXT.md — D-14 (instant hide on SOCIAL/UNKNOWN, fade in on REELS return)
    - src/slowgram.js — emit('contextchange') (:1673) + setContext; classifyContext (Phase 2); the wave-2 overlay block (subscriptions, overlayRender, fade markers)
    - test/slowgram.test.js — how Phase 3 suites drive context (SlowGram.setContext('SOCIAL'), pushState to /direct/)
  </action>
  Create the context-gating slice: one path — `counter visible on REELS at phase 2 → pushState /direct/ → contextchange SOCIAL → host opacity 0 INSTANTLY (no transition) → pushState /reels/ → contextchange REELS → fades back with phase/time preserved`. Production-quality, committed for keeps.

  src/slowgram.js — extend the overlay block:
  - Extend the wave-1 predicate: `overlayShouldShow()` gains the context gate — `state.context === 'REELS' && state.phase >= 1 && <visibility> && <fullscreen>` where the last two clauses are stubs joining in Task 2/wave 4 (single boolean function; wave 3 fills the REELS gate if not already explicit).
  - Subscribe: `SlowGram.on('contextchange', onOverlayContext);` (contained handler).
  - `onOverlayContext(context)` — if context !== 'REELS' (SOCIAL or UNKNOWN): `overlayHideInstant()` — set host opacity to '0' with the transition DISABLED for this write (`host.style.transition = 'none'` then opacity 0, then restore the transition on the next frame/carrier — the CSS transition from CONFIG.overlay.fadeMs must not animate this hide). If context === 'REELS' and overlayCreated: `overlayShow()` — opacity '1' (the existing transition animates the fade-in, ~400ms).
  - `overlayHideInstant()` — the instant path (D-14): transition off → opacity 0. `overlayShow()` — opacity 1 (transition on). Both no-ops when no host exists.
  - Keep the fade-out on phase drop / reset from wave 2 as the FADED path (D-2) — context is the only INSTANT path (D-14). The distinction is the transition flag.
  - Test handle extension: `_overlayState()` gains `{ opacity: host ? host.style.opacity : null, instant: <whether the last hide was instant> }` (or expose the transition-disabled flag).

  test/slowgram.test.js — context gating suite:
  - T-O21: instant hide on SOCIAL — freshEnv REELS, advance to phase 2 (elapsed 420000+), flush → host visible (opacity '1', text '7 min'); `SlowGram.setContext('SOCIAL')` (or pushState /direct/ + flush) → contextchange fires → `_overlayState().opacity === '0'` AND the hide was instant (transition was disabled for the write — assert the state's instant flag or that host.style.transition was 'none' at write time).
  - T-O22: never on preserved routes — for EACH of CONFIG.preservedRoutes (/direct/, /messages/, /p/, /explore/, /accounts/, /stories/): freshEnv at phase 3 (overlay created on REELS first, then route change) → after context change the host opacity is '0'; and a fresh session starting ON a preserved route never creates the host (hostExists false).
  - T-O23: UNKNOWN context never shows — location '/some-unclassified-path' at phase 3 → hostExists false; and a contextchange to UNKNOWN hides an existing host instantly.
  - T-O24: fade back in on REELS return — from T-O21's state (hidden on SOCIAL, phase 2 preserved), pushState /reels/ + flush → contextchange REELS → `_overlayState().opacity === '1'` and text STILL '7 min' (phase/time preserved — no re-creation, no time jump; assert hostExists still true, single host).
  - T-O25: contextchange containment — a throwing onOverlayContext consumer never breaks setContext/emit (engine healthy after, getState().context correct).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "SOCIAL/UNKNOWN context change hides the overlay instantly — no fading frame on a social route (T-O21/T-O23)"
    - "No overlay on any preserved route, from either direction (T-O22)"
    - "Return to REELS fades the counter back in with phase/time preserved, single host, no time jump (T-O24)"
    - "Containment holds — a throwing context subscriber never breaks the engine (T-O25)"
  </acceptance_criteria>
  <done>The trust contract holds: the counter vanishes instantly on any social/unknown surface and returns gracefully on REELS — never seen where it must not be seen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fullscreen gating — OVER-03 instant hide, poll-free detection</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - 04-RESEARCH.md — Pattern D (webkitDisplayingFullscreen is canonical; document.fullscreenElement unsupported on iOS — WebKit bug 149386)
    - 04-CONTEXT.md — D-15 (instant hide on fullscreen entry, fade in on exit; PiP needs nothing)
    - src/slowgram.js — the live registry (registryElements / videoStates) for iterating videos; the wave-2/3 overlay block
  </action>
  Complete the OVER-03 gate: fullscreen detection via the canonical iOS property + the document fullscreen API, poll-free and timer-free.

  src/slowgram.js — extend the overlay block:
  - `overlayIsFullscreen()` — reads `env.document.fullscreenElement || env.document.webkitFullscreenElement` (desktop/standard API) and, as the iOS-canonical check, iterates the live registry: any registered VIDEO with `webkitDisplayingFullscreen === true`. Cheap reads only; returns boolean. (The iteration reuses registryElements — the same pruned live list Phase 3 iterates; bounded.)
  - Extend `overlayShouldShow()` with the fullscreen clause: `... && !overlayIsFullscreen()`.
  - Listener: `fullscreenchange` (and `webkitfullscreenchange` for Safari) on `env.document` — registered in a small overlay-handlers list mirroring the lifecycleHandlers pattern (removed on teardown, wave 4) so re-init doesn't stack listeners. Handler: `overlaySync()` — on fullscreen entry the predicate flips false → `overlayHideInstant()` (D-15 instant); on exit → `overlayShow()` (fade in).
  - Also call `overlaySync()` from the existing rAF/elapsed carrier (overlayRender path) so the predicate is re-evaluated without polling — the webkitDisplayingFullscreen property is checked on each carry, catching fullscreen changes the event missed (WebView variance).
  - PiP: NO code. D-15 states the PiP window is a separate DOM; the plan adds zero PiP handling (a comment documenting the decision is acceptable).
  - No new timers, no new literals — SCAN stays green.

  test/slowgram.test.js — fullscreen suite:
  - T-O26: webkitDisplayingFullscreen hides instantly — REELS at phase 2 with visible overlay ('7 min'); set a registered video's `webkitDisplayingFullscreen = true`; trigger the carrier (flush) → `_overlayState().opacity === '0'` and the hide was instant (transition disabled).
  - T-O27: document.fullscreenElement hides — freshEnv where `doc.fullscreenElement` is a truthy fake element; advance to phase 2 → overlay created BUT `_overlayState().opacity === '0'` (never visibly shown while fullscreen); clear fullscreenElement + flush → fades to '1' with correct text.
  - T-O28: exit fullscreen fades back — from T-O26's state, set webkitDisplayingFullscreen = false, dispatch fullscreenchange on doc, flush → opacity '1', text still '7 min' (preserved), single host.
  - T-O29: PiP no-handling — with a video in the registry (no fullscreen flags set), PiP state is a no-op by design: assert no crash and no new listeners beyond the fullscreenchange pair (count doc listeners of type fullscreenchange/webkitfullscreenchange === 1 each after one init).
  - T-O30: no timers for fullscreen — SCAN block: no setTimeout/setInterval anywhere (stays green); extractBody('function overlayIsFullscreen') contains no querySelector (reads state/registry only, no DOM scan).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "Fullscreen entry (webkitDisplayingFullscreen OR document.fullscreenElement) hides the overlay instantly — OVER-03 (T-O26/T-O27)"
    - "Exit fades the counter back with phase/time preserved (T-O28)"
    - "Detection is poll-free and timer-free — rAF/elapsed carrier + fullscreenchange listener, no polling (T-O30)"
    - "PiP needs no handling and adds no listeners (T-O29)"
  </acceptance_criteria>
  <done>OVER-03 is locked: the counter is never visible over fullscreen video, with canonical WebKit detection, zero polling, zero timers, and PiP correctly untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route verdict → overlay presence | SOCIAL/UNKNOWN must hide the counter instantly — a lingering fade violates the Phase 2 trust contract |
| fullscreen state → overlay presence | video takeover must be clean; a counter over fullscreen breaks OVER-03 and the immersive promise |
| DOM fullscreen signals → engine | fullscreen detection must not become a polling loop or timer (Phase 1 ban) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Spoofing | overlay visible on SOCIAL/UNKNOWN (fading linger, route leak) | high | mitigate | contextchange → instant hide with transition disabled (D-14); predicate REELS gate; T-O21/T-O22/T-O23 lock it |
| T-04-07 | Tampering | overlay visible during fullscreen (clean-takeover violation) | high | mitigate | fullscreen clause in the predicate + instant hide on entry; T-O26/T-O27/T-O28 lock it |
| T-04-02 | Denial of Service | fullscreen polling loop or timer (perf) | medium | mitigate | event-driven (fullscreenchange) + rAF/elapsed carrier checks only; SCAN forbids timers (T-O30) |
| T-04-08 | Denial of Service | duplicate fullscreen listeners across init/destroy cycles | medium | mitigate | overlay-handlers list removed on teardown (wave 4); T-O29 asserts exactly one listener pair |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior 560 + T-O suite
- OVER-02 proven by T-O21/T-O22/T-O23; OVER-03 proven by T-O26/T-O27/T-O28
- No-timer/no-poll proven by T-O30 scans; listener hygiene by T-O29
</verification>

<success_criteria>
- The overlay is trustworthy: instant-vanishing on social/unknown routes and over fullscreen video, fading back only where it belongs
- OVER-02 and OVER-03 are locked by tests; the wave-4 lifecycle edges (visibilitychange, destroy/re-init, D-14 seam proof, two-host smoke) close the phase
</success_criteria>

<output>
Create `.planning/phases/04-overlay-polish/04-03-SUMMARY.md` when done
</output>
