---
phase: 04-overlay-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 03-04
files_modified:
  - src/slowgram.js
  - test/slowgram.test.js
  - test/harness.js
autonomous: true
requirements:
  - OVER-01
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "CONFIG.overlay exists in initConfig (deep-frozen with the rest of CONFIG) and holds unitLabel:'min', zIndex: 2147483000, fadeMs: 400, and a pill block with the UI-SPEC geometry (left 16px, bottom 16px, padding 8px 12px, radius 8px, maxWidth 200px, fontSize 13px, fontWeight 500, lineHeight 1.4, background rgba(12,12,14,0.42), color #F5F5F7) — every user-facing overlay value is in CONFIG, none in a module body (CORE-05)"
    - "The overlay host is created LAZILY: no host node exists in the document until the first time the visibility predicate (context === 'REELS' && phase >= 1 && document visible && !fullscreen) becomes true (D-12, D-1); before first degradation `_overlayState().hostExists === false`"
    - "The host is a real DOM element with attachShadow({mode:'open'}) — the injected <style> inside the shadow root carries position:fixed, pointer-events:none, z-index from CONFIG.overlay.zIndex, and the full pill style set (all values concatenated from CONFIG.overlay.pill, never literals)"
    - "pointer-events:none is never re-enabled anywhere in the overlay (D-13) — the host and pill are strictly non-interactive; no 'pointer-events: auto' string exists in the engine"
    - "The created host is registered with the D-14 overlayHost seam (the module var at src/slowgram.js:98) so the engine's self-mutation batch filter excludes its subtree (isInsideOverlayHost at :677-678)"
    - "node test/slowgram.test.js green with the new T-O suite; harness.js gains additive FakeShadowRoot/attachShadow + document.createElement/body support so the real shadow-DOM construction path runs deterministically on BOTH hosts"
  artifacts:
    - src/slowgram.js
    - test/slowgram.test.js
    - test/harness.js
  key_links:
    - "overlayHost seam var at src/slowgram.js:98 and isInsideOverlayHost exclusion check at :677-678 — the host registers here at creation"
    - "phaseMachine.sync → emit('phasechange', next) at :142 — the overlay's appear trigger (next >= 1)"
    - "getState() exposes { phase, context, visible, elapsedMs } — the predicate's inputs (never re-derive from the DOM)"
    - "FakeDocument lacks createElement/body and FakeElement lacks attachShadow today — harness.js must be extended additively (precedent: every prior phase added fakes without breaking existing suites)"
  prohibitions:
    - statement: "No overlay host node exists in the document before the first phase >= 1 on REELS — lazy creation (D-12); an inert pre-created host is a regression."
      status: unverified
      flagged: true
    - statement: "No overlay value (zIndex, colors, sizes, fadeMs, unitLabel) appears as a literal in a module body — all come from CONFIG.overlay (CORE-05)."
      status: unverified
      flagged: true
    - statement: "No interactive overlay surface — pointer-events:none is never re-enabled (D-13); no click/tap handlers are ever attached to the host or pill."
      status: unverified
      flagged: true
---

<objective>
Wave 1 tracer of the Phase 4 overlay spine: the frozen `CONFIG.overlay` block (CORE-05), the lazy host factory (`ensureOverlayHost`) building a Shadow DOM pill with the approved UI-SPEC look, the visibility predicate core (REELS && phase >= 1), and the D-14 overlayHost seam registration — plus the additive harness fakes (FakeShadowRoot / attachShadow / document.createElement+body) that let the real construction path run on both hosts.

Purpose: prove the architectural spine — CONFIG → lazy host creation → shadow pill injection → seam registration — end-to-end before the data flow (wave 2), context/fullscreen gating (wave 3), and lifecycle edges (wave 4) layer on top. A dead end in attachShadow handling, style injection, or lazy-creation timing costs one commit, not four.
Output: `src/slowgram.js` (CONFIG.overlay + ensureOverlayHost + visibility predicate + seam registration + `_overlayState` test handle), `test/harness.js` (additive shadow/DOM fakes), `test/slowgram.test.js` (T-O01..T-O06 tracer suite).
</objective>

<execution_context>
@C:/Users/Usuario/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Usuario/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overlay-polish/04-CONTEXT.md
@.planning/phases/04-overlay-polish/04-UI-SPEC.md
@.planning/phases/04-overlay-polish/04-RESEARCH.md
@.planning/phases/04-overlay-polish/04-VALIDATION.md
@.planning/phases/03-degradation-levers/03-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: CONFIG.overlay + host factory + D-14 seam — the lazy Shadow DOM pill, end-to-end</name>
  <files>src/slowgram.js, test/harness.js, test/slowgram.test.js</files>
  <read_first>
    - 04-RESEARCH.md — Pattern A (bus rider), Pattern E (CONFIG.overlay shape), the Shadow DOM + stacking notes, pitfalls (z-index needs positioned element; pointer-events leakage)
    - 04-UI-SPEC.md — the exact pill contract (spacing 8/12/16, typography 13px/500/1.4, colors rgba(12,12,14,0.42)/#F5F5F7, geometry)
    - 04-CONTEXT.md — D-1, D-8, D-10, D-11, D-12, D-13 (appear-at-phase>=1, CONFIG.overlay, dark pill, z-index, lazy, no dismissal)
    - src/slowgram.js — initConfig (320-367) for the CONFIG block shape; overlayHost var (:98) + isInsideOverlayHost (:677-678); the existing test-handle pattern (_getWatcherState :1568, _setOverlayHostForTest :1591)
    - test/harness.js — FakeDocument (30-58, no createElement/body today), FakeElement (160-209, no attachShadow today)
  </action>
  Create the tracer slice: one path — `init → REELS → advance(180000) → flush → phasechange(1) → predicate true → host created with shadow pill → seam registered`. Production-quality, committed for keeps.

  src/slowgram.js — extend the IIFE (same file, same idioms, closure-private):
  - CONFIG addition in initConfig (deep-freeze covers it; no other CONFIG change): `overlay: { unitLabel: 'min', zIndex: 2147483000, fadeMs: 400, pill: { position: 'fixed', left: '16px', bottom: '16px', padding: '8px 12px', borderRadius: '8px', maxWidth: '200px', fontSize: '13px', fontWeight: '500', lineHeight: '1.4', background: 'rgba(12, 12, 14, 0.42)', color: '#F5F5F7' } }` — the UI-SPEC contract verbatim (D-8/D-10/D-11).
  - Module vars (near overlayHost :98): `var overlayHostEl = null;` (the host node), `var overlayCreated = false;` (lazy-creation latch per init cycle).
  - `overlayShouldShow()` — the predicate CORE for wave 1: `return state.context === 'REELS' && state.phase >= 1;` (visibility/fullscreen clauses join in waves 3/4 — single function, one boolean).
  - `ensureOverlayHost()` — create via `env.document.createElement('div')`; guard attachShadow availability (`typeof host.attachShadow === 'function'`, else skip silently — never break the host); `host.attachShadow({ mode: 'open' })`; set `host.style.cssText` minimal (position: fixed is set via the injected shadow style; the host itself carries only `pointer-events: none; z-index: <CONFIG.overlay.zIndex>;` from CONFIG — concatenated, no literals); set `overlayHost = host;` (the D-14 seam — the self-mutation filter at :677-678 now excludes the subtree); `overlayHostEl = host; overlayCreated = true;` return host.
  - `buildOverlayCss()` — returns the injected <style> textContent: `position: fixed; left/bottom/padding/border-radius/max-width/font-size/font-weight/line-height/background/color` ALL concatenated from `CONFIG.overlay.pill`, plus `pointer-events: none; z-index: <zIndex>;` and `transition: opacity <fadeMs>ms;` (fade base — D-2; the actual show/hide wiring is Task 2). CSS property NAMES are structural (same as Phase 3's `style.filter` writes); VALUES come from CONFIG only.
  - Append the style element: `var style = env.document.createElement('style'); style.textContent = buildOverlayCss();` then shadowRoot.appendChild(style). The text node ("N min") is created in wave 2 — wave 1 renders an empty pill.
  - Wire into the lifecycle: after `emit('phasechange', next)` in syncPhase, call `overlaySync()` — if `overlayShouldShow()` and !overlayCreated → `ensureOverlayHost()` and append to `env.document.body` (guard body exists). Guard everything in try/catch — a failure logs, never breaks the host (engine containment contract).
  - `_overlayState()` test handle (precedent: _getWatcherState/_registrySize): returns `{ hostExists: !!overlayHostEl, created: overlayCreated, seamRegistered: overlayHost === overlayHostEl, bodyAppended: !!overlayHostEl && overlayHostEl.parentNode !== null }`.

  test/harness.js — additive only (existing suites untouched):
  - FakeDocument: add `body` (a FakeElement('div')) and `createElement(tag)` returning a FakeElement — when the engine calls document.createElement('div') + appendChild, the fake tree tracks it. Guard: only add if absent.
  - FakeElement: add `attachShadow(init)` returning a FakeShadowRoot — `{ mode: init.mode, host: el, children: [], styleSheets: [], appendChild(child){ this.children.push(child); child.parentNode = this; return child; } }` — the engine's shadowRoot.appendChild(styleEl) + styleEl.textContent writes are recorded so tests assert the injected CSS.

  test/slowgram.test.js — T-O tracer suite (appended after the Phase 3 suite):
  - T-O01: CONFIG.overlay exists, frozen (getConfig), unitLabel 'min', zIndex 2147483000, fadeMs 400, pill block matches UI-SPEC geometry (assert via getConfig — never literals in the engine; the TEST may assert against the spec values).
  - T-O02: lazy creation — freshEnv REELS, advance(179999) + flush → phase 0, `_overlayState().hostExists === false` (D-12: nothing before first degradation).
  - T-O03: created at first phase >= 1 — advance to 180000 + flush → phase 1, `hostExists === true`, `bodyAppended === true`, host has a shadowRoot, `seamRegistered === true` (D-14).
  - T-O04: never on SOCIAL — setContext('SOCIAL'), advance to phase 3 (elapsed ≥ 12 min) → `hostExists === false` (predicate REELS-gate holds).
  - T-O05: injected CSS contract — after creation, read the host's shadowRoot style element textContent: contains 'position: fixed', 'pointer-events: none', 'z-index: 2147483000', the pill colors/sizes (rgba(12, 12, 14, 0.42), #F5F5F7, 8px 12px, 13px…), and 'transition: opacity 400ms' (D-2 base).
  - T-O06: no pointer-events re-enable (D-13) — the full engine source contains no 'pointer-events: auto' and the injected CSS contains 'pointer-events: none' exactly once.
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "CONFIG.overlay is frozen with the UI-SPEC contract; no overlay value appears as a module-body literal (T-O01)"
    - "No host node exists before first degradation on REELS; host appears exactly at phase >= 1 (T-O02/T-O03)"
    - "Host is a real shadow-DOM element with the full injected pill style; seam-registered for D-14 exclusion (T-O03/T-O05)"
    - "Strictly non-interactive — pointer-events none, no re-enable anywhere (T-O06)"
    - "All prior suites (560 assertions) stay green; new T-O suite adds ~6+ assertions; harness fakes are additive"
  </acceptance_criteria>
  <done>The overlay spine is proven: frozen CONFIG.overlay → lazy shadow-host creation at first phase >= 1 on REELS → D-14 seam registered, with the UI-SPEC pill injected via CONFIG-built CSS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: overlaySync wiring — appear/fade base + containment — and the two-host determinism check</name>
  <files>src/slowgram.js, test/slowgram.test.js</files>
  <read_first>
    - src/slowgram.js — the syncPhase emit site (:142), the teardown/init contract (:1420/:1483), and Task 1's landed block
    - 04-CONTEXT.md — D-2 (fade ~400ms via CSS transition, no timers)
  </read_first>
  <action>
    Complete the wave-1 wiring: the appear path is fully observable and contained, and the same suite runs identically on both hosts.

    src/slowgram.js (additions/fixes only):
    - `overlaySync()` must be idempotent: called from syncPhase after emit('phasechange') — with phase >= 1 on REELS it creates the host once (latch) and, for wave 1, sets the host's base visibility (opacity 1 via a 'visible' marker the wave-2 text renderer will reuse); with phase 0 or non-REELS it must NOT create and must not error. No timers — the fade rides the CSS transition (D-2).
    - Containment: every overlay function body (ensureOverlayHost, buildOverlayCss, overlaySync, and the future update path) is try/catch-contained — a failure console.errors and never escapes into the host frame (engine containment contract from Phase 1).
    - Ensure `_overlayState()` is exported with a doc comment following the _getWatcherState precedent, and that `overlayCreated` resets on destroy/teardown so a re-init after teardown can create a fresh single host (D-16 contract — the full destroy path lands in wave 4; the latch reset is the precondition).
    - No new timers: the wave-1 code adds zero setTimeout/setInterval — the SCAN block must stay green.

    test/slowgram.test.js (edge coverage for the wiring):
    - T-O07: idempotence — at phase 2, flush repeatedly → exactly one host in document.body (body children count of created hosts === 1); overlayCreated latch prevents duplication.
    - T-O08: no creation on phase drop — from phase 2, force fatigue reset (visibility hidden + advance > window + resume) → phase 0 → hostExists stays true (host persists for wave-3/4 fade-out; creation is one-way) but `_overlayState().created` remains true — no new host.
    - T-O09: containment — a document.createElement throwing in a hostile env: freshEnv with a createElement stub that throws → init + advance to phase 1 does NOT throw (overlay failure swallowed); engine still reports phase 1 via getState.
    - T-O10: SCAN additions (Node-only) — extend the existing scan block: assert the overlay's user-facing values appear exactly once each in source, inside initConfig: `2147483000` (zIndex), `'min'` (unitLabel — count of `'min'` occurrences === 1), `rgba(12, 12, 14, 0.42)` and `#F5F5F7` (pill colors) — a second occurrence anywhere means value drift (CORE-05). The timer scan (no setTimeout/setInterval) must stay green with the overlay code present.

    Two-host smoke (run both, record both results in the summary):
    - Node: `node test/slowgram.test.js` — exit 0, all suites green (report the new total).
    - Edge headless harness.html: `& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\eco-edge-smoke4" --dump-dom "file:///C:/Users/Usuario/Downloads/EcoInsta/test/harness.html" 2>$null | Out-String` then assert the output contains `TOTAL: N passed / N run` with N equal to the browser-host count (Node count − NodeOnly scans; the parity contract from Phases 1-3).
  </action>
  <verify>
    <automated>node test/slowgram.test.js</automated>
  </verify>
  <acceptance_criteria>
    - "overlaySync is idempotent and containment-safe: repeated flushes create one host; a throwing DOM never breaks the engine (T-O07/T-O09)"
    - "Phase drop leaves the single host in place for later fade-out wiring; the creation latch resets on teardown (T-O08)"
    - "SCAN proves overlay values live only in CONFIG.overlay and no timer API entered the engine (T-O10)"
    - "node test/slowgram.test.js exits 0 with the full suite green; Edge headless harness.html shows TOTAL parity"
  </acceptance_criteria>
  <done>The wave-1 spine is complete and contained: single lazy host, seam-registered, UI-SPEC pill injected from CONFIG, no timers, both hosts green — ready for the wave-2 data flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine phase/context state → overlay visibility | the predicate must read state only (never the DOM); a wrong predicate shows the counter during innocent early minutes or on social routes |
| injected DOM writes → host page | host/style creation mutates the page; a failure must log, never break the host (containment) |
| overlay subtree → engine observer | the host's own DOM must never feed back into the MutationObserver batch (D-14 seam) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | overlay visible outside REELS phase >= 1 (leak during innocent minutes / non-REELS) | high | mitigate | predicate core reads state only: context === 'REELS' && phase >= 1; lazy creation latch; T-O02/T-O03/T-O04 lock it |
| T-04-02 | Denial of Service | overlay DOM writes re-trigger the MutationObserver (feedback loop) | medium | mitigate | D-14 seam registration at creation (isInsideOverlayHost :677-678); full proof lands in wave 4 (T-O23) |
| T-04-03 | Tampering | overlay value drift (magic literals — zIndex/colors/unitLabel) | low | mitigate | values only in CONFIG.overlay; T-O10 source scan forbids duplicates |
| T-04-04 | Tampering | interactive overlay surface (pointer-events re-enabled, tap handler attached) | medium | mitigate | pointer-events:none everywhere, never re-enabled; T-O06 scan; no listener APIs in overlay code |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new packages this phase — no install task, no [ASSUMED]/[SUS] packages to vet |
</threat_model>

<verification>
- All tasks green: `node test/slowgram.test.js` exits 0 with the prior 560 + new T-O suite
- Edge headless smoke: `TOTAL: N passed / N run` in harness.html dump (parity with Node − NodeOnly scans)
- Lazy creation proven by T-O02/T-O03; predicate REELS-gate by T-O04; seam by T-O03; containment by T-O09
- CORE-05 proven by T-O01/T-O10 (values only in CONFIG.overlay)
</verification>

<success_criteria>
- The overlay's architectural spine is proven end-to-end: frozen CONFIG → lazy shadow-host at first phase >= 1 on REELS → D-14 seam registration → UI-SPEC pill injected, contained, timer-free
- OVER-01's structural half (Shadow DOM, pointer-events none, z-index above click catchers) is locked by tests; the data half (≤1/s update) is queued for wave 2
- Phase 4 opens with a validated spine; the wave-2 data flow, wave-3 context/fullscreen gating, and wave-4 lifecycle edges build on it
</success_criteria>

<output>
Create `.planning/phases/04-overlay-polish/04-01-SUMMARY.md` when done
</output>
