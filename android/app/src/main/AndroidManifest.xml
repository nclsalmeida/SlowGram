<?xml version="1.0" encoding="utf-8"?>
<!--
  SlowGram — Android wrapper manifest.

  Permissions (minimal, requested at the moment of use — never up front):
  - INTERNET: always (the app IS an Instagram Web host).
  - CAMERA / RECORD_AUDIO: only when the user uploads a photo/video or the
    Instagram page asks for the camera/mic (WebRTC). Runtime-gated.
  - READ_MEDIA_IMAGES / READ_MEDIA_VIDEO (33+) or READ_EXTERNAL_STORAGE
    (maxSdkVersion 32): legacy gate for the media picker / camera capture;
    the modern picker (SAF) itself needs no storage permission.
  No location, contacts, notifications, overlay, accessibility, or analytics.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Camera/mic are optional features: the app installs and runs fine
         (feed, DMs, Reels) on devices without them. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:usesCleartextTraffic="false">

        <!-- Serves camera-capture output files (cache/capture/) to the camera
             app via content:// — file:// Uris are forbidden on API 24+. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:enableOnBackInvokedCallback="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- Launcher shortcut: long-press icon -> Settings & privacy -->
        <meta-data
            android:name="android.app.shortcuts"
            android:resource="@xml/shortcuts" />
    </application>

</manifest>
